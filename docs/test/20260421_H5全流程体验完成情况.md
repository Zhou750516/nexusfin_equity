# 2026-04-21 H5 全流程体验完成情况

## 1. 文档目的

本文档用于沉淀 `2026-04-21` 本轮 H5 页面全流程体验过程中已经完成的事项、当前仍未完成的事项，以及本轮核对结论，便于后续继续联调、问题收口和任务衔接。

本轮范围主要包括：

1. 本地联调环境拉起；
2. H5 页面主流程体验；
3. 后端日志检查；
4. MySQL 落库一致性核对。

---

## 2. 已完成

### 2.1 本地联调环境已拉起

本轮已确认并实际拉起以下本地环境：

| 组件 | 状态 | 说明 |
|---|---|---|
| H5 前端 | 已完成 | 本项目 H5 实际运行在 `http://127.0.0.1:3001/` |
| Java 后端 | 已完成 | 本地后端运行在 `http://127.0.0.1:8080` |
| 科技平台用户信息 stub | 已完成 | 本地 stub 运行在 `http://127.0.0.1:18080/api/users/me` |
| 云卡 gateway stub | 已完成 | 本地 stub 运行在 `http://127.0.0.1:18081/api/gateway/proxy` |
| MySQL | 已完成 | 已用于本轮主流程落库核对 |

补充说明：

1. 当前 `3000` 端口被其它项目占用，因此本项目 H5 本轮使用 `3001`；
2. 本轮为避免本机代理影响本地回环访问，后端已显式指向：
   - `http://127.0.0.1:18080`
   - `http://127.0.0.1:18081`
3. 本轮体验建议统一使用 `127.0.0.1`，不要混用 `localhost`。

### 2.2 本地登录态已打通

本轮已完成本地登录态生成与验证：

1. 通过 `GET /api/auth/sso-callback?token=mock-tech-token&redirect_url=/equity/index` 成功生成本地 `NEXUSFIN_AUTH`；
2. 后端已成功调用科技平台用户信息 stub；
3. `GET /api/users/me` 已返回当前用户信息。

当前体验用户信息：

- `memberId`: `mem4df7afe3c3c54dd6a8e56497b68ad827`
- `externalUserId`: `tech-user-local-001`
- `techPlatformUserId`: `tech-user-local-001`
- `memberStatus`: `ACTIVE`

### 2.3 H5 主流程已完成体验

本轮已完成 H5 页面主流程体验，主路径如下：

1. 首页试算；
2. 确认借款；
3. 审批中；
4. 审批结果；
5. 还款确认；
6. 还款成功。

本轮接口主链已实际跑通：

1. `GET /api/loan/calculator-config`
2. `POST /api/loan/calculate`
3. `POST /api/loan/apply`
4. `GET /api/loan/approval-status/{applicationId}`
5. `GET /api/loan/approval-result/{applicationId}`
6. `GET /api/repayment/info/{loanId}`
7. `POST /api/repayment/submit`
8. `GET /api/repayment/result/{repaymentId}`

本轮示例链路数据：

#### 第一轮

- `applicationId`: `APP-0fe442266f57447f9e3df0cd4409af77`
- `benefitOrderNo`: `ordb94b2d3c17a9412e80df138b8330133a`
- `loanId`: `LN-778963fbf55543768ff91935d8fdfd43`
- `repaymentId`: `RP-20260421-0001`

#### 第二轮

- `applicationId`: `APP-7a42e8bfa1d14e58a6ba745ba930871f`
- `benefitOrderNo`: `ord75b73799fed643e795d8ee1fb6e0e74d`
- `loanId`: `LN-4adb2a6bfb8949588864463efd7c79de`
- `repaymentId`: `RP-20260421-0001`

### 2.4 后端日志已完成检查

本轮已完成后端日志检查，结论如下：

1. 未发现新的 `ERROR`；
2. 未发现新的 `WARN`；
3. 未发现超时、回滚、重试等异常痕迹；
4. 登录、借款、权益单创建、审批查询、还款查询 / 提交 / 结果查询的关键日志均已出现。

日志已确认的关键业务节点包括：

1. `sso callback login succeeded`
2. `current user resolved`
3. `loan calculator config requested`
4. `loan calculate requested`
5. `loan apply requested`
6. `resolved active payProtocolNo from ALLINPAY`
7. `order created and synced`
8. `approval status requested`
9. `approval result requested`
10. `repayment info requested`
11. `repayment submit requested`
12. `repayment result requested`

结论：

> 本轮后端日志表现正常，接口链路与业务主流程处理无明显异常。

### 2.5 MySQL 已完成主链落库核对

本轮已完成 MySQL 对账核对，以下内容与日志一致：

#### 协议数据一致

`member_payment_protocol` 已确认存在当前用户的有效协议：

- `provider_code = ALLINPAY`
- `protocol_status = ACTIVE`
- `protocol_no = AIP-MYSQL-1776243495403`

与后端日志中的：

- `resolved active payProtocolNo from ALLINPAY`

保持一致。

#### 权益订单数据一致

`benefit_order` 已确认存在本轮产生的 2 笔权益订单：

1. `ordb94b2d3c17a9412e80df138b8330133a`
2. `ord75b73799fed643e795d8ee1fb6e0e74d`

两笔数据均满足：

- `product_code = HUXUAN_CARD`
- `loan_amount = 300000`
- `order_status = FIRST_DEDUCT_PENDING`
- `sync_status = SYNC_SUCCESS`
- `pay_protocol_source = ALLINPAY`

与日志中的：

- `order created and synced`

保持一致。

#### 借款映射数据一致

`loan_application_mapping` 已确认存在本轮产生的 2 笔借款申请映射：

1. `APP-0fe442266f57447f9e3df0cd4409af77`
2. `APP-7a42e8bfa1d14e58a6ba745ba930871f`

两笔数据均满足：

- `mapping_status = ACTIVE`
- `benefit_order_no` 与对应权益单一致
- `upstream_query_value` 与对应 `loanId` 一致

#### 幂等记录一致

`idempotency_record` 已确认存在两条本轮 `loan-apply-APP-*` 记录：

- `biz_type = CREATE_ORDER`
- `biz_key` 对应各自的 `benefitOrderNo`

结论：

> 借款申请主链对应的协议、权益订单、借款映射和幂等记录均已落库，且与本轮日志一致。

---

## 3. 未完成

### 3.1 还款链路的本地持久化证据未确认

虽然本轮还款接口链路已成功跑通：

1. `GET /api/repayment/info/{loanId}`
2. `POST /api/repayment/submit`
3. `GET /api/repayment/result/{repaymentId}`

但从当前 MySQL 核对结果看，尚未确认与本轮还款动作直接对应的本地持久化记录。

当前核对结果：

- `payment_record` 中未查到与本轮两笔 `benefitOrderNo` 对应的新增记录。

因此当前不能下结论说：

> “从页面到还款成功，每一步都已经完成了本地数据库落库闭环。”

当前更准确的口径应为：

> 还款链路目前已完成接口级成功验证，但未确认存在本地持久化落库闭环。

### 3.2 还款本地落库口径仍需明确

后续仍需进一步确认以下事项：

1. `payment_record` 是否本来就不承接当前 H5 还款流程；
2. 若承接，当前为什么没有写入；
3. 若不承接，当前 H5 还款结果是否只依赖云卡 / 科技平台查询结果，不在本地留存；
4. 是否需要单独设计 H5 还款提交 / 查询的本地记录表。

### 3.3 当前仍是本地 stub 口径，不是外部真实联调

本轮体验已完成的是：

- 艾博生 H5
- 艾博生后端
- 本地科技平台 stub
- 本地云卡 stub
- 本地 MySQL

因此当前尚未完成：

1. 与真实云卡服务联调；
2. 与真实科技平台能力联调；
3. 真实外部异常场景验证；
4. 真实多系统时延、失败、重试场景验证。

---

## 4. 当前结论

截至 `2026-04-21` 本轮体验结束时，可以给出如下判断：

1. H5 页面主流程已可正常体验；
2. 后端日志整体正常；
3. 借款主链落库与日志一致；
4. 还款主链接口成功，但当前未看到本地持久化记录与之完全对应；
5. 因此当前状态可定义为：

> **H5 页面体验已可用，借款主链已完成日志 + MySQL 双重核对；还款主链已完成接口体验，但本地落库闭环仍待确认。**

---

## 5. 后续建议

建议下一步按以下顺序继续推进：

1. 先明确 H5 还款链路的本地落库口径；
2. 若应落库，则继续补核对或补实现；
3. 若不应落库，则需要把当前产品 / 技术口径写入文档，避免后续误判为缺陷；
4. 在本地 stub 口径稳定后，再推进与真实外部服务的联调。
