# 2026-05-01 联调前技术检查清单演练结果

## 1. 演练时间窗口

```text
2026-05-01 17:41:43 ~ 17:47:11 +08:00
```

本轮目标不是重复夜间全量回归，而是验证 [20260501_对外联调前技术检查清单.md](/Users/lixiaokun/Projects/nexusFin/nexusfin-equity/docs/test/20260501_对外联调前技术检查清单.md) 中的检查项，在当前环境下是否真的可执行、可留证、可区分“本地已证实”和“外部待确认”。

## 2. 当前 Runtime 基线

1. HEAD=`4a8e5b9eb2e71ee1086cbbb012c589d679988ff1`
2. backend=`http://127.0.0.1:8080`
3. H5=`http://127.0.0.1:3000`
4. Yunka=`REST -> http://127.0.0.1:18081/api/gateway/proxy`
5. QW=`MOCK`
6. tech-user stub=`http://127.0.0.1:18080`
7. backend start=`2026-05-01T09:47:13.766+08:00`
8. 当前 SSO 用户：`memberId=mem4df7afe3c3c54dd6a8e56497b68ad827` / `externalUserId=tech-user-local-001`

启动日志证据：

```text
2026-05-01T09:47:12.750+08:00 qw benefit client routing initialized mode=MOCK
2026-05-01T09:47:13.082+08:00 yunka gateway client initialized enabled=true mode=REST baseUrl=http://127.0.0.1:18081 gatewayPath=/api/gateway/proxy
2026-05-01T09:47:13.766+08:00 Started NexusfinEquityApplication
```

health 证据：

```http
GET /api/equity/health
```

```http
HTTP/1.1 200
X-Trace-Id: 9b8ddff4d7324e58bffba51badd83f1e

{"code":0,"message":"OK","data":{"service":"nexusfin-equity","status":"UP","timestamp":"2026-05-01T09:43:14.312389Z"}}
```

## 3. 按清单逐项演练

### 3.1 A. 本地 / 测试环境配置检查

| 检查项 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| backend / H5 / Yunka / QW / tech-user 基线是否和清单一致 | PASS | `start-h5-fullflow-env.sh` 启动参数 + backend 启动日志 + `/api/equity/health` | 当前仍是本地唯一稳定基线，不是对外环境 |
| 启动日志能否真实区分当前是本地 stub 还是对外环境 | PASS | 启动日志明确出现 `QW mode=MOCK`、`Yunka baseUrl=http://127.0.0.1:18081` | 如果联调当天仍看到该值，说明还没切到外部 Yunka |
| 配置项是否能被实际核对 | PASS | `application.yml` 可核对默认键；`start-h5-fullflow-env.sh` 可核对当前本地覆盖值 | 当前能核对 `yunka.mode/base-url/gateway-path`、`qw.mode`、`auth.tech-platform-base-url`、`jwt.cookie-secure=false` |
| 对外联调切换前提是否已具备真实值 | 外部待确认 | 本地只看到占位/本地值：`127.0.0.1:18081`、`ABS`、占位签名 secret | 切外部前必须由云卡 / 齐为 / 科技平台提供正式值 |

### 3.2 B. Yunka / QW / 科技平台联调前提

| 检查项 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| Yunka 本地协议入口 `/api/gateway/proxy` 是否可执行 | PASS | 当前 `loan/calculate`、`benefits/card-detail`、`benefits/activate`、`repayment/submit` 均经该入口成功 | 仅说明本地 stub 联通，不代表公网 Yunka 可用 |
| Yunka 借款试算 path 本地是否按 `/loan/trial` 工作 | PASS | `TRACE_STABILITY_R13_LOAN_CALC_001` 命中 `path=/loan/trial` 并成功 | 本地运行态已证明 `/loan/trial` 可用 |
| Yunka 正式 `baseUrl / X-Channel-Code / X-Signature / KJ_NOT_READY 修复时间 / 电子签章方案` | 外部待确认 | 当前无公网 Yunka 演练；`application.yml` 中仍是默认/占位口径 | 不应误报成本地失败 |
| QW 当前是否继续使用 MOCK 作为本地基线 | PASS | 启动日志 `qw benefit client routing initialized mode=MOCK` | 与 `CURRENT_STATE` 一致 |
| 真实 QW 物料（baseUrl / 商户号 / 签名 / 证书）是否齐备 | 外部待确认 | 本轮未切真实 QW，当前没有外部物料 | 当前只验证了本地 MOCK 可用 |
| 科技平台本地 stub 边界是否可执行 | PASS | `GET /api/auth/sso-callback?token=mock-tech-token&redirect_url=/equity/index` 可建立本地 `NEXUSFIN_AUTH` | 仅说明本地 stub 用户校验通路存在 |
| 科技平台真实域名 / 用户查询 / callback 签名等是否齐备 | 外部待确认 | [20260501_外部联调待确认问题清单.md](/Users/lixiaokun/Projects/nexusFin/nexusfin-equity/docs/third-part/20260501_外部联调待确认问题清单.md) 仍保留未闭合项 | 不能靠本地环境自行推断 |

### 3.3 C. 日志与可观测性

| 检查项 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| `traceId / path / requestId / bizOrderNo / requestBodyJson / responseBodyJson / elapsedMs / errorNo / errorMsg` 是否能稳定拿到 | PASS | 本轮 `joint-login`、`loan/calculate`、`benefits/activate`、`repayment/submit` 均有完整日志 | 当前日志足够支持联调排障 |
| joint-login 代表性证据 | PASS | `TRACE_STABILITY_R13_JOINT_OK_001`，可见 `/user/token`、`/user/query`、`joint login entry resolved targetPage=landing` | 错误分支另见 invalid token |
| benefit redirect 代表性证据 | PASS | `TRACE_STABILITY_R13_REDIRECT_OK_001`，接口返回 `redirectUrl`；backend 有完整 joint-login 子链请求/响应日志 | 当前 controller 日志关键词为 `benefit redirect url generated` |
| loan 代表性证据 | PASS | `TRACE_STABILITY_R13_LOAN_CALC_001`，`path=/loan/trial`，含完整 `requestBodyJson / responseBodyJson / elapsedMs` | 可直接用于 5 月 7 日联调前自检模板 |
| benefits 代表性证据 | PASS | `TRACE_STABILITY_R13_BENEFITS_ACTIVATE_001`，`benefits sync benefiturl generated` + `/benefit/sync` request/response | 同时证明 `benefiturl` 已真实出站 |
| repayment 代表性证据 | PASS | `TRACE_STABILITY_R13_REPAY_SUBMIT_001`，`path=/repay/apply` 完整请求响应体 | 当前可留证到 `repaymentId` 级别 |

本轮代表性日志摘录：

```text
TRACE_STABILITY_R13_LOAN_CALC_001 path=/loan/trial requestBodyJson={"requestId":"LC-1189ceaf8b0a498a8fa54324c2d17ffd","path":"/loan/trial","bizOrderNo":"LC-1189ceaf8b0a498a8fa54324c2d17ffd","data":{"uid":"tech-user-local-001","applyId":"LC-1189ceaf8b0a498a8fa54324c2d17ffd","loanAmount":300000,"loanPeriod":3}} yunka gateway request begin
TRACE_STABILITY_R13_LOAN_CALC_001 path=/loan/trial elapsedMs=40 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"receiveAmount":300000,"repayAmount":312350,"yearRate":18,"repayPlan":[...]}} yunka gateway request success
TRACE_STABILITY_R13_BENEFITS_ACTIVATE_001 benefits sync benefiturl generated applicationId=APP-stability-r13-001 benefitUrlPresent=true
TRACE_STABILITY_R13_BENEFITS_ACTIVATE_001 path=/benefit/sync requestBodyJson={"requestId":"BENEFITS-SYNC-APP-stability-r13-001","path":"/benefit/sync","bizOrderNo":"ord9dceb0f1b77440d6becdfde5a1614faf","data":{"benefiturl":"https://mock-qw.local/exercise?...","userId":"tech-user-local-001","platformBenefitOrderNo":"APP-stability-r13-001","benefitStatus":"ACTIVE","benefitAmount":300000}} yunka gateway request begin
TRACE_STABILITY_R13_REPAY_SUBMIT_001 path=/repay/apply requestBodyJson={"requestId":"RS-55fe0d1514a44a23a120734affb271f4","path":"/repay/apply","bizOrderNo":"LN-4539d2c1bd8847b59b413d72391409d3","data":{"uid":"tech-user-local-001","loanId":"LN-4539d2c1bd8847b59b413d72391409d3","repayType":"EARLY","periods":[],"bankCardNo":"6222020202028648","repayAmount":101850}} yunka gateway request begin
TRACE_STABILITY_R13_JOINT_INVALID_001 path=/user/token elapsedMs=7 yunkaCode=40101 errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=token invalid responseBodyJson={"code":40101,"message":"token invalid","traceId":null,"requestId":null,"data":null}
TRACE_STABILITY_R13_JOINT_INVALID_001 method=POST path=/api/auth/joint-login errorNo=JOINT_LOGIN_TOKEN_INVALID errorMsg=Joint login session expired
```

### 3.4 D. `benefiturl` 当前语义

| 检查项 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| 字段已接线 | PASS | `BenefitsServiceImpl.activate()` 中调用 `BenefitRedirectUrlService.generate(...)`，且本轮 `benefits/activate` 成功 | 当前不是“接口已实现但主链未接”状态 |
| 字段已出站 | PASS | `TRACE_STABILITY_R13_BENEFITS_ACTIVATE_001` 中 `/benefit/sync requestBodyJson` 明确带 `benefiturl` | 本轮新单：`ord9dceb0f1b77440d6becdfde5a1614faf` |
| 当前语义不能泛化 | PASS | 代码注释与日志均明确当前来源是 `QW exercise redirect URL` | 这是语义边界，不是代码故障 |
| 外部是否接受当前实现语义 | 外部待确认 | 需科技平台 / 齐为确认当前“后端生成、来源为 QW exercise redirect URL”的字段语义是否可接受 | 不应在本地演练里误判成 bug |

### 3.5 E. `joint-login` 当前边界

| 检查项 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| `push` 无 `benefitOrderNo` 可成功 | PASS | 本轮 `TRACE_STABILITY_R13_JOINT_OK_001` 返回 `targetPage=landing` | 当前运行态与清单描述一致 |
| `exercise / refund` 必须带 `benefitOrderNo` | PASS | 已有闭环运行态证据，见夜间回归 Round 9 / joint-login 专项结果 | 本轮未重复刷该两项，按“已闭合且可复用”处理 |
| invalid token 能否与 generic reject / timeout 区分 | PASS | invalid token：本轮 `TRACE_STABILITY_R13_JOINT_INVALID_001`；timeout / reject：夜间回归 Round 10 | 当前证据已足够支撑联调口径 |
| 当前边界是否还存在未闭合本地问题 | NOT_APPLICABLE | 当前无新增本地 blocker | 不应把已闭合项重新报成 FAIL |

### 3.6 F. 已知外部 blocker

| 检查项 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| Yunka `KJ_NOT_READY / kj.private-key 配置无效` | 外部待确认 | `CURRENT_STATE` 和外部待确认清单均仍保留 | 当前应继续归类为外部问题 |
| 电子签章接入方案 | 外部待确认 | 外部待确认清单仍未闭合 | 非当前本地代码故障 |
| 科技平台测试 / 生产域名 | 外部待确认 | 外部待确认清单第 3 节 | 需要 5 月 7 日前优先确认 |
| `/loan/trial` vs `/loan/trail` 文档真值 | 外部待确认 | 本地运行态已证明 `/loan/trial` 可用，但外部文档仍未完全统一 | 这是文档/协议口径问题，不是当前本地 blocker |
| `benefiturl` 业务语义边界 | 外部待确认 | 当前本地只证明“已接线、已出站、来源明确” | 是否被外部按该语义接受，仍需确认 |

## 4. 当前可直接带去 5 月 7 日联调的项

1. 本地稳定基线的启动方法、启动日志和 health 检查。
2. `joint-login` push 正常链路的最小请求与日志证据。
3. `POST /api/auth/redrect_benefit_url` 的正常链路和最新 `redirectUrl` 响应证据。
4. `POST /api/loan/calculate` 的最新 request/response/log 证据。
5. `GET /api/benefits/card-detail` 与 `POST /api/benefits/activate` 的最新 request/response/log 证据。
6. `benefiturl` 已接线、已出站、已落日志的最新证据。
7. `GET /api/repayment/info/{loanId}` 与 `POST /api/repayment/submit` 的代表性样本。
8. `401` 受控错误态与 `JOINT_LOGIN_TOKEN_INVALID` 错误分支证据。

## 5. 当前必须外部确认后才能继续的项

1. Yunka 真实测试地址、正式 `X-Channel-Code`、正式签名算法/原串/密钥。
2. Yunka `KJ_NOT_READY / kj.private-key 配置无效` 的解除时间点。
3. 电子签章最终接入方案和本轮联调范围。
4. 真实 QW 的 baseUrl、签名物料、证书和失败样例。
5. 科技平台测试域名、真实 token 校验 / 用户信息查询接口、callback header / 签名 / 重试策略。
6. `benefiturl` 当前“QW exercise redirect URL 来源”语义是否被科技平台 / 齐为接受。
7. `/loan/trial` 与 `/loan/trail` 的最终文档口径，以哪一份对外文档为准。

## 6. 当前不应再重复讨论的已闭合项

1. Round 1 的本地 Yunka stub path 缺口不应再按当前 FAIL 讨论。
2. invalid token 运行态不应再标成 BLOCKED。
3. `benefiturl` 主链“未接线 / 未出站”不应再重复讨论。
4. `joint-login` push 依赖 `benefitOrderNo` 不应再重复讨论。

## 7. 清单本身需要修订的地方

1. 第 5.3 节当前建议 grep 的 `benefit redirect url resolved from qw exercise redirect url` 不足以覆盖最新运行态。  
   当前 backend 更稳定可见的关键词是：
   - `benefit redirect url generated`
   - `benefits sync benefiturl generated`
2. 第 6 节建议补一句更贴近当前业务口径的话：  
   `benefiturl` 当前用于“科技平台 -> benefiturl -> 艾博生后端 -> 齐为行权页”的跳转链，不应表述成泛化权益 URL。
3. 第 8 节第 4 条建议改成：  
   `本地运行态当前已验证 /loan/trial 可用；待外部确认的是历史文档中 /loan/trail 的最终真值。`

## 8. 最小结论

1. 这份技术检查清单在当前环境下是“可演练、可留证”的，不只是纸面文档。
2. 本地稳定基线、日志可观测性、`joint-login`、`benefit redirect`、`loan/calculate`、`benefits/activate`、`repayment submit` 都已经有可复用证据。
3. 当前需要继续推进的不是本地修 bug，而是外部联调物料与口径确认。
4. 本轮没有新增本地 `FAIL`；新增需要注意的是清单第 5.3 / 6 / 8 节有表述可进一步收紧。
