# 2026-04-29 H5 全流程回归结果

## 1. 结论摘要

截至本轮收口完成，H5 本地联调 focused 回归已通过，当前可复用环境和结论如下：

1. 当前唯一真值环境：
   - H5：`http://127.0.0.1:3000/`
   - 后端：`http://127.0.0.1:8080/`
   - tech-user stub：`http://127.0.0.1:18080/api/users/me`
   - Yunka stub：`http://127.0.0.1:18081/api/gateway/proxy`
2. 当前环境由 `tmux detached session` 常驻承载，不再依赖一次性前台启动。
3. 首轮 focused 回归已通过：
   - `/` 可稳定进入 `/landing`
   - 借款主路径可继续走到 `/repayment-success`
   - 未登录访问受保护接口仍返回 `401`，并携带 `redirectHint`

## 2. 当前环境口径

### 2.1 常驻承载方式

本轮环境通过 `tmux` 持续运行，测试会话接手时无需重新拉起。

启动脚本：

```bash
cd /Users/lixiaokun/Projects/nexusFin/nexusfin-equity
scripts/local-stubs/start-h5-fullflow-env.sh
```

停止脚本：

```bash
cd /Users/lixiaokun/Projects/nexusFin/nexusfin-equity
scripts/local-stubs/stop-h5-fullflow-env.sh
```

### 2.2 当前持续运行组件

| 组件 | 地址 | 说明 |
|---|---|---|
| H5 | `http://127.0.0.1:3000/` | Vite dev server |
| Java 后端 | `http://127.0.0.1:8080/` | Spring Boot 本地服务 |
| 科技平台 stub | `http://127.0.0.1:18080/api/users/me` | 仅服务登录链路 |
| Yunka stub | `http://127.0.0.1:18081/api/gateway/proxy` | 仅服务 Yunka gateway |

## 3. 本轮已通过范围

### 3.1 路由入口

已确认根入口不再直接落到 `404`：

1. `"/"` 使用最小重定向方案进入 `"/landing"`；
2. `"/landing"` 现有业务页语义保持不变；
3. `LandingPage` 上的“去借款”仍按既有逻辑跳转到 `"/calculator"`。

### 3.2 登录与保护接口

已确认：

1. `GET /api/auth/sso-callback?token=mock-tech-token` 可成功返回 `302`；
2. 响应头可写入本地 `NEXUSFIN_AUTH` Cookie；
3. 未登录访问受保护接口时，后端仍返回：
   - `401`
   - `redirectHint = joint-entry`

### 3.3 借款到还款成功主路径

本轮 focused 回归已覆盖并跑通以下链路：

1. `GET /api/loan/calculator-config`
2. `POST /api/loan/calculate`
3. `POST /api/loan/apply`
4. `GET /api/loan/approval-status/{applicationId}`
5. `GET /api/loan/approval-result/{applicationId}`
6. `GET /api/repayment/info/{loanId}`
7. `POST /api/repayment/sms-send`
8. `POST /api/repayment/sms-confirm`
9. `POST /api/repayment/submit`
10. `GET /api/repayment/result/{repaymentId}`

链路结论：

1. 借款申请可进入审批中；
2. Yunka stub 可将审批查询推进到通过；
3. 还款短信链路已补齐 happy path；
4. 还款提交后可查询到成功结果；
5. 因此“借款主路径到 `/repayment-success`”这一条已可作为当前本地联调真值。

## 4. 当前已知限制

### 4.1 Yunka stub 仍是 happy-path stub

当前 Yunka stub 只服务本地联调 happy path，不是完整上游行为仿真。已覆盖的最小路径包括：

- `/loan/trail`
- `/loan/apply`
- `/loan/query`
- `/loan/repayPlan`
- `/repay/trial`
- `/repay/apply`
- `/repay/query`
- `/card/userCards`
- `/card/smsSend`
- `/card/smsConfirm`

说明：

1. `/loan/query` 仍使用最小内存状态推进；
2. `/card/smsSend` 与 `/card/smsConfirm` 只提供成功分支；
3. 不代表真实云卡失败、拒绝、超时、多次验证等完整场景。

### 4.2 借款失败分支当前未提供触发条件

本轮本地联调环境面向 happy path，当前未为以下分支提供固定触发方式：

1. 借款申请拒绝；
2. 审批失败；
3. 还款短信校验失败；
4. 还款提交失败。

### 4.3 联合登录入口链路本轮未验证

本轮 focused 回归没有覆盖：

1. `/joint-entry`
2. `/joint-dispatch`
3. `/joint-refund-entry`
4. `/joint-unsupported`

这些页面和入口链路仍需单独验证，不应与本轮 focused 结论混写。

### 4.4 浏览器层存在非阻断 issue

当前已知非阻断问题：

1. 部分表单字段缺少 `id` 或 `name`；
2. 该问题会影响浏览器自动化和可访问性检查；
3. 但不阻断本轮 focused 联调结论。

## 5. 后续使用建议

### 5.1 当前建议直接复用的范围

如后续只是继续 H5 本地联调、页面走查或 focused API 验证，建议直接复用当前环境，不要重复改 stub 口径。

### 5.2 当前不建议立即重复跑的范围

若没有改到以下内容，不建议重复执行完全相同的 focused 回归：

1. 根路由入口；
2. 还款短信链路；
3. 本地 tech-user stub；
4. 本地 Yunka stub；
5. 持续运行环境脚本。

换句话说，除非变更涉及：

- `H5/client/src/App.tsx`
- `H5/client/src/pages/ConfirmRepaymentPage.tsx`
- `scripts/local-stubs/**`
- 登录态 / 本地联调启动方式

否则不必为相同 happy path 重复做一次完整 focused 回归。

## 6. 当前判断

截至本轮文档收口时，当前本地联调结论可以表述为：

> H5 focused 回归已通过；`tmux` 常驻环境可复用；根入口、借款主路径、还款短信链路和还款成功页已打通；当前剩余限制主要是 Yunka happy-path stub 的覆盖边界，而不是当前主路径不可用。
