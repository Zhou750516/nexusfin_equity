# 2026-05-01 夜间自动化联调运行记录

## Round 1

时间窗口：

```text
2026-05-01 00:08:35 ~ 00:20:41 +08:00
```

当前基线：

1. HEAD=`b4b0fd5387437b30d5979bee7170bf7d7b9d386f`
2. backend start=`2026-05-01T00:08:46.440+08:00`
3. QW=`MOCK`
4. Yunka=`REST -> http://127.0.0.1:18081`
5. health=`UP`

本轮执行集合：

1. runtime 重启与基线确认
2. H5 `joint-entry push/exercise/refund`
3. H5 `/benefits-card`
4. H5 `/calculator`
5. H5 `/confirm-repayment?loanId=...`
6. `POST /api/auth/joint-login`
7. `POST /api/auth/redrect_benefit_url`
8. `GET /api/benefits/card-detail`
9. `POST /api/benefits/activate`
10. `POST /api/loan/calculate`
11. `GET /api/repayment/info/{loanId}`
12. SQL 回查 `benefit_order` / `loan_application_mapping`

本轮结论：

1. `auth / joint-login / redrect_benefit_url` 可继续联调。
2. `loan` 与 `benefits` 主链被本地 Yunka stub path 能力缺口阻断。
3. invalid token 语义仍是 `BLOCKED`。
4. `benefiturl` 代码已接入，但运行态实发证据未闭合。

下一轮优先项：

1. 补 Yunka stub：
   - `/loan/trial`
   - `/protocol/queryProtocolAggregationLink`
2. 重跑：
   - `/calculator`
   - `/benefits-card`
   - `POST /api/loan/calculate`
   - `POST /api/benefits/activate`
3. 若 `activate` 可通，再继续拿 `benefiturl` 出站 payload 证据。

## Round 2

时间窗口：

```text
2026-05-01 08:12:29 ~ 08:17:47 +08:00
```

当前基线：

1. HEAD=`b4b0fd5387437b30d5979bee7170bf7d7b9d386f`
2. backend start=`2026-05-01T08:12:29.077+08:00`
3. QW=`MOCK`
4. Yunka=`REST -> http://127.0.0.1:18081`
5. 本地 stub 已补 `/loan/trial`、`/protocol/queryProtocolAggregationLink`、`/benefit/sync`

本轮执行集合：

1. 修改 Yunka stub 并补测试
2. `node --test scripts/local-stubs/yunka-stub.test.js`
3. 重启 `yunka-stub + backend`
4. `POST /api/loan/calculate`
5. `GET /api/benefits/card-detail`
6. `POST /api/benefits/activate`
7. `GET /api/repayment/info/{validLoanId}`
8. H5 `/calculator`
9. H5 `/benefits-card`
10. H5 `/confirm-repayment`
11. H5 还款交互到 `/repayment-success`

本轮结论：

1. Round 1 的两个本地 Yunka stub blocker 已解除。
2. `benefiturl` 主链运行态实发证据已经拿到。
3. 新的最小失败点转为 H5 权益开通按钮拿不到 token。

下一轮优先项：

1. 验证 / 修复 `ApprovalPendingPage -> BenefitsCardPage` 的 token 传递或持久化方案。
2. 修完后重跑：
   - H5 `/benefits-card` 点击“立即开通”
   - `/api/benefits/activate` 页面侧闭环
3. invalid token 语义仍保持 `BLOCKED`，除非 stub 补 fault。

## Round 3

时间窗口：

```text
2026-05-01 08:27:12 ~ 08:30:34 +08:00
```

当前基线：

1. HEAD=`b4b0fd5387437b30d5979bee7170bf7d7b9d386f`
2. backend start=`2026-05-01T08:12:29.077+08:00`
3. QW=`MOCK`
4. Yunka=`REST -> http://127.0.0.1:18081`
5. H5 运行态热更新生效

本轮执行集合：

1. 修复 H5 `benefits-card` 普通 SSO 链误暴露可开通入口的问题
2. 新增 / 回归前端逻辑单测
3. 执行全量 H5 `vitest run`
4. 执行 H5 `tsc --noEmit`
4. 浏览器回归：
   - 清空 joint session 的普通 SSO 场景
   - 恢复 joint session 的联登场景

本轮结论：

1. P1-3 已在当前 workspace 修复并完成运行态回归。
2. 普通 SSO 借款链不再出现“点击立即开通 -> 缺 token 本地报错”的假失败。
3. joint-login session 恢复后，页面不再误报缺 token，而是按真实协议状态控制 CTA。
4. invalid token 语义仍然 `BLOCKED`，下一轮如果继续推进，需要先补 stub fault 注入点。

下一轮优先项：

1. invalid token fault 注入入口
2. `benefiturl` 主链循环烟测
3. 安全 / 监控 / 资损例行轮巡

## Round 4

时间窗口：

```text
2026-05-01 09:46:59 ~ 09:47:48 +08:00
```

当前基线：

1. HEAD=`b4b0fd5387437b30d5979bee7170bf7d7b9d386f`
2. backend start=`2026-05-01T09:47:13.766+08:00`
3. QW=`MOCK`
4. Yunka=`REST -> http://127.0.0.1:18081`
5. 本地 stub 已新增 `/user/token` invalid-token fault

本轮执行集合：

1. 补 Yunka stub invalid-token fault
2. 执行 `node --test scripts/local-stubs/yunka-stub.test.js`
3. 重启 fullflow runtime
4. `POST /api/auth/joint-login` invalid token
5. `POST /api/auth/redrect_benefit_url` invalid token
6. `POST /api/auth/joint-login` session expired

本轮结论：

1. invalid token 语义已从 `BLOCKED` 收口为 `PASS`。
2. `joint-login` 与 `redrect_benefit_url` 都已真实命中 `JOINT_LOGIN_TOKEN_INVALID`。
3. `benefit redirect` 在 joint-login 子链失败时没有误映射成 QW redirect 失败。

下一轮优先项：

1. `benefiturl` 主链循环烟测
2. loan / repayment 主链轮巡
3. H5 页面轮巡

## Round 5

时间窗口：

```text
2026-05-01 09:49:10 ~ 09:57:38 +08:00
```

当前基线：

1. HEAD=`b4b0fd5387437b30d5979bee7170bf7d7b9d386f`
2. backend start=`2026-05-01T09:47:13.766+08:00`
3. QW=`MOCK`
4. Yunka=`REST -> http://127.0.0.1:18081`
5. auth user=`mem4df7afe3c3c54dd6a8e56497b68ad827 / tech-user-local-001`

本轮执行集合：

1. 重建 SSO 登录态
2. `GET /api/users/me`
3. `POST /api/benefits/activate`
4. 回查 `benefiturl` 出站日志
5. 回查 `benefit_order` / `async_compensation_task`

本轮结论：

1. `benefiturl` 主链运行态实发证据已用新单据刷新。
2. 新 activation 单据 `ord745622266f074850ab5d5dc2bf577bda` 已确认 `SYNC_SUCCESS`。
3. 本轮没有观察到补偿任务副作用。

下一轮优先项：

1. loan / repayment 主链轮巡
2. H5 页面轮巡
3. 安全专项代表性检查

## Round 6

时间窗口：

```text
2026-05-01 09:59:20 ~ 09:59:40 +08:00
```

当前基线：

1. HEAD=`b4b0fd5387437b30d5979bee7170bf7d7b9d386f`
2. backend start=`2026-05-01T09:47:13.766+08:00`
3. auth user=`mem4df7afe3c3c54dd6a8e56497b68ad827 / tech-user-local-001`

本轮执行集合：

1. `loan/calculate`
2. `repayment/info(valid)`
3. `repayment/info(history-stale-sample)`
4. `repayment/submit`
5. `repayment/info(missing)`
6. SQL 回查 `loan_application_mapping`

本轮结论：

1. loan / repayment 主链当前稳定。
2. 历史 stale 样例 `LN-9731...` 已因数据前置变化变成有效样例。
3. 明确不存在的 loanId 仍能稳定返回受控 404。

下一轮优先项：

1. H5 页面轮巡
2. 安全专项检查
3. 监控 traceId 贯通

## Round 7

时间窗口：

```text
2026-05-01 10:00:26 ~ 10:02:36 +08:00
```

当前基线：

1. H5 浏览器自动化继续使用本地 `http://127.0.0.1:3000`
2. 浏览器上下文中已补一次 SSO 登录

本轮执行集合：

1. invalid-token joint-entry 错误页
2. `/calculator`
3. `/benefits-card`
4. `/confirm-repayment`
5. 正常 push joint-entry -> `/landing`
6. console issue 检查

本轮结论：

1. H5 关键页面均可打开，未见白屏或崩溃。
2. invalid-token 错误页已闭环到受控文案。
3. 观察到低优先级浏览器 issue：表单控件缺少 `id/name`。

下一轮优先项：

1. 安全专项检查
2. 监控 traceId 贯通
3. 最终夜间收口

## Round 8

时间窗口：

```text
2026-05-01 10:03:12 +08:00
```

当前基线：

1. 使用未登录态直接访问受保护接口

本轮执行集合：

1. `GET /api/benefits/card-detail`
2. `POST /api/benefits/activate`
3. `GET /api/repayment/info/{loanId}`
4. 鉴权日志检查

本轮结论：

1. 代表性受保护接口都返回 `401 + redirectHint=joint-entry`。
2. 未观察到未登录态掉成 500。

下一轮优先项：

1. 监控 traceId 贯通
2. 最终夜间收口

## Round 9

时间窗口：

```text
2026-05-01 10:03:38 +08:00
```

当前基线：

1. 代表性 auth / loan 请求继续走本地 stable baseline

本轮执行集合：

1. `POST /api/auth/joint-login`
2. `POST /api/loan/calculate`
3. traceId / requestBodyJson / responseBodyJson / elapsedMs 日志核查

本轮结论：

1. caller traceId 在 auth / loan 两条链路上已真实贯通。
2. 本地联调基线下监控排障所需日志字段完整可用。
3. Round 4 ~ Round 9 均已落盘。

## Round 10

时间窗口：

```text
2026-05-01 11:43:28 ~ 11:43:31 +08:00
```

当前基线：

1. 继续使用本地 stub fault 注入能力
2. 重点观察 auth / redirect 上游 timeout / reject 收口

本轮执行集合：

1. `joint-login` timeout
2. `joint-login` reject
3. `redrect_benefit_url` timeout
4. `redrect_benefit_url` reject
5. backend 日志核查

本轮结论：

1. timeout -> `JOINT_LOGIN_UPSTREAM_TIMEOUT`
2. generic reject -> `JOINT_LOGIN_UPSTREAM_FAILED`
3. `redrect_benefit_url` 没有把 joint-login 子链异常误映射成 QW redirect 失败

## Round 11

时间窗口：

```text
2026-05-01 10:00:26 ~ 10:02:36 +08:00
```

当前基线：

1. H5 浏览器自动化轮巡

本轮执行集合：

1. invalid-token joint-entry
2. `/calculator`
3. `/benefits-card`
4. 浏览器内 SSO 登录后 `/confirm-repayment`
5. 正常 push joint-entry -> `/landing`
6. console issue 检查

本轮结论：

1. H5 关键页面可展示、错误态可控。
2. 继续观察到低优先级表单 `id/name` issue。

## Round 12

时间窗口：

```text
2026-05-01 11:43:31 ~ 11:46:00 +08:00
```

当前基线：

1. 汇总夜间轮巡结果并完成文档收口

本轮执行集合：

1. 汇总 Round 4 ~ Round 11
2. 复核文档落盘
3. 输出最终本地联调状态

本轮结论：

1. 截至 Round 12，未新增 P0 / P1 blocker。
2. 本地联调主链稳定。
3. 当前主要遗留为 P2 级浏览器 issue。
