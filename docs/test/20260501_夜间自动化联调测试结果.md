# 2026-05-01 夜间自动化联调测试结果

## 1. 总体执行概况

本轮为夜间自动化联调首轮摸底，时间窗口为 `2026-05-01 00:08:35 ~ 00:20:41 +08:00`。

本轮执行目标：

1. 把 runtime 切到最新 workspace HEAD 后重新建立本地联调基线。
2. 先覆盖 H5 `joint-login`、`benefits`、`loan`、`repayment` 的代表性页面。
3. 先覆盖后端 `auth`、`benefits`、`loan`、`repayment` 的代表性接口。
4. 先补一组监控 / 安全 / 资损证据，而不是只看接口返回。

本轮未扩到：

1. Yunka 公网真实链路。
2. QW 签约异常矩阵。
3. callback 再回归。
4. invalid token 语义闭环。

## 2. 当前 Runtime 基线

当前 workspace HEAD：

```text
b4b0fd5387437b30d5979bee7170bf7d7b9d386f
```

本轮已执行 runtime 重启，最新 backend 启动时间：

```text
2026-05-01T00:08:46.440+08:00
```

本轮真值基线：

1. backend：`http://127.0.0.1:8080`
2. H5：`http://127.0.0.1:3000`
3. QW：`MOCK`
4. Yunka：`REST -> http://127.0.0.1:18081/api/gateway/proxy`
5. tech-user：本地 stub 常驻

基线日志证据：

```text
2026-05-01T00:08:50.436+08:00  INFO ... RoutingQwBenefitClient    : qw benefit client routing initialized mode=MOCK businessDelegate=qweimobileClient signDelegate=qweimobileClient
2026-05-01T00:08:50.801+08:00  INFO ... RestYunkaGatewayClient    : yunka gateway client initialized enabled=true mode=REST baseUrl=http://127.0.0.1:18081 gatewayPath=/api/gateway/proxy
2026-05-01T00:08:51.426+08:00  INFO ... NexusfinEquityApplication : Started NexusfinEquityApplication in 5.567 seconds
```

health 检查：

```json
{"code":0,"message":"OK","data":{"service":"nexusfin-equity","status":"UP","timestamp":"2026-04-30T16:09:06.832464Z"}}
```

日志路径：

1. backend：`/var/folders/3j/dxwf_kbd1113yfhy4dm80fnr0000gn/T/nexusfin-h5-fullflow/backend.log`
2. h5：`/var/folders/3j/dxwf_kbd1113yfhy4dm80fnr0000gn/T/nexusfin-h5-fullflow/h5.log`

## 3. 本轮执行集合

### 3.1 H5 页面

已执行：

1. `/joint-entry?token=joint-token-push-runtime-001&scene=push`
2. `/joint-entry?token=joint-token-exercise-runtime-002&scene=exercise&benefitOrderNo=ord06dd5d69aa34499abf7e1151715937c1`
3. `/joint-entry?token=joint-token-refund-runtime-002&scene=refund&benefitOrderNo=ord06dd5d69aa34499abf7e1151715937c1`
4. `/benefits-card?applicationId=APP-night-001`
5. `/calculator`
6. `/confirm-repayment?loanId=LN-9731edd8858644d691862e6439c4d503`

### 3.2 后端接口

已执行：

1. `GET /api/equity/health`
2. `POST /api/auth/joint-login`
3. `POST /api/auth/redrect_benefit_url`
4. `GET /api/benefits/card-detail`
5. `POST /api/benefits/activate`
6. `POST /api/loan/calculate`
7. `GET /api/repayment/info/{loanId}`

### 3.3 SQL 回查

已执行：

1. `benefit_order` 针对 `activate-APP-night-activate-001` 的落单检查。
2. `loan_application_mapping` 最新数据探查，用于确认还款页面 loanId 前置。

## 4. PASS 列表

### 4.1 基线与启动

1. PASS: runtime 已成功切到最新 HEAD，并重新拉起。
2. PASS: `QW=MOCK`、`Yunka=REST -> 127.0.0.1:18081`、`health=UP`。

### 4.2 Joint Login 页面与接口

1. PASS: `push` 场景无 `benefitOrderNo` 可正常联登并跳转 `/landing`。
2. PASS: `exercise` 场景带 `benefitOrderNo` 可正常进入 `/joint-dispatch`。
3. PASS: `refund` 场景带 `benefitOrderNo` 可正常进入 `/joint-refund-entry`。
4. PASS: `POST /api/auth/joint-login` 返回 `HTTP 200 + code=0`，并下发 `Set-Cookie`。

关键页面快照证据：

```text
/landing 页面可见按钮：去借款
/joint-dispatch 页面可见按钮：Continue to Supplier
/joint-refund-entry 页面可见字段：refundStatus=NONE / refundableAmount=29900
```

关键接口请求与响应：

```http
POST /api/auth/joint-login
X-Trace-Id: TRACE_NIGHT_JOINT_PUSH_20260501_001
Content-Type: application/json

{"token":"joint-token-push-runtime-001","scene":"push"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_JOINT_PUSH_20260501_001
Set-Cookie: NEXUSFIN_AUTH=...

{"code":0,"message":"OK","data":{"loginSuccess":true,"scene":"push","targetPage":"landing","benefitOrderNo":null,"localUserReady":true}}
```

关键 backend 日志：

```text
traceId=TRACE_NIGHT_JOINT_PUSH_20260501_001 path=/user/token requestBodyJson={"requestId":"xa4f39150bf00e4e639fd9b9c1140eec0e","path":"/user/token","bizOrderNo":null,"data":{"userId":null,"token":"joint-token-push-runtime-001"}} yunka gateway request begin
traceId=TRACE_NIGHT_JOINT_PUSH_20260501_001 path=/user/token elapsedMs=21 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"cid":"cid-joint-token-push-runtime-001","name":"联合登录用户","phone":"13800138000"}} yunka gateway request success
traceId=TRACE_NIGHT_JOINT_PUSH_20260501_001 path=/user/query requestBodyJson={"requestId":"xa4f39150bf00e4e639fd9b9c1140eec0e","path":"/user/query","bizOrderNo":null,"data":{"userId":"memac3d4c4d15a94b249b0d295f06f5608e","cid":"cid-joint-token-push-runtime-001"}} yunka gateway request begin
traceId=TRACE_NIGHT_JOINT_PUSH_20260501_001 path=/user/query elapsedMs=10 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"userId":"memac3d4c4d15a94b249b0d295f06f5608e","cid":"cid-joint-token-push-runtime-001","phone":"13800138000","basicInfo":{"phone":"13800138000"},"idInfo":{"idno":"310101199001011111","name":"联合登录用户"}}} yunka gateway request success
traceId=TRACE_NIGHT_JOINT_PUSH_20260501_001 bizOrderNo=null joint login entry resolved targetPage=landing
```

### 4.3 Benefit Redirect URL 接口

1. PASS: `POST /api/auth/redrect_benefit_url` 在当前本地基线可成功返回 `redirectUrl`。
2. PASS: 本轮日志可证明它真实走了 joint-login 子链，再生成 redirect URL，不是伪成功。

关键接口请求与响应：

```http
POST /api/auth/redrect_benefit_url
X-Trace-Id: TRACE_NIGHT_REDIRECT_20260501_001
Content-Type: application/json

{"token":"joint-token-benefit-001","benefitOrderNo":"ord06dd5d69aa34499abf7e1151715937c1"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_REDIRECT_20260501_001

{"code":0,"message":"OK","data":{"redirectUrl":"https://mock-qw.local/exercise?partnerOrderNo=ord06dd5d69aa34499abf7e1151715937c1&uniqueId=cid-joint-token-benefit-001&token=mock-token-ord06dd5d69aa34499abf7e1151715937c1"}}
```

关键 backend 日志：

```text
traceId=TRACE_NIGHT_REDIRECT_20260501_001 path=/user/token requestBodyJson={"requestId":"xad0a14f940101415e89698d14a392e04d","path":"/user/token","bizOrderNo":"ord06dd5d69aa34499abf7e1151715937c1","data":{"userId":null,"token":"joint-token-benefit-001"}} yunka gateway request begin
traceId=TRACE_NIGHT_REDIRECT_20260501_001 path=/user/token elapsedMs=21 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"cid":"cid-joint-token-benefit-001","name":"联合登录用户","phone":"13800138000"}} yunka gateway request success
traceId=TRACE_NIGHT_REDIRECT_20260501_001 path=/user/query requestBodyJson={"requestId":"xad0a14f940101415e89698d14a392e04d","path":"/user/query","bizOrderNo":"ord06dd5d69aa34499abf7e1151715937c1","data":{"userId":"memd7d7a450552442bbaccd8dc0ae3f67e9","cid":"cid-joint-token-benefit-001"}} yunka gateway request begin
traceId=TRACE_NIGHT_REDIRECT_20260501_001 path=/user/query elapsedMs=10 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"userId":"memd7d7a450552442bbaccd8dc0ae3f67e9","cid":"cid-joint-token-benefit-001","phone":"13800138000","basicInfo":{"phone":"13800138000"},"idInfo":{"idno":"310101199001011111","name":"联合登录用户"}}} yunka gateway request success
traceId=TRACE_NIGHT_REDIRECT_20260501_001 bizOrderNo=ord06dd5d69aa34499abf7e1151715937c1 benefit redirect url generated
```

### 4.4 安全与受控错误样例

1. PASS: 未带有效 auth cookie 调用 `GET /api/benefits/card-detail` / `POST /api/benefits/activate` 会返回 `401`，并带 `redirectHint=joint-entry`，不是 500。
2. PASS: stale `loanId` 访问 `GET /api/repayment/info/{loanId}` 和 H5 `/confirm-repayment` 时，后端返回 `404 RESOURCE_NOT_FOUND`，前端展示受控错误态，不是白屏。

401 响应证据：

```http
HTTP/1.1 401

{"code":401,"message":"Missing auth cookie","data":{"redirectHint":"joint-entry"}}
```

还款页面错误态快照：

```text
Tải thất bại
repayment loan reference not found
```

还款接口响应：

```http
GET /api/repayment/info/LN-9731edd8858644d691862e6439c4d503
X-Trace-Id: TRACE_NIGHT_REPAY_INFO_20260501_001
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_REPAY_INFO_20260501_001

{"code":404,"message":"repayment loan reference not found","data":null}
```

关键 backend 日志：

```text
traceId=TRACE_NIGHT_REPAY_INFO_20260501_001 bizOrderNo=LN-9731edd8858644d691862e6439c4d503 repayment info requested by memberId=mem4088fcb2f3b04e009c95e62e376f10af
traceId=TRACE_NIGHT_REPAY_INFO_20260501_001 bizOrderNo=LN-9731edd8858644d691862e6439c4d503 method=GET path=/api/repayment/info/LN-9731edd8858644d691862e6439c4d503 errorNo=RESOURCE_NOT_FOUND errorMsg=repayment loan reference not found
```

### 4.5 监控与资损样例

1. PASS: `joint-login` 与 `redrect_benefit_url` 的 backend 日志已具备 `traceId`、`path`、`requestBodyJson`、`responseBodyJson`、`elapsedMs` 证据，监控可观测性成立。
2. PASS: `benefits/activate` 在上游 path 不支持时未新建本地权益单，没有先落单再失败。

SQL 证据：

```sql
SELECT COUNT(*) AS activate_request_order_count
FROM nexusfin_equity.benefit_order
WHERE request_id = 'activate-APP-night-activate-001';
```

```json
[{"activate_request_order_count":0}]
```

## 5. FAIL 列表

### 5.1 FAIL: `loan/calculate` 与 H5 `/calculator` 在当前本地基线下被 Yunka stub path 拒绝

影响：

1. H5 `/calculator` 页面无法拿到试算结果，页面展示受控错误态。
2. `POST /api/loan/calculate` 在当前本地基线稳定失败。
3. 这不是 500，但会阻断借款主链继续联调。

H5 页面快照：

```text
Tải thất bại
YUNKA_UPSTREAM_REJECTED:unsupported path: /loan/trial
```

浏览器网络证据：

```http
POST /api/loan/calculate
Request Body: {"amount":3000,"term":3}
```

```http
HTTP/1.1 200
X-Trace-Id: 86a0d8de701948f99532ed65fcad4c98

{"code":-1,"message":"YUNKA_UPSTREAM_REJECTED:unsupported path: /loan/trial","data":null}
```

backend 日志证据：

```text
traceId=86a0d8de701948f99532ed65fcad4c98 bizOrderNo=loan-calculate loan calculate requested by memberId=mem4088fcb2f3b04e009c95e62e376f10af
traceId=86a0d8de701948f99532ed65fcad4c98 bizOrderNo=LC-94ab3e52453e42b7aacfba6097e22f2f path=/loan/trial requestBodyJson={"requestId":"LC-94ab3e52453e42b7aacfba6097e22f2f","path":"/loan/trial","bizOrderNo":"LC-94ab3e52453e42b7aacfba6097e22f2f","data":{"uid":"cid-joint-token-refund-runtime-002","applyId":"LC-94ab3e52453e42b7aacfba6097e22f2f","loanAmount":300000,"loanPeriod":3}} yunka gateway request begin
traceId=86a0d8de701948f99532ed65fcad4c98 bizOrderNo=LC-94ab3e52453e42b7aacfba6097e22f2f path=/loan/trial elapsedMs=43 yunkaCode=10004 errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=unsupported path: /loan/trial responseBodyJson={"code":10004,"message":"unsupported path: /loan/trial","traceId":null,"requestId":null,"data":{"supportedPaths":["/loan/trail","/loan/apply","/loan/query","/loan/repayPlan","/repay/trial","/repay/apply","/repay/query","/user/token","/user/query","/card/userCards","/card/smsSend","/card/smsConfirm"]}}
traceId=86a0d8de701948f99532ed65fcad4c98 method=POST path=/api/loan/calculate errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=unsupported path: /loan/trial
```

当前最小判断：

1. 应用配置使用 `YUNKA_LOAN_CALCULATE_PATH=/loan/trial`。
2. 本地 Yunka stub 仅支持 `/loan/trail`。
3. 当前属于本地联调基线漂移，已形成真实 blocker。

代码 / stub 证据：

```text
src/main/resources/application.yml -> loan-calculate: /loan/trial
scripts/local-stubs/yunka-stub.js -> case "/loan/trail"
scripts/local-stubs/yunka-stub.js -> supportedPaths 包含 "/loan/trail"
```

### 5.2 FAIL: `benefits-card` 与 `benefits/activate` 在当前本地基线下被 Yunka stub path 拒绝

影响：

1. H5 `/benefits-card` 页面稳定进入受控错误态。
2. `GET /api/benefits/card-detail` 稳定失败。
3. `POST /api/benefits/activate` 在 `getCardDetail` 前置阶段即失败，无法继续跑到 `benefiturl generated -> syncBenefitOrder`。

H5 页面快照：

```text
Tải thất bại
YUNKA_UPSTREAM_REJECTED:unsupported path: /protocol/queryProtocolAggregationLink
```

接口响应：

```http
GET /api/benefits/card-detail
X-Trace-Id: TRACE_NIGHT_BENEFITS_CARD_20260501_001
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_BENEFITS_CARD_20260501_001

{"code":-1,"message":"YUNKA_UPSTREAM_REJECTED:unsupported path: /protocol/queryProtocolAggregationLink","data":null}
```

```http
POST /api/benefits/activate
X-Trace-Id: TRACE_NIGHT_BENEFITS_ACTIVATE_20260501_001
Content-Type: application/json

{"applicationId":"APP-night-activate-001","cardType":"huixuan_card","token":"joint-token-benefit-001"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_BENEFITS_ACTIVATE_20260501_001

{"code":-1,"message":"YUNKA_UPSTREAM_REJECTED:unsupported path: /protocol/queryProtocolAggregationLink","data":null}
```

backend 日志证据：

```text
traceId=TRACE_NIGHT_BENEFITS_CARD_20260501_001 bizOrderNo=benefits-card-detail benefits card detail requested by memberId=mem4088fcb2f3b04e009c95e62e376f10af
traceId=TRACE_NIGHT_BENEFITS_CARD_20260501_001 path=/protocol/queryProtocolAggregationLink requestBodyJson={"requestId":"BENEFITS-PROTOCOL-cid-joint-token-refund-runtime-002","path":"/protocol/queryProtocolAggregationLink","bizOrderNo":"benefits-card-detail","data":{"userId":"cid-joint-token-refund-runtime-002","loanAmount":300000,"loanPeriod":3}} yunka gateway request begin
traceId=TRACE_NIGHT_BENEFITS_CARD_20260501_001 path=/protocol/queryProtocolAggregationLink elapsedMs=16 yunkaCode=10004 errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=unsupported path: /protocol/queryProtocolAggregationLink responseBodyJson={"code":10004,"message":"unsupported path: /protocol/queryProtocolAggregationLink","traceId":null,"requestId":null,"data":{"supportedPaths":["/loan/trail","/loan/apply","/loan/query","/loan/repayPlan","/repay/trial","/repay/apply","/repay/query","/user/token","/user/query","/card/userCards","/card/smsSend","/card/smsConfirm"]}}
traceId=TRACE_NIGHT_BENEFITS_ACTIVATE_20260501_001 path=/protocol/queryProtocolAggregationLink requestBodyJson={"requestId":"BENEFITS-PROTOCOL-cid-joint-token-refund-runtime-002","path":"/protocol/queryProtocolAggregationLink","bizOrderNo":"benefits-card-detail","data":{"userId":"cid-joint-token-refund-runtime-002","loanAmount":300000,"loanPeriod":3}} yunka gateway request begin
traceId=TRACE_NIGHT_BENEFITS_ACTIVATE_20260501_001 path=/protocol/queryProtocolAggregationLink elapsedMs=10 yunkaCode=10004 errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=unsupported path: /protocol/queryProtocolAggregationLink responseBodyJson={"code":10004,"message":"unsupported path: /protocol/queryProtocolAggregationLink","traceId":null,"requestId":null,"data":{"supportedPaths":["/loan/trail","/loan/apply","/loan/query","/loan/repayPlan","/repay/trial","/repay/apply","/repay/query","/user/token","/user/query","/card/userCards","/card/smsSend","/card/smsConfirm"]}}
traceId=TRACE_NIGHT_BENEFITS_ACTIVATE_20260501_001 method=POST path=/api/benefits/activate errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=unsupported path: /protocol/queryProtocolAggregationLink
```

当前最小判断：

1. 最新代码已把 `benefiturl` 接入 `BenefitsServiceImpl.activate -> syncBenefitOrder`。
2. 但当前 runtime 还没跑到 `benefiturl generated` 或 `syncBenefitOrder`。
3. 阻断点不是代码缺这条链，而是本地 Yunka stub 缺少 `/protocol/queryProtocolAggregationLink` 支持。
4. 当前属于本地联调基线能力缺口，已形成真实 blocker。

## 6. BLOCKED 列表

### 6.1 BLOCKED: invalid token 语义仍未闭合

当前状态：

1. 代码中已存在把特定上游 reject 映射到 `JOINT_LOGIN_TOKEN_INVALID` 的逻辑。
2. 当前 stub 只能构造通用 `_FAULT_REJECT_503`。
3. 当前 reject message 仍是 `Mock Yunka rejection code=503`，不包含 `token invalid` / `token expired` / `session expired`。

因此本轮结论：

1. 这不是已确认的代码逻辑失败。
2. 这是运行态 fault 注入入口不足。
3. 继续联调时应标记为 `BLOCKED`，不能误报成业务缺陷。

## 7. 语义检查

### 7.1 `joint-login`

当前运行态已证明：

1. `push` 已摆脱 `benefitOrderNo` 依赖。
2. `push` 不带 `benefitOrderNo` 时，接口和 H5 都能落到 `landing`。

### 7.2 `redrect_benefit_url`

当前运行态已证明：

1. 接口本身可用。
2. 它内部先走 joint-login，再返回 QW mock exercise URL。
3. 当前返回值语义更接近 `QW exercise redirect URL`，不能泛化宣称为所有权益系统统一的“权益联登 URL”。

### 7.3 `benefiturl` 主链

当前最新代码已证明：

1. `benefiturl` 字段已存在于 `BenefitOrderSyncRequest`。
2. `BenefitsServiceImpl.activate` 已调用 `BenefitRedirectUrlService.generate(...)`，并将结果传给 `syncBenefitOrder(...)`。

但本轮运行态结论仍然是：

1. 代码已接入。
2. 运行态未实发到 `syncBenefitOrder`。
3. 原因是前置 `queryProtocolAggregationLink` 被本地 stub 阻断。

## 8. PASS / FAIL / BLOCKED / NOT_APPLICABLE 汇总

PASS：

1. runtime 最新基线重启成功。
2. `health` 正常。
3. `joint-login push` 页面与接口通过。
4. `joint-login exercise/refund` 页面通过。
5. `redrect_benefit_url` 正常返回 `redirectUrl`。
6. 未登录访问受保护权益接口时受控返回 `401 + redirectHint`。
7. `repayment/info` stale loanId 受控返回，H5 页面无白屏。
8. `traceId + requestBodyJson + responseBodyJson + elapsedMs` 监控证据已拿到。
9. `benefits/activate` 阻断时未落本地权益单。

FAIL：

1. `loan/calculate` / H5 `/calculator` 被本地 Yunka stub 的 `/loan/trail` / `/loan/trial` 漂移阻断。
2. `benefits/card-detail` / `benefits/activate` / H5 `/benefits-card` 被本地 Yunka stub 缺少 `/protocol/queryProtocolAggregationLink` 阻断。

BLOCKED：

1. invalid token 语义缺少 stub fault 注入入口。
2. `benefiturl -> syncBenefitOrder` 运行态实发证据仍被 `queryProtocolAggregationLink` 前置阻断。

NOT_APPLICABLE：

1. 本轮未进入 Yunka 公网真实链路。
2. 本轮未回归 callback。
3. 本轮未执行 QW 签约异常矩阵。

## 9. 最小结论

截至本轮收口，当前夜间自动化联调首轮可以给出三条直接结论：

1. `auth / joint-login / redrect_benefit_url` 在本地 baseline 下可继续联调，且日志可观测性足够。
2. `loan` 与 `benefits` 两条 H5 主路径当前都被本地 Yunka stub 能力缺口阻断，不是 500，但已经构成真实 baseline blocker。
3. `benefiturl` 在最新代码中已接入主链，但今晚尚未拿到运行态实发证据，不能写成主链已闭环。

## 10. 下一轮建议

下一轮优先顺序：

1. 先修本地 Yunka stub 与当前应用配置的 path 漂移：
   - `/loan/trail` vs `/loan/trial`
   - `/protocol/queryProtocolAggregationLink`
2. 修完后优先重跑：
   - H5 `/calculator`
   - H5 `/benefits-card`
   - `POST /api/loan/calculate`
   - `POST /api/benefits/activate`
3. 若修通 `benefits/activate`，继续补拿：
   - `benefits sync benefiturl generated`
   - `syncBenefitOrder` 的 `requestBodyJson`
   - Yunka / Xiaohua 出站 payload 中的 `benefiturl`
4. invalid token 语义继续保持 `BLOCKED`，直到 stub 提供语义化 fault 注入。

## 11. 第二轮增量结果

第二轮时间窗口：

```text
2026-05-01 08:12:29 ~ 08:17:47 +08:00
```

第二轮动作：

1. 修复本地 Yunka stub 支持路径：
   - `/loan/trial`
   - `/protocol/queryProtocolAggregationLink`
   - `/benefit/sync`
2. 运行 `node --test scripts/local-stubs/yunka-stub.test.js`，结果 `6/6 PASS`
3. 重启 `yunka-stub + backend`
4. 重新回归 `loan/calculate`、`benefits/card-detail`、`benefits/activate`
5. 用浏览器重新回归 H5 `/calculator`、`/benefits-card`、`/confirm-repayment`
6. 继续把 H5 还款链走到 `/repayment-success`

### 11.1 第二轮 PASS

PASS：

1. `POST /api/loan/calculate` 已从上轮失败恢复为成功。
2. H5 `/calculator` 已恢复正常展示试算结果和“确认借款”按钮。
3. `GET /api/benefits/card-detail` 已恢复成功。
4. H5 `/benefits-card` 已恢复正常展示协议、默认卡、权益内容。
5. `POST /api/benefits/activate` 已恢复成功。
6. 已拿到 `benefits sync benefiturl generated` 日志。
7. 已拿到 `path=/benefit/sync requestBodyJson`，其中真实包含 `benefiturl`。
8. `GET /api/repayment/info/{loanId}` 对有效 `loanId` 已恢复成功。
9. H5 `/confirm-repayment?loanId=LN-4539d2c1bd8847b59b413d72391409d3` 已恢复正常。
10. H5 还款交互已成功走通：
    - `sms-send`
    - `sms-confirm`
    - `repayment/submit`
    - `/repayment-success`

第二轮 `loan/calculate` 接口证据：

```http
POST /api/loan/calculate
X-Trace-Id: TRACE_NIGHT_ROUND2_LOAN_CALC_001
Content-Type: application/json

{"amount":3000,"term":3}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_ROUND2_LOAN_CALC_001

{"code":0,"message":"OK","data":{"totalFee":123.50,"annualRate":"18.0%","repaymentPlan":[{"period":1,"date":"2026-05-29","principal":1000.00,"interest":45.00,"total":1045.00},{"period":2,"date":"2026-06-29","principal":1000.00,"interest":40.00,"total":1040.00},{"period":3,"date":"2026-07-29","principal":1000.00,"interest":38.50,"total":1038.50}]}}
```

第二轮 `benefits/activate` 接口证据：

```http
POST /api/benefits/activate
X-Trace-Id: TRACE_NIGHT_ROUND2_BENEFITS_ACTIVATE_001
Content-Type: application/json

{"applicationId":"APP-night-activate-002","cardType":"huixuan_card","token":"joint-token-benefit-001"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_ROUND2_BENEFITS_ACTIVATE_001

{"code":0,"message":"OK","data":{"activationId":"ord19afee31cb13456ea2ff94b490c2a03a","status":"activated","message":"惠选卡开通成功"}}
```

第二轮关键 backend 日志：

```text
traceId=TRACE_NIGHT_ROUND2_LOAN_CALC_001 bizOrderNo=loan-calculate loan calculate requested by memberId=mem4df7afe3c3c54dd6a8e56497b68ad827
traceId=TRACE_NIGHT_ROUND2_LOAN_CALC_001 path=/loan/trial requestBodyJson={"requestId":"LC-ae653b33ef50468f8918d5701bd7ac8b","path":"/loan/trial","bizOrderNo":"LC-ae653b33ef50468f8918d5701bd7ac8b","data":{"uid":"tech-user-local-001","applyId":"LC-ae653b33ef50468f8918d5701bd7ac8b","loanAmount":300000,"loanPeriod":3}} yunka gateway request begin
traceId=TRACE_NIGHT_ROUND2_LOAN_CALC_001 path=/loan/trial elapsedMs=24 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"receiveAmount":300000,"repayAmount":312350,"yearRate":18,"repayPlan":[{"period":1,"date":"2026-05-29","principal":100000,"interest":4500,"total":104500},{"period":2,"date":"2026-06-29","principal":100000,"interest":4000,"total":104000},{"period":3,"date":"2026-07-29","principal":100000,"interest":3850,"total":103850}]}} yunka gateway request success

traceId=TRACE_NIGHT_ROUND2_BENEFITS_ACTIVATE_001 bizOrderNo=ord19afee31cb13456ea2ff94b490c2a03a benefits sync benefiturl generated applicationId=APP-night-activate-002 benefitUrlPresent=true
traceId=TRACE_NIGHT_ROUND2_BENEFITS_ACTIVATE_001 path=/benefit/sync requestBodyJson={"requestId":"BENEFITS-SYNC-APP-night-activate-002","path":"/benefit/sync","bizOrderNo":"ord19afee31cb13456ea2ff94b490c2a03a","data":{"benefiturl":"https://mock-qw.local/exercise?partnerOrderNo=ord19afee31cb13456ea2ff94b490c2a03a&uniqueId=cid-joint-token-benefit-001&token=mock-token-ord19afee31cb13456ea2ff94b490c2a03a","userId":"tech-user-local-001","platformBenefitOrderNo":"APP-night-activate-002","benefitStatus":"ACTIVE","benefitAmount":300000}} yunka gateway request begin
traceId=TRACE_NIGHT_ROUND2_BENEFITS_ACTIVATE_001 path=/benefit/sync elapsedMs=13 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"status":"SUCCESS","msg":"权益订单同步成功","acceptedBenefitUrl":"https://mock-qw.local/exercise?partnerOrderNo=ord19afee31cb13456ea2ff94b490c2a03a&uniqueId=cid-joint-token-benefit-001&token=mock-token-ord19afee31cb13456ea2ff94b490c2a03a"}} yunka gateway request success
```

第二轮 SQL 证据：

```sql
SELECT benefit_order_no, member_id, external_user_id, order_status, sync_status, request_id, created_ts, updated_ts
FROM nexusfin_equity.benefit_order
WHERE request_id = 'activate-APP-night-activate-002';
```

```json
[{"benefit_order_no":"ord19afee31cb13456ea2ff94b490c2a03a","member_id":"mem4df7afe3c3c54dd6a8e56497b68ad827","external_user_id":"tech-user-local-001","order_status":"FIRST_DEDUCT_PENDING","sync_status":"SYNC_SUCCESS","request_id":"activate-APP-night-activate-002","created_ts":"2026-05-01T00:15:08.000Z","updated_ts":"2026-05-01T00:15:08.000Z"}]
```

H5 还款交互证据：

```http
POST /api/repayment/sms-send
{"loanId":"LN-4539d2c1bd8847b59b413d72391409d3","bankCardId":"6222020202028648"}
-> {"code":0,"message":"OK","data":{"smsSeq":"sms-stub-001","status":"sent","message":"验证码已发送"}}

POST /api/repayment/sms-confirm
{"loanId":"LN-4539d2c1bd8847b59b413d72391409d3","captcha":"123456"}
-> {"code":0,"message":"OK","data":{"status":"confirmed","message":"验证码校验成功"}}

POST /api/repayment/submit
{"loanId":"LN-4539d2c1bd8847b59b413d72391409d3","amount":1018.5,"bankCardId":"6222020202028648","repaymentType":"early"}
-> {"code":0,"message":"OK","data":{"repaymentId":"RP-LN-4539d2c1bd8847b59b413d72391409d3","status":"processing","message":"还款请求已提交，正在处理中"}}

GET /api/repayment/result/RP-LN-4539d2c1bd8847b59b413d72391409d3
-> {"code":0,"message":"OK","data":{"repaymentId":"RP-LN-4539d2c1bd8847b59b413d72391409d3","swiftNumber":"RP-LN-4539d2c1bd8847b59b413d72391409d3","status":"success","amount":1018.50,...}}
```

### 11.2 第二轮新 FAIL

FAIL：

1. H5 `/benefits-card` 页面虽然已恢复展示，但普通 SSO 路径下点击“立即开通”仍会本地报错：

```text
加载失败
缺少联登凭证，暂无法生成权益跳转链接。
```

当前判断：

1. 这是前端页面接线问题，不是上游接口失败。
2. `ApprovalPendingPage` 跳转到 `/benefits-card` 时只传了 `applicationId`，没有 joint token。
3. `BenefitsCardPage.handleActivate()` 又强依赖 `readJointLoginParams()?.token`。
4. 对普通借款 / SSO 用户，这个 token 不存在，因此页面按钮无法完成开通。

代码证据：

```text
H5/client/src/pages/ApprovalPendingPage.tsx
  buildPath("/benefits-card", { applicationId })

H5/client/src/pages/BenefitsCardPage.tsx
  const jointLoginToken = readJointLoginParams()?.token;
  if (!jointLoginToken) {
    setError(MISSING_JOINT_TOKEN_COPY[locale]);
    return;
  }
```

### 11.3 第三轮结果

第三轮时间窗口：

```text
2026-05-01 08:27:12 ~ 08:30:34 +08:00
```

第三轮执行集合：

1. 针对 H5 `ApprovalPendingPage` / `BenefitsCardPage` 补最小防呆修复。
2. 新增前端逻辑单测并回归：
   - `client/src/pages/benefits-card.logic.test.ts`
   - `client/src/pages/approval-pending-benefits.logic.test.ts`
3. 执行 H5 校验：
   - `cd H5 && ./node_modules/.bin/vitest run client/src/pages/benefits-card.logic.test.ts client/src/pages/approval-pending-benefits.logic.test.ts`
   - `cd H5 && ./node_modules/.bin/vitest run`
   - `cd H5 && ./node_modules/.bin/tsc --noEmit`
4. 浏览器运行态回归：
   - 清空 joint-login session，验证普通 SSO 路径
   - 恢复带 token 的 joint-login session，验证不会误判成缺 token

第三轮修复内容：

1. `ApprovalPendingPage` 只有在当前会话存在 joint-login token 时才展示权益入口。
2. `BenefitsCardPage` 将“缺少 joint-login token”纳入 CTA enable 条件，而不是点击后才抛本地错误。
3. `BenefitsCardPage` 在协议已就绪但 token 缺失时，直接展示前置提示文案。

第三轮本地校验结果：

```text
RUN  v2.1.9 /Users/lixiaokun/Projects/nexusFin/nexusfin-equity/H5/client

✓ src/pages/approval-pending-benefits.logic.test.ts (4 tests)
✓ src/pages/benefits-card.logic.test.ts (6 tests)

Test Files  2 passed (2)
Tests       10 passed (10)
```

全量 H5 单测结果：

```text
RUN  v2.1.9 /Users/lixiaokun/Projects/nexusFin/nexusfin-equity/H5/client

Test Files  15 passed (15)
Tests       67 passed (67)
```

`tsc --noEmit` 结果：

```text
exit code 0
```

第三轮浏览器运行态证据一：普通 SSO 链清空 joint session 后，权益页不再报本地错误，而是直接禁用 CTA 并展示前置提示

```text
URL: /benefits-card?applicationId=APP-night-001
sessionStorage.removeItem('nexusfin.h5.joint-login.params')
```

页面快照关键内容：

```text
button "立即开通 享专属权益" disabled
StaticText "缺少联登凭证，暂无法生成权益跳转链接。"
button "立即开通" disabled
```

第三轮浏览器运行态证据二：恢复 joint-login session 后，不再出现“缺 token”提示，页面按真实协议状态控制 CTA

```text
sessionStorage['nexusfin.h5.joint-login.params'] =
{"token":"joint-token-benefit-001","scene":"exercise","benefitOrderNo":"ord06dd5d69aa34499abf7e1151715937c1"}
```

页面快照关键内容：

```text
StaticText "签约状态待完成"
StaticText "当前缺少可用协议或扣费协议未生效，请先完成签约后再开通权益。"
```

第三轮结论：

1. 上一轮发现的 P1-3 已在当前 workspace 修复并完成前端运行态回归。
2. 普通借款 / SSO 链路不再暴露“可点击但必失败”的权益开通入口。
3. `benefits-card` 在无 token 场景下不再制造新的假失败请求，也不会误导成页面/后端异常。
4. 当前仍未新增 invalid token 运行态 fault，因此 `JOINT_LOGIN_TOKEN_INVALID` 仍保持 `BLOCKED`。

### 11.4 第四轮结果

第四轮时间窗口：

```text
2026-05-01 09:46:59 ~ 09:47:48 +08:00
```

第四轮执行集合：

1. 为本地 Yunka stub 新增 `/user/token` 语义化 invalid-token fault：
   - `_FAULT_TOKEN_INVALID`
   - `_FAULT_SESSION_EXPIRED`
2. 执行 `node --test scripts/local-stubs/yunka-stub.test.js`
3. 重启 fullflow runtime
4. 回归 `GET /api/equity/health`
5. 运行态验证：
   - `POST /api/auth/joint-login` invalid token
   - `POST /api/auth/redrect_benefit_url` invalid token
   - `POST /api/auth/joint-login` session expired

第四轮 stub 校验结果：

```text
tests 7
pass 7
fail 0
```

第四轮请求一：joint-login invalid token

```http
POST /api/auth/joint-login
X-Trace-Id: TRACE_NIGHT_ROUND4_JOINT_INVALID_001
Content-Type: application/json

{"token":"joint-token-invalid-runtime-001_FAULT_TOKEN_INVALID","scene":"push"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_ROUND4_JOINT_INVALID_001

{"code":-1,"message":"JOINT_LOGIN_TOKEN_INVALID:Joint login session expired","data":null}
```

第四轮请求二：benefit redirect invalid token

```http
POST /api/auth/redrect_benefit_url
X-Trace-Id: TRACE_NIGHT_ROUND4_REDIRECT_INVALID_001
Content-Type: application/json

{"token":"joint-token-invalid-runtime-001_FAULT_TOKEN_INVALID","benefitOrderNo":"ord06dd5d69aa34499abf7e1151715937c1"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_ROUND4_REDIRECT_INVALID_001

{"code":-1,"message":"JOINT_LOGIN_TOKEN_INVALID:Joint login session expired","data":null}
```

第四轮请求三：joint-login session expired

```http
POST /api/auth/joint-login
X-Trace-Id: TRACE_NIGHT_ROUND4_JOINT_EXPIRED_001
Content-Type: application/json

{"token":"joint-token-expired-runtime-001_FAULT_SESSION_EXPIRED","scene":"push"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_ROUND4_JOINT_EXPIRED_001

{"code":-1,"message":"JOINT_LOGIN_TOKEN_INVALID:Joint login session expired","data":null}
```

第四轮关键 backend 日志：

```text
traceId=TRACE_NIGHT_ROUND4_JOINT_INVALID_001 path=/user/token requestBodyJson={"requestId":"xa68a34946493340bba902dd1f8defa380","path":"/user/token","bizOrderNo":null,"data":{"userId":null,"token":"joint-token-invalid-runtime-001_FAULT_TOKEN_INVALID"}} yunka gateway request begin
traceId=TRACE_NIGHT_ROUND4_JOINT_INVALID_001 path=/user/token elapsedMs=77 yunkaCode=40101 errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=token invalid responseBodyJson={"code":40101,"message":"token invalid","traceId":null,"requestId":null,"data":null}
traceId=TRACE_NIGHT_ROUND4_JOINT_INVALID_001 method=POST path=/api/auth/joint-login errorNo=JOINT_LOGIN_TOKEN_INVALID errorMsg=Joint login session expired

traceId=TRACE_NIGHT_ROUND4_REDIRECT_INVALID_001 path=/user/token requestBodyJson={"requestId":"xa5ad3300c30f747cf81fe4778d4575464","path":"/user/token","bizOrderNo":"ord06dd5d69aa34499abf7e1151715937c1","data":{"userId":null,"token":"joint-token-invalid-runtime-001_FAULT_TOKEN_INVALID"}} yunka gateway request begin
traceId=TRACE_NIGHT_ROUND4_REDIRECT_INVALID_001 path=/user/token elapsedMs=77 yunkaCode=40101 errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=token invalid responseBodyJson={"code":40101,"message":"token invalid","traceId":null,"requestId":null,"data":null}
traceId=TRACE_NIGHT_ROUND4_REDIRECT_INVALID_001 bizOrderNo=ord06dd5d69aa34499abf7e1151715937c1 benefit redirect joint login failed errorNo=JOINT_LOGIN_TOKEN_INVALID errorMsg=Joint login session expired
traceId=TRACE_NIGHT_ROUND4_REDIRECT_INVALID_001 method=POST path=/api/auth/redrect_benefit_url errorNo=JOINT_LOGIN_TOKEN_INVALID errorMsg=Joint login session expired

traceId=TRACE_NIGHT_ROUND4_JOINT_EXPIRED_001 path=/user/token requestBodyJson={"requestId":"xa189703bd336b4b3fbf790540cdc39be3","path":"/user/token","bizOrderNo":null,"data":{"userId":null,"token":"joint-token-expired-runtime-001_FAULT_SESSION_EXPIRED"}} yunka gateway request begin
traceId=TRACE_NIGHT_ROUND4_JOINT_EXPIRED_001 path=/user/token elapsedMs=5 yunkaCode=40101 errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=session expired responseBodyJson={"code":40101,"message":"session expired","traceId":null,"requestId":null,"data":null}
traceId=TRACE_NIGHT_ROUND4_JOINT_EXPIRED_001 method=POST path=/api/auth/joint-login errorNo=JOINT_LOGIN_TOKEN_INVALID errorMsg=Joint login session expired
```

第四轮结论：

1. `invalid token` 语义已不再是 `BLOCKED`。
2. `POST /api/auth/joint-login` 已真实验证：
   - `token invalid -> JOINT_LOGIN_TOKEN_INVALID`
   - `session expired -> JOINT_LOGIN_TOKEN_INVALID`
3. `POST /api/auth/redrect_benefit_url` 已真实验证 joint-login 子链失败不会误映射成 QW redirect 失败。
4. 本地错误收口稳定为 `HTTP 200 + code=-1`，没有落通用 500。

### 11.5 第五轮结果

第五轮时间窗口：

```text
2026-05-01 09:49:10 ~ 09:57:38 +08:00
```

第五轮执行集合：

1. 重新建立 SSO 登录态
2. 校验 `GET /api/users/me`
3. 使用新 `applicationId=APP-night-activate-005` 执行一条新的 `POST /api/benefits/activate`
4. 回查 backend log 中的 `benefiturl -> /benefit/sync` 实发证据
5. 回查 `benefit_order` 与补偿任务 SQL

第五轮用户基线：

```json
{"code":0,"message":"OK","data":{"memberId":"mem4df7afe3c3c54dd6a8e56497b68ad827","techPlatformUserId":"tech-user-local-001","externalUserId":"tech-user-local-001","memberStatus":"ACTIVE"}}
```

第五轮核心请求：

```http
POST /api/benefits/activate
X-Trace-Id: TRACE_NIGHT_ROUND5_BENEFITS_ACTIVATE_001
Content-Type: application/json

{"applicationId":"APP-night-activate-005","cardType":"huixuan_card","token":"joint-token-benefit-001"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_NIGHT_ROUND5_BENEFITS_ACTIVATE_001

{"code":0,"message":"OK","data":{"activationId":"ord745622266f074850ab5d5dc2bf577bda","status":"activated","message":"惠选卡开通成功"}}
```

第五轮关键 backend 日志：

```text
traceId=TRACE_NIGHT_ROUND5_BENEFITS_ACTIVATE_001 bizOrderNo=ord745622266f074850ab5d5dc2bf577bda benefits sync benefiturl generated applicationId=APP-night-activate-005 benefitUrlPresent=true
traceId=TRACE_NIGHT_ROUND5_BENEFITS_ACTIVATE_001 path=/benefit/sync requestBodyJson={"requestId":"BENEFITS-SYNC-APP-night-activate-005","path":"/benefit/sync","bizOrderNo":"ord745622266f074850ab5d5dc2bf577bda","data":{"benefiturl":"https://mock-qw.local/exercise?partnerOrderNo=ord745622266f074850ab5d5dc2bf577bda&uniqueId=cid-joint-token-benefit-001&token=mock-token-ord745622266f074850ab5d5dc2bf577bda","userId":"tech-user-local-001","platformBenefitOrderNo":"APP-night-activate-005","benefitStatus":"ACTIVE","benefitAmount":300000}} yunka gateway request begin
traceId=TRACE_NIGHT_ROUND5_BENEFITS_ACTIVATE_001 path=/benefit/sync elapsedMs=12 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"status":"SUCCESS","msg":"权益订单同步成功","acceptedBenefitUrl":"https://mock-qw.local/exercise?partnerOrderNo=ord745622266f074850ab5d5dc2bf577bda&uniqueId=cid-joint-token-benefit-001&token=mock-token-ord745622266f074850ab5d5dc2bf577bda"}} yunka gateway request success
```

第五轮 SQL 证据：

```sql
SELECT benefit_order_no, member_id, external_user_id, order_status, sync_status, request_id, created_ts, updated_ts
FROM nexusfin_equity.benefit_order
WHERE request_id = 'activate-APP-night-activate-005';
```

```json
[{"benefit_order_no":"ord745622266f074850ab5d5dc2bf577bda","member_id":"mem4df7afe3c3c54dd6a8e56497b68ad827","external_user_id":"tech-user-local-001","order_status":"FIRST_DEDUCT_PENDING","sync_status":"SYNC_SUCCESS","request_id":"activate-APP-night-activate-005","created_ts":"2026-05-01T01:57:38.000Z","updated_ts":"2026-05-01T01:57:39.000Z"}]
```

```sql
SELECT COUNT(*) AS compensation_task_count
FROM nexusfin_equity.async_compensation_task
WHERE biz_order_no = 'ord745622266f074850ab5d5dc2bf577bda'
   OR biz_key LIKE '%ord745622266f074850ab5d5dc2bf577bda%';
```

```json
[{"compensation_task_count":0}]
```

第五轮结论：

1. `benefiturl` 主链运行态实发证据已再次刷新，不再只依赖第二轮旧单据。
2. 新单据 `ord745622266f074850ab5d5dc2bf577bda` 已确认：
   - 本地 `benefit_order` 落库成功
   - `sync_status=SYNC_SUCCESS`
   - Yunka `/benefit/sync` payload 真实包含 `benefiturl`
   - 无额外补偿副作用

### 11.6 第六轮结果

第六轮时间窗口：

```text
2026-05-01 09:59:20 ~ 09:59:40 +08:00
```

第六轮执行集合：

1. `POST /api/loan/calculate`
2. `GET /api/repayment/info/LN-4539d2c1bd8847b59b413d72391409d3`
3. `GET /api/repayment/info/LN-9731edd8858644d691862e6439c4d503`
4. `POST /api/repayment/submit`
5. `GET /api/repayment/info/LN-NOT-FOUND-ROUND6-001`
6. SQL 回查历史 `loan_application_mapping`

第六轮关键响应：

```http
POST /api/loan/calculate
X-Trace-Id: TRACE_NIGHT_ROUND6_LOAN_CALC_001
-> {"code":0,"message":"OK","data":{"totalFee":123.50,"annualRate":"18.0%",...}}
```

```http
GET /api/repayment/info/LN-4539d2c1bd8847b59b413d72391409d3
X-Trace-Id: TRACE_NIGHT_ROUND6_REPAY_INFO_VALID_001
-> {"code":0,"message":"OK","data":{"loanId":"LN-4539d2c1bd8847b59b413d72391409d3","repaymentAmount":1018.50,...}}
```

```http
POST /api/repayment/submit
X-Trace-Id: TRACE_NIGHT_ROUND6_REPAY_SUBMIT_001
-> {"code":0,"message":"OK","data":{"repaymentId":"RP-LN-4539d2c1bd8847b59b413d72391409d3","status":"processing","message":"还款请求已提交，正在处理中"}}
```

```http
GET /api/repayment/info/LN-NOT-FOUND-ROUND6-001
X-Trace-Id: TRACE_NIGHT_ROUND6_REPAY_INFO_MISSING_001
-> {"code":404,"message":"repayment loan reference not found","data":null}
```

第六轮关键 backend 日志：

```text
traceId=TRACE_NIGHT_ROUND6_LOAN_CALC_001 path=/loan/trial requestBodyJson={"requestId":"LC-761496b2c7a3432ba02b1b392a82b0a3","path":"/loan/trial","bizOrderNo":"LC-761496b2c7a3432ba02b1b392a82b0a3","data":{"uid":"tech-user-local-001","applyId":"LC-761496b2c7a3432ba02b1b392a82b0a3","loanAmount":300000,"loanPeriod":3}} yunka gateway request begin
traceId=TRACE_NIGHT_ROUND6_LOAN_CALC_001 path=/loan/trial elapsedMs=12 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"receiveAmount":300000,"repayAmount":312350,"yearRate":18,"repayPlan":[...]}} yunka gateway request success

traceId=TRACE_NIGHT_ROUND6_REPAY_SUBMIT_001 path=/repay/apply requestBodyJson={"requestId":"RS-891387da548042eabbe08f040cf661bc","path":"/repay/apply","bizOrderNo":"LN-4539d2c1bd8847b59b413d72391409d3","data":{"uid":"tech-user-local-001","loanId":"LN-4539d2c1bd8847b59b413d72391409d3","repayType":"EARLY","periods":[],"bankCardNo":"6222020202028648","repayAmount":101850}} yunka gateway request begin
traceId=TRACE_NIGHT_ROUND6_REPAY_SUBMIT_001 path=/repay/apply elapsedMs=10 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"status":"5001","swiftNumber":"RP-LN-4539d2c1bd8847b59b413d72391409d3","remark":"还款请求已提交，正在处理中"}} yunka gateway request success

traceId=TRACE_NIGHT_ROUND6_REPAY_INFO_MISSING_001 method=GET path=/api/repayment/info/LN-NOT-FOUND-ROUND6-001 errorNo=RESOURCE_NOT_FOUND errorMsg=repayment loan reference not found
```

第六轮 SQL 说明：

```json
[{"application_id":"APP-ac4873a34e174237909c3339aa3bf5f6","upstream_query_value":"LN-4539d2c1bd8847b59b413d72391409d3","mapping_status":"ACTIVE"},{"application_id":"APP-1e04aab83e934585a0c01b2ac133599d","upstream_query_value":"LN-9731edd8858644d691862e6439c4d503","mapping_status":"ACTIVE"}]
```

第六轮结论：

1. `loan/calculate`、`repayment/info(valid)`、`repayment/submit` 当前本地联调基线均正常。
2. 历史上曾作为 stale 样例的 `LN-9731...` 现已在库里存在 `ACTIVE` 映射，因此本轮返回成功，不再适合作为 404 样例。
3. 受控 404 仍成立，但需要使用明确不存在的 loanId。

### 11.7 第七轮结果

第七轮时间窗口：

```text
2026-05-01 10:00:26 ~ 10:02:36 +08:00
```

第七轮执行集合：

1. H5 invalid-token 错误页：`/joint-entry?token=joint-token-invalid-runtime-001_FAULT_TOKEN_INVALID&scene=push`
2. H5 正常借款页：`/calculator`
3. H5 权益页：`/benefits-card?applicationId=APP-night-001`
4. 浏览器内先做 SSO 登录，再回归 `confirm-repayment`
5. H5 正常 joint-login landing：`/joint-entry?token=joint-token-push-runtime-001&scene=push`
6. 回查浏览器 console

第七轮页面快照要点：

```text
invalid token 页面：
- 加载失败
- 联合登录会话已失效，请返回来源页面重新进入。

/calculator：
- 总息费 ¥123.50
- 综合年化成本 18.0%
- 按钮：确认借款

/benefits-card：
- 页面可正常打开
- CTA disabled
- 提示：当前缺少可用协议或扣费协议未生效，请先完成签约后再开通权益。

/confirm-repayment（浏览器内补登录后）：
- 确认还款
- 还款金额 ¥1,018.50
- 按钮：发送验证码

/landing：
- 惠聚平台
- 按钮：去借款
```

第七轮 console 结果：

1. `landing` 页面未见 console error / warn。
2. 其余页面仅见同类 browser issue：
   - `A form field element should have an id or name attribute`
3. 未观察到前端崩溃或空白页。

第七轮结论：

1. invalid-token H5 错误态已真实闭环到受控文案。
2. `calculator`、`benefits-card`、`confirm-repayment`、`landing` 页面都可打开。
3. `confirm-repayment` 的页面可达性依赖浏览器上下文已有登录态，这与后端接口状态一致。

### 11.8 第八轮结果

第八轮时间窗口：

```text
2026-05-01 10:03:12 +08:00
```

第八轮执行集合：

1. 未带 cookie 调 `GET /api/benefits/card-detail`
2. 未带 cookie 调 `POST /api/benefits/activate`
3. 未带 cookie 调 `GET /api/repayment/info/LN-4539d2c1bd8847b59b413d72391409d3`
4. 回查鉴权日志

第八轮响应：

```http
HTTP/1.1 401
{"code":401,"message":"Missing auth cookie","data":{"redirectHint":"joint-entry"}}
```

第八轮关键日志：

```text
method=GET path=/api/benefits/card-detail errorNo=UNAUTHORIZED errorMsg=Missing auth cookie
method=POST path=/api/benefits/activate errorNo=UNAUTHORIZED errorMsg=Missing auth cookie
method=GET path=/api/repayment/info/LN-4539d2c1bd8847b59b413d72391409d3 errorNo=UNAUTHORIZED errorMsg=Missing auth cookie
```

第八轮结论：

1. 代表性受保护接口在未登录场景下均继续返回 `401 + redirectHint=joint-entry`。
2. 本轮没有出现鉴权回退到通用 500 的情况。

### 11.9 第九轮结果

第九轮时间窗口：

```text
2026-05-01 10:03:38 +08:00
```

第九轮执行集合：

1. `POST /api/auth/joint-login`
2. `POST /api/loan/calculate`
3. 回查 caller traceId 是否贯通到：
   - controller
   - yunka gateway request begin
   - requestBodyJson
   - responseBodyJson
   - elapsedMs

第九轮请求一：

```http
POST /api/auth/joint-login
X-Trace-Id: TRACE_NIGHT_ROUND9_MONITOR_AUTH_001
-> {"code":0,"message":"OK","data":{"loginSuccess":true,"scene":"push","targetPage":"landing","benefitOrderNo":null,"localUserReady":true}}
```

第九轮请求二：

```http
POST /api/loan/calculate
X-Trace-Id: TRACE_NIGHT_ROUND9_MONITOR_LOAN_001
-> {"code":0,"message":"OK","data":{"totalFee":123.50,"annualRate":"18.0%",...}}
```

第九轮关键日志：

```text
traceId=TRACE_NIGHT_ROUND9_MONITOR_AUTH_001 path=/user/token requestBodyJson={"requestId":"xadb67402135ec456a807ff7a409fa6f9a","path":"/user/token","bizOrderNo":null,"data":{"userId":null,"token":"joint-token-push-runtime-001"}} yunka gateway request begin
traceId=TRACE_NIGHT_ROUND9_MONITOR_AUTH_001 path=/user/token elapsedMs=2 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"cid":"cid-joint-token-push-runtime-001","name":"联合登录用户","phone":"13800138000"}} yunka gateway request success
traceId=TRACE_NIGHT_ROUND9_MONITOR_AUTH_001 path=/user/query elapsedMs=5 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"userId":"memac3d4c4d15a94b249b0d295f06f5608e","cid":"cid-joint-token-push-runtime-001","phone":"13800138000","basicInfo":{"phone":"13800138000"},"idInfo":{"idno":"310101199001011111","name":"联合登录用户"}}} yunka gateway request success
traceId=TRACE_NIGHT_ROUND9_MONITOR_AUTH_001 bizOrderNo=null joint login entry resolved targetPage=landing

traceId=TRACE_NIGHT_ROUND9_MONITOR_LOAN_001 bizOrderNo=loan-calculate loan calculate requested by memberId=mem4df7afe3c3c54dd6a8e56497b68ad827
traceId=TRACE_NIGHT_ROUND9_MONITOR_LOAN_001 path=/loan/trial requestBodyJson={"requestId":"LC-e46e70fc77164843ae930fd1abb0cfa9","path":"/loan/trial","bizOrderNo":"LC-e46e70fc77164843ae930fd1abb0cfa9","data":{"uid":"tech-user-local-001","applyId":"LC-e46e70fc77164843ae930fd1abb0cfa9","loanAmount":300000,"loanPeriod":3}} yunka gateway request begin
traceId=TRACE_NIGHT_ROUND9_MONITOR_LOAN_001 path=/loan/trial elapsedMs=4 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"receiveAmount":300000,"repayAmount":312350,"yearRate":18,"repayPlan":[...]}} yunka gateway request success
```

第九轮结论：

1. caller 传入的 `X-Trace-Id` 已在 auth 和 loan 两条代表性链路上完整贯通。
2. backend 日志当前具备：
   - `requestBodyJson`
   - `responseBodyJson`
   - `elapsedMs`
   - controller 入口日志
3. 今晚本地联调基线下，监控排障所需的关键日志字段是可用的。

### 11.10 第十轮结果

第十轮时间窗口：

```text
2026-05-01 11:43:28 ~ 11:43:31 +08:00
```

第十轮执行集合：

1. `POST /api/auth/joint-login` token timeout
2. `POST /api/auth/joint-login` generic reject
3. `POST /api/auth/redrect_benefit_url` token timeout
4. `POST /api/auth/redrect_benefit_url` generic reject
5. 回查 backend 日志中的上游超时 / reject 映射

第十轮响应：

```http
TRACE_NIGHT_ROUND10_JOINT_TIMEOUT_001
-> {"code":-1,"message":"JOINT_LOGIN_UPSTREAM_TIMEOUT:Joint login temporarily unavailable","data":null}

TRACE_NIGHT_ROUND10_JOINT_REJECT_001
-> {"code":-1,"message":"JOINT_LOGIN_UPSTREAM_FAILED:Joint login temporarily unavailable","data":null}

TRACE_NIGHT_ROUND10_REDIRECT_TIMEOUT_001
-> {"code":-1,"message":"JOINT_LOGIN_UPSTREAM_TIMEOUT:Joint login temporarily unavailable","data":null}

TRACE_NIGHT_ROUND10_REDIRECT_REJECT_001
-> {"code":-1,"message":"JOINT_LOGIN_UPSTREAM_FAILED:Joint login temporarily unavailable","data":null}
```

第十轮关键 backend 日志：

```text
traceId=TRACE_NIGHT_ROUND10_JOINT_REJECT_001 path=/user/token elapsedMs=48 yunkaCode=503 errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=Mock Yunka rejection code=503 responseBodyJson={"code":503,"message":"Mock Yunka rejection code=503","traceId":null,"requestId":null,"data":null}
traceId=TRACE_NIGHT_ROUND10_JOINT_REJECT_001 method=POST path=/api/auth/joint-login errorNo=JOINT_LOGIN_UPSTREAM_FAILED errorMsg=Joint login temporarily unavailable

traceId=TRACE_NIGHT_ROUND10_JOINT_TIMEOUT_001 path=/user/token elapsedMs=3017 errorNo=YUNKA_UPSTREAM_TIMEOUT errorMsg=Yunka gateway timeout
traceId=TRACE_NIGHT_ROUND10_JOINT_TIMEOUT_001 method=POST path=/api/auth/joint-login errorNo=JOINT_LOGIN_UPSTREAM_TIMEOUT errorMsg=Joint login temporarily unavailable

traceId=TRACE_NIGHT_ROUND10_REDIRECT_REJECT_001 benefit redirect joint login failed errorNo=JOINT_LOGIN_UPSTREAM_FAILED errorMsg=Joint login temporarily unavailable
traceId=TRACE_NIGHT_ROUND10_REDIRECT_TIMEOUT_001 benefit redirect joint login failed errorNo=JOINT_LOGIN_UPSTREAM_TIMEOUT errorMsg=Joint login temporarily unavailable
```

第十轮结论：

1. `joint-login` 的 timeout / generic reject 收口稳定。
2. `redrect_benefit_url` 在 joint-login 子链 timeout / reject 时，仍然把错误准确停留在 joint-login 层，不会伪装成 QW redirect 失败。
3. 全部请求均为 `HTTP 200 + code=-1`，未观察到 500。

### 11.11 第十一轮结果

第十一轮时间窗口：

```text
2026-05-01 10:00:26 ~ 10:02:36 +08:00
```

第十一轮执行集合：

1. 浏览器打开 invalid-token `joint-entry`
2. 浏览器打开 `/calculator`
3. 浏览器打开 `/benefits-card`
4. 浏览器内做一次 SSO 登录，再打开 `/confirm-repayment`
5. 浏览器打开正常 push `joint-entry -> /landing`
6. 检查 console

第十一轮页面结果：

1. `joint-entry invalid token`：
   - 页面展示 `联合登录会话已失效，请返回来源页面重新进入。`
   - 错误态受控，无白屏
2. `/calculator`：
   - 展示 `总息费 ¥123.50`
   - 展示 `综合年化成本 18.0%`
   - `确认借款` 按钮可见
3. `/benefits-card`：
   - 页面正常展示
   - `立即开通` CTA 为 disabled
   - 协议未就绪提示展示正确
4. `/confirm-repayment`：
   - 浏览器内补登录后，页面展示 `确认还款`、`还款金额 ¥1,018.50`、`发送验证码`
5. `/landing`：
   - 页面正常展示 `去借款`

第十一轮 console 结果：

1. 未见前端 error / warn 导致功能中断。
2. 继续观察到浏览器 issue：
   - `A form field element should have an id or name attribute`

第十一轮结论：

1. H5 关键页面当前可打开、可展示、错误态受控。
2. `confirm-repayment` 页面依赖浏览器上下文内已有登录态，这与接口鉴权语义一致，不是新的主链回退。

### 11.12 第十二轮结果

第十二轮时间窗口：

```text
2026-05-01 11:43:31 ~ 11:46:00 +08:00
```

第十二轮执行集合：

1. 汇总 Round 4 ~ Round 11 证据
2. 复核文档落盘完整性
3. 给出今夜本地联调基线最终收口结论

第十二轮最终收口：

PASS：

1. `joint-login` push 正常联登
2. `joint-login` invalid token / session expired / timeout / generic reject 受控收口
3. `redrect_benefit_url` 正常返回 redirectUrl，且 joint-login 子链异常收口正确
4. `benefiturl` 已多轮拿到主链运行态实发证据
5. `loan/calculate` 正常
6. `repayment/info(valid)`、`repayment/submit` 正常
7. 未登录态受保护接口稳定返回 `401 + redirectHint=joint-entry`
8. auth / loan 的 traceId 与 request/response/elapsedMs 日志完整

FAIL：

1. 无新的 P0 / P1 失败

BLOCKED：

1. 无新增 BLOCKED

OPEN：

1. H5 浏览器 issue：表单控件缺少 `id/name` 属性，当前记录为 `P2-1`

第十二轮最小结论：

1. 以“Yunka local stub + QW MOCK + tech-user stub”为基线，今夜本地联调主链已稳定。
2. 截至第十二轮，没有新增 P0 / P1 业务 blocker。
3. 当前剩余主要是低优先级 H5 可访问性 issue，而不是主链功能性阻断。

### 11.13 第十三轮结果

第十三轮时间窗口：

```text
2026-05-01 17:41:43 ~ 17:47:11 +08:00
```

第十三轮当前 runtime 基线：

1. HEAD=`4a8e5b9eb2e71ee1086cbbb012c589d679988ff1`
2. backend start=`2026-05-01T09:47:13.766+08:00`
3. QW=`MOCK`
4. Yunka=`REST -> http://127.0.0.1:18081/api/gateway/proxy`
5. health=`UP`
6. 当前 SSO 用户：`memberId=mem4df7afe3c3c54dd6a8e56497b68ad827` / `externalUserId=tech-user-local-001`

第十三轮执行集合：

1. `GET /api/equity/health`
2. `GET /api/auth/sso-callback?token=mock-tech-token&redirect_url=/equity/index`
3. `GET /api/users/me`
4. `POST /api/auth/joint-login`
5. `POST /api/auth/redrect_benefit_url`
6. `POST /api/loan/calculate`
7. `GET /api/benefits/card-detail`
8. `POST /api/benefits/activate`
9. `GET /api/repayment/info/{loanId}`
10. `POST /api/repayment/submit`
11. 安全代表性用例：未登录 `GET /api/benefits/card-detail`
12. joint-login 错误分支补证：invalid token
13. H5 页面：
    - `/joint-entry?token=joint-token-push-runtime-001&scene=push`
    - `/calculator`
    - `/benefits-card?applicationId=APP-stability-r13-001`
    - `/confirm-repayment?loanId=LN-4539d2c1bd8847b59b413d72391409d3`
14. backend 日志回捞与 SQL 回查

第十三轮 PASS：

1. `/api/equity/health` 持续返回 `UP`。
2. `POST /api/auth/joint-login` 正常返回 `code=0`，`targetPage=landing`。
3. `POST /api/auth/redrect_benefit_url` 正常返回非空 `redirectUrl`。
4. `POST /api/loan/calculate` 持续返回 `code=0`，试算结果稳定。
5. `GET /api/benefits/card-detail` 持续返回 `code=0`，协议与默认卡信息完整。
6. `POST /api/benefits/activate` 新增单据 `ord9dceb0f1b77440d6becdfde5a1614faf`，接口成功。
7. SQL 回查确认新权益单 `sync_status=SYNC_SUCCESS`，且无新增补偿任务。
8. `GET /api/repayment/info/LN-4539d2c1bd8847b59b413d72391409d3` 返回有效还款信息。
9. `POST /api/repayment/submit` 返回 `repaymentId=RP-LN-4539d2c1bd8847b59b413d72391409d3`，状态 `processing`。
10. 安全代表性用例持续稳定：未登录 `GET /api/benefits/card-detail` 返回 `401 + redirectHint=joint-entry`。
11. invalid token 运行态继续稳定收口到 `JOINT_LOGIN_TOKEN_INVALID`。
12. 浏览器本轮未发现新的 console error / warn，只有既有 Vite/React 开发提示。
13. H5 `/joint-entry`、`/calculator`、`/benefits-card` 均可正常打开；`/confirm-repayment` 在当前浏览器联登用户上下文下展示受控 404 错误页，而不是白屏。

第十三轮新 FAIL：

1. 无

第十三轮新 BLOCKED：

1. 无

第十三轮关键证据：

接口响应摘录：

```http
POST /api/auth/joint-login
X-Trace-Id: TRACE_STABILITY_R13_JOINT_OK_001
{"token":"joint-token-push-runtime-001","scene":"push"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_STABILITY_R13_JOINT_OK_001

{"code":0,"message":"OK","data":{"loginSuccess":true,"scene":"push","targetPage":"landing","benefitOrderNo":null,"localUserReady":true}}
```

```http
POST /api/auth/redrect_benefit_url
X-Trace-Id: TRACE_STABILITY_R13_REDIRECT_OK_001
{"token":"joint-token-benefit-001","benefitOrderNo":"ord745622266f074850ab5d5dc2bf577bda"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_STABILITY_R13_REDIRECT_OK_001

{"code":0,"message":"OK","data":{"redirectUrl":"https://mock-qw.local/exercise?partnerOrderNo=ord745622266f074850ab5d5dc2bf577bda&uniqueId=cid-joint-token-benefit-001&token=mock-token-ord745622266f074850ab5d5dc2bf577bda"}}
```

```http
POST /api/loan/calculate
X-Trace-Id: TRACE_STABILITY_R13_LOAN_CALC_001
{"amount":3000,"term":3}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_STABILITY_R13_LOAN_CALC_001

{"code":0,"message":"OK","data":{"totalFee":123.50,"annualRate":"18.0%","repaymentPlan":[{"period":1,"date":"2026-05-29","principal":1000.00,"interest":45.00,"total":1045.00},{"period":2,"date":"2026-06-29","principal":1000.00,"interest":40.00,"total":1040.00},{"period":3,"date":"2026-07-29","principal":1000.00,"interest":38.50,"total":1038.50}]}}
```

```http
POST /api/benefits/activate
X-Trace-Id: TRACE_STABILITY_R13_BENEFITS_ACTIVATE_001
{"applicationId":"APP-stability-r13-001","cardType":"huixuan_card","token":"joint-token-benefit-001"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_STABILITY_R13_BENEFITS_ACTIVATE_001

{"code":0,"message":"OK","data":{"activationId":"ord9dceb0f1b77440d6becdfde5a1614faf","status":"activated","message":"惠选卡开通成功"}}
```

```http
POST /api/repayment/submit
X-Trace-Id: TRACE_STABILITY_R13_REPAY_SUBMIT_001
{"loanId":"LN-4539d2c1bd8847b59b413d72391409d3","amount":1018.5,"bankCardId":"6222020202028648","repaymentType":"early"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_STABILITY_R13_REPAY_SUBMIT_001

{"code":0,"message":"OK","data":{"repaymentId":"RP-LN-4539d2c1bd8847b59b413d72391409d3","status":"processing","message":"还款请求已提交，正在处理中"}}
```

```http
POST /api/auth/joint-login
X-Trace-Id: TRACE_STABILITY_R13_JOINT_INVALID_001
{"token":"joint-token-invalid-runtime-001_FAULT_TOKEN_INVALID","scene":"push"}
```

```http
HTTP/1.1 200
X-Trace-Id: TRACE_STABILITY_R13_JOINT_INVALID_001

{"code":-1,"message":"JOINT_LOGIN_TOKEN_INVALID:Joint login session expired","data":null}
```

backend 日志摘录：

```text
traceId=TRACE_STABILITY_R13_LOAN_CALC_001 path=/loan/trial requestBodyJson={"requestId":"LC-1189ceaf8b0a498a8fa54324c2d17ffd","path":"/loan/trial","bizOrderNo":"LC-1189ceaf8b0a498a8fa54324c2d17ffd","data":{"uid":"tech-user-local-001","applyId":"LC-1189ceaf8b0a498a8fa54324c2d17ffd","loanAmount":300000,"loanPeriod":3}} yunka gateway request begin
traceId=TRACE_STABILITY_R13_LOAN_CALC_001 path=/loan/trial elapsedMs=40 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"receiveAmount":300000,"repayAmount":312350,"yearRate":18,"repayPlan":[{"period":1,"date":"2026-05-29","principal":100000,"interest":4500,"total":104500},{"period":2,"date":"2026-06-29","principal":100000,"interest":4000,"total":104000},{"period":3,"date":"2026-07-29","principal":100000,"interest":3850,"total":103850}]}} yunka gateway request success
traceId=TRACE_STABILITY_R13_BENEFITS_ACTIVATE_001 bizOrderNo=ord9dceb0f1b77440d6becdfde5a1614faf benefits sync benefiturl generated applicationId=APP-stability-r13-001 benefitUrlPresent=true
traceId=TRACE_STABILITY_R13_BENEFITS_ACTIVATE_001 path=/benefit/sync requestBodyJson={"requestId":"BENEFITS-SYNC-APP-stability-r13-001","path":"/benefit/sync","bizOrderNo":"ord9dceb0f1b77440d6becdfde5a1614faf","data":{"benefiturl":"https://mock-qw.local/exercise?partnerOrderNo=ord9dceb0f1b77440d6becdfde5a1614faf&uniqueId=cid-joint-token-benefit-001&token=mock-token-ord9dceb0f1b77440d6becdfde5a1614faf","userId":"tech-user-local-001","platformBenefitOrderNo":"APP-stability-r13-001","benefitStatus":"ACTIVE","benefitAmount":300000}} yunka gateway request begin
traceId=TRACE_STABILITY_R13_BENEFITS_ACTIVATE_001 path=/benefit/sync elapsedMs=5 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"status":"SUCCESS","msg":"权益订单同步成功","acceptedBenefitUrl":"https://mock-qw.local/exercise?partnerOrderNo=ord9dceb0f1b77440d6becdfde5a1614faf&uniqueId=cid-joint-token-benefit-001&token=mock-token-ord9dceb0f1b77440d6becdfde5a1614faf"}} yunka gateway request success
traceId=TRACE_STABILITY_R13_REPAY_SUBMIT_001 path=/repay/apply requestBodyJson={"requestId":"RS-55fe0d1514a44a23a120734affb271f4","path":"/repay/apply","bizOrderNo":"LN-4539d2c1bd8847b59b413d72391409d3","data":{"uid":"tech-user-local-001","loanId":"LN-4539d2c1bd8847b59b413d72391409d3","repayType":"EARLY","periods":[],"bankCardNo":"6222020202028648","repayAmount":101850}} yunka gateway request begin
traceId=TRACE_STABILITY_R13_REPAY_SUBMIT_001 path=/repay/apply elapsedMs=5 yunkaCode=0 responseBodyJson={"code":0,"message":"SUCCESS","traceId":null,"requestId":null,"data":{"status":"5001","swiftNumber":"RP-LN-4539d2c1bd8847b59b413d72391409d3","remark":"还款请求已提交，正在处理中"}} yunka gateway request success
traceId=TRACE_STABILITY_R13_JOINT_INVALID_001 path=/user/token elapsedMs=7 yunkaCode=40101 errorNo=YUNKA_UPSTREAM_REJECTED errorMsg=token invalid responseBodyJson={"code":40101,"message":"token invalid","traceId":null,"requestId":null,"data":null}
traceId=TRACE_STABILITY_R13_JOINT_INVALID_001 method=POST path=/api/auth/joint-login errorNo=JOINT_LOGIN_TOKEN_INVALID errorMsg=Joint login session expired
```

SQL / 本地状态摘录：

```sql
SELECT benefit_order_no, request_id, order_status, sync_status, created_ts
FROM nexusfin_equity.benefit_order
WHERE benefit_order_no = 'ord9dceb0f1b77440d6becdfde5a1614faf';
```

```json
[{"benefit_order_no":"ord9dceb0f1b77440d6becdfde5a1614faf","request_id":"activate-APP-stability-r13-001","order_status":"FIRST_DEDUCT_PENDING","sync_status":"SYNC_SUCCESS","created_ts":"2026-05-01T09:44:20.000Z"}]
```

```sql
SELECT task_id, task_type, biz_order_no, task_status, created_ts
FROM nexusfin_equity.async_compensation_task
WHERE biz_order_no = 'ord9dceb0f1b77440d6becdfde5a1614faf';
```

```json
[]
```

H5 页面快照摘要：

```text
/joint-entry?token=joint-token-push-runtime-001&scene=push -> 自动落到 /landing，页面可见“去借款”
/calculator -> 展示“总息费 ¥123.50”“综合年化成本 18.0%”“确认借款”
/benefits-card?applicationId=APP-stability-r13-001 -> 页面正常展示，CTA disabled，提示“当前缺少可用协议或扣费协议未生效，请先完成签约后再开通权益。”
/confirm-repayment?loanId=LN-4539... -> 当前浏览器联登用户上下文下展示“加载失败 / repayment loan reference not found”，为受控错误页
```

第十三轮最小结论：

1. 当前稳定基线未回退，接口主链和关键页面继续可巡检、可留证。
2. 本轮无新增本地 `FAIL / BLOCKED`。
3. `benefiturl` 主链、`joint-login` 错误分支、`repayment submit`、`401` 受控错误态都已补到最新运行态证据。
4. 仍需继续单独跟踪的只有外部 Yunka blocker、`benefiturl` 字段语义风险和低优先级 H5 页面质量项。
