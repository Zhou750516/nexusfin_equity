# 2026-05-06 联调完成度总结

> 口径基于 `docs/test/20260506_联调case设计方案.md`、`docs/test/20260506_联调结果.md` 与 `docs/test/20260506_联调case覆盖矩阵.md` 的最新真值，反映当前这一轮本地联调的实际完成情况。

## 1. 总体完成度

当前 `20260506` 联调 case 的完成情况如下：

- 总 case：`45`
- `COVERED`：`36`
- `PENDING`：`0`
- `BLOCKED`：`0`
- `N/A`：`9`

分节统计：

- `§4` 主流程：`17 COVERED / 0 PENDING / 0 BLOCKED / 5 N/A`
- `§5` 异常流程：`19 COVERED / 0 PENDING / 0 BLOCKED / 4 N/A`

最小结论：

1. 当前主联调范围内，需要执行的主流程与异常流程都已经拿到运行态证据。
2. 本轮已不存在活跃的 `PENDING` 和 `BLOCKED`。
3. 剩余 `N/A` 项不是漏测，而是按当前边界收紧后，不再属于本轮 FE + BE 主联调范围。

## 2. 已确认覆盖的主流程

当前已收口的主流程包括：

1. `joint-login`：
   - `push`
   - `exercise`
   - `refund`
2. `redrect_benefit_url`
3. `loan/calculate`
4. `benefits/card-detail`
5. `benefits/activate`
6. 签约主链：
   - `sign query`
   - `sign apply`
   - `sign confirm`
7. 借款主链：
   - 借款申请
   - 审批结果
8. 还款主链：
   - `repayment/info`
   - `repayment/submit`
   - `info -> sms-send -> sms-confirm -> submit -> result` 完整样本

## 3. 已确认覆盖的异常流程

当前已收口的异常流程包括：

1. `joint-login` 异常：
   - token 过期
   - token 缺失
   - token 篡改
2. 签约异常：
   - sign query 上游失败
   - sign apply 超时
   - sign confirm 超时
   - sign confirm 业务失败码
   - 重复 confirm 幂等
3. 借款异常：
   - loan apply 超时
   - loan apply 失败后重试
   - 审批轮询处理中
   - 审批拒绝
4. 还款异常：
   - 超额还款
   - 短信验证码错误
   - 提交超时
   - 重复还款
5. 页面 / 网络类：
   - 网络中断后恢复
   - 慢响应（不超时）
   - 异常字段 / 缺字段

## 4. 本轮已经关闭的真实业务问题

本轮联调过程中，已经确认并关闭的真实业务问题主要包括：

1. `/api/repayment/submit` 上游 timeout 之前会落成通用 `500`，现已改为受控错误 `YUNKA_UPSTREAM_TIMEOUT`。
2. 超额还款之前未拦截，现已在本地先校验可还金额，超额时直接返回 `REPAYMENT_AMOUNT_EXCEEDED`。
3. 重复还款之前会重复出站 `/repay/apply`，现已增加短窗口防重，第二次返回 `REPAYMENT_SUBMIT_DUPLICATED`。
4. 重复 `sign-confirm` 之前会返回不同的 `agreementNo`，现已复用本地已生效协议，不再重复出站 QW confirmSign。
5. `loan/query` 之前会把上游坏数据吞掉并错误映射成 `reviewing / approved`，现已改为先校验再映射：
   - `data=null` -> `YUNKA_RESPONSE_EMPTY`
   - 缺 `status` -> `YUNKA_RESPONSE_INVALID`
   - 缺 `loanId` -> `YUNKA_RESPONSE_INVALID`

## 5. 当前剩余事项

从联调执行角度看，当前已经没有活跃的 `PENDING` 和 `BLOCKED`。  
剩余事项主要是两类：

1. `N/A` 项继续保持边界收紧，不回头再拉回本轮 FE + BE 主联调范围：
   - 旧订单入口
   - 旧 callback 线
   - 旧 exercise-url 入口
2. 如果后续进入对外联调，需要继续基于当前本地真值准备外部联调输入、联调说明和问题归属口径。

## 6. 当前判断

一句话总结：

> 截至目前，`20260506` 这轮联调 case 已可视为“主范围收口完成”：该测的主流程和异常流程都已覆盖，最近这批真实业务问题也都已经关闭，后续重点不再是补当前矩阵漏洞，而是准备对外联调与保持文档口径一致。
