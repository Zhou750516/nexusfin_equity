# 2026-04-01 重点工作计划

## 1. 计划背景

截至 2026-03-31，齐为 / 通联直连主线已经完成以下内部准备：

1. `ALLINPAY_DIRECT` 模式已接入工程主线
2. `QwBenefitClient` 路由、证书加载、签名准备、HTTP 执行、响应验签、响应解析边界已经拆分完成
3. `RequestFactory / PayloadMapperRegistry / EnvelopeFactory / ProtocolSerializer / TransportMapper / HttpExecutor / ResponseVerificationStage / ResponseParser` 已全部收敛到可替换装配边界
4. 单测、应用上下文、MySQL 回归、`compile`、`checkstyle` 已通过

因此，2026-04-01 的重点已经不是继续做框架层改造，而是把“还差什么不能真实联调”尽快清零。

一句话目标：

**推动齐为 / 通联直连从“内部准备完成”进入“真实协议可联调”状态。**

## 2. 明日核心目标

### P0：完成齐为 / 通联外部协议对齐

**目标**

- 尽快拿到进入真实联调所需的最小协议闭环信息。

**原因**

- 当前最大的阻塞已经不在本地代码，而在外部协议资料尚未补齐。
- 如果这些信息明天仍拿不到，继续改代码不会直接缩短联调路径。

**必须拿到的内容**

1. 三个业务动作对应的真实接口名称 / `serviceCode`
   - `syncMemberOrder`
   - `getExerciseUrl`
   - `notifyLending`
2. 请求报文格式
   - XML / 表单 / 其他固定格式
   - 公共字段与业务字段命名、层级
3. 请求签名规则
   - 签名串拼接顺序
   - 摘要 / 签名算法
   - 签名字段位置
4. 响应验签规则
   - 响应签名值来源
   - 验签原文范围
5. 响应解析规则
   - 成功码 / 失败码 / 数据节点结构
   - 三个业务动作的关键返回字段
6. 证书与 TLS 规则
   - `public-rsa.cer` 是否为当前有效验签证书
   - `allinpay-pds.cer` 是否作废
   - 是否强制双向 TLS

**依赖文档**

1. [20260331_齐为通联直连外部确认清单.md](/Users/lixiaokun/Projects/nexusFin/nexusfin-equity/docs/plan/20260331_齐为通联直连外部确认清单.md)
2. [20260331_通联直连联调执行清单.md](/Users/lixiaokun/Projects/nexusFin/nexusfin-equity/docs/plan/20260331_通联直连联调执行清单.md)

### P0：拿到资料后立即回填协议模板并进入真实实现

**目标**

- 不再停留在 skeleton，直接把真实协议实现接进现有可替换边界。

**执行顺序**

1. 先回填 [20260331_通联直连联调字段待填模板.md](/Users/lixiaokun/Projects/nexusFin/nexusfin-equity/docs/plan/20260331_通联直连联调字段待填模板.md)
2. 优先替换请求侧组件：
   - `AllinpayDirectRequestFactory`
   - `AllinpayDirectProtocolSerializer`
   - `AllinpayDirectRequestPreparer`
   - `AllinpayDirectTransportMapper`
3. 再替换响应侧组件：
   - `AllinpayDirectResponseSignatureResolver`
   - `AllinpayDirectResponseVerificationStage`
   - `AllinpayDirectResponseParser`

**原因**

- 现在代码结构已经收敛，继续设计意义不大。
- 最有效的推进方式是“资料一到，立即落真实协议实现”。

### P0：按业务优先级推进首轮真实联调

**目标**

- 如果外部资料与联调条件在明天到位，当天立即进入首轮真实联调。

**建议顺序**

1. `syncMemberOrder`
2. `getExerciseUrl`
3. `notifyLending`

**每个接口都要保留的证据**

1. 请求报文
2. 响应报文
3. 验签结果
4. `traceId + bizOrderNo`
5. 本地状态变化结果
6. 若失败，对方错误码与我方问题分类

### P1：若资料未到位，固定阻塞说明和交付影响

**目标**

- 避免项目状态继续停留在“已经差不多可以联调”的模糊口径中。

**计划动作**

1. 按责任方拆分未确认项：
   - 齐为
   - 通联
   - 我方运维 / 网关
2. 明确每个阻塞项影响：
   - 影响真实请求出站
   - 影响响应验签
   - 影响业务解析
   - 影响首轮联调开始时间
3. 输出一版可直接对内同步的风险口径

## 3. 明日工作主线

建议按以下顺序推进，不要并行发散：

1. 先推动齐为 / 通联返回协议资料。
2. 资料一到，先填模板，不要直接跳进代码。
3. 按“请求侧 -> 响应侧 -> 业务联调”的顺序落真实实现。
4. 若当天无法拿齐资料，立即转为更新阻塞清单和时间线，不再继续做泛化改造。

## 4. 明日完成标准

如果 2026-04-01 结束前达到以下任一结果，可认为当天工作达标：

### A 档：进入真实联调

1. 外部协议资料已补齐到可实现程度。
2. 已完成模板回填。
3. 已开始至少 1 个业务动作的真实协议实现或真实联调。

### B 档：阻塞清晰可控

1. 外部资料仍未补齐。
2. 但已明确哪些信息缺失、由谁提供、影响哪一步联调。
3. 已形成可直接对内 / 对外同步的阻塞说明，不再重复判断相同问题。

## 5. 风险提示

2026-04-01 的计划受以下因素直接影响：

1. 若齐为 / 通联仍未提供 `serviceCode` 和报文样例，真实联调无法启动。
2. 若签名 / 验签规则未确认，即使能发请求，也无法判定联调结果是否可信。
3. 若证书用途和 TLS 要求未确认，联调环境握手仍存在不确定性。
4. 若没有成功 / 失败样例，解析逻辑存在高返工风险。

## 6. 备注

本计划只聚焦：

1. 齐为 / 通联直连主线
2. 真实协议实现与首轮联调
3. 外部阻塞项管理

本计划不再把科技平台接口梳理、云卡独立交互主线等并行事项混入当天主线，以避免优先级失真。
