# 2026-04-01 重点工作计划与实际结果

## 1. 当日目标

截至 2026-03-31，项目已经完成 `M1 开发完成` 的内部收口，并补齐了：

1. `T021` 控制器访问日志
2. `T026` 上游用户校验重试与失败可观测性
3. 齐为待外部确认项清单
4. `M3.1` 出站接口联调执行说明

因此 `2026-04-01` 的工作重点，不再是继续补本地开发细节，而是围绕“拿确认、推联调、收阻塞”推进三件事：

1. 尽快把 `M2 联调准备完成` 从“内部准备完成”推进到“外部条件确认完成”。
2. 在条件具备时立即启动 `M3.1` 出站接口联调。
3. 如果外部条件仍不完整，则把阻塞信息、责任方和影响路径进一步固定，避免交付节奏失真。

说明：

1. 本计划聚焦 **齐为联调主线** 与当前交付主链。
2. **云卡完整页面交互、借款确认、还款页编排** 属于独立功能工作流，不与 2026-04-01 的齐为联调推进混在同一执行主线中。

## 2. 当日实际结果

### 2.1 外部确认项推进结果

当天围绕外部确认项，形成了以下实际产出：

1. 已补齐一版按责任方拆分的催办视图与阻塞跟踪
2. 已把 `M2` 的状态明确为：
   - 齐为出站测试环境已基本打通
   - 但整体外部条件仍未全部确认完成
3. 已把科技平台真实地址 / token、回调公网条件、扣款回调映射规则继续保留为未闭环项

对应沉淀文档：

1. `docs/plan/20260401_M2外部确认与M3.1推进跟踪.md`
2. `docs/plan/20260401_对外同步口径.md`

### 2.2 齐为真实联调推进结果

当天已实际启动齐为真实联调，并拿到以下结果：

1. 标准 HTTP 协议已打通，不再返回 `504 数据协议非法`
2. `abs.push.order` 已在齐为测试环境真实成功
3. `abs.token.get` 已能进入业务层，但当前返回 `515 会员卡未生效`
4. `abs.lending.notify` 仍未完成真实样例验证

对应沉淀文档：

1. `docs/bug/20260401_齐为504数据协议非法分析.md`
2. `docs/third-part/齐为/20260401_齐为线上部署说明.md`

### 2.3 阻塞固化结果

当天已经把“阻塞不在本地代码，而在外部条件”这件事进一步固定下来，当前主要剩余阻塞为：

1. 齐为 `abs.token.get` 的生效时序与额外前置条件
2. 齐为 `abs.lending.notify` 成功 / 失败样例
3. 科技平台 `/api/users/me` 真实地址、token 样例、真实返回结构
4. 我方公网测试回调域名、路径暴露方式、白名单策略

### 2.4 科技平台文档补充结果

当天还补充了科技平台的阶段性部署说明，用于承接“今天刚完成一轮接口对齐、后续还会继续更新”的当前状态：

1. `docs/third-part/科技平台/20260401_科技平台线上部署说明.md`

该文档明确定位为“阶段性基线版”，不是最终定版协议。

## 3. 原计划拆解与落地结果

### P0：推动外部确认项落地

**目标**

- 把 [20260331_待外部确认项清单.md](/Users/lixiaokun/Projects/nexusFin/nexusfin-equity/docs/plan/20260331_待外部确认项清单.md) 中的关键项推进到“已确认”或“已明确阻塞”的状态。

**对应里程碑**

- `M2 联调准备完成`

**原因**

- 当前项目最大的交付风险已经不在本地代码，而在外部协同是否给出真实参数、回调映射规则和测试地址。
- 如果 2026-04-01 仍拿不到这些信息，项目虽然看起来“代码完成”，但实际上还不能稳定进入联调执行。
- 这里的“联调执行”特指 **齐为与当前 ABS 主链** 的真实联调，不包含云卡完整页面流程开发。

**计划动作**

1. 对齐为确认以下信息：
   - `partnerNo / signKey / aesKeyBase64`
   - 最终测试网关地址与版本口径
   - 扣款回调区分首扣 / 兜底代扣的规则
   - 3 个出站接口与关键回调样例
2. 对科技平台确认以下信息：
   - `/api/users/me` 真实测试地址
   - Bearer Token 样例
   - 真实返回字段结构
3. 对我方网关 / 运维确认以下信息：
   - 公网测试回调域名
   - 回调路径暴露方式
   - 白名单策略

**实际结果**

1. 一份“已确认项 / 仍阻塞项”更新结果
2. 一份按责任方拆分的催办视图
3. 对 `M2` 是否完成给出明确判断

**结果判断**

1. 已完成
2. 但“完成”的含义是：状态已经更新清楚，不代表所有外部条件已经拿齐

### P0：在条件具备时启动 `M3.1` 出站接口联调

**目标**

- 一旦外部参数和测试地址齐备，当天直接按既定顺序推进出站接口联调，不再临场设计流程。

**对应里程碑**

- `M3.1 出站接口联调完成`

**原因**

- 2026-03-31 已经把联调顺序、通过标准和观察点整理到 [20260331_出站接口联调执行说明.md](/Users/lixiaokun/Projects/nexusFin/nexusfin-equity/docs/plan/20260331_出站接口联调执行说明.md)。
- 当前最优策略是“条件一到位就开联调”，避免再把内部准备和外部等待割裂成新的时间浪费。
- 本段同样只针对齐为出站接口联调，不把云卡完整借款 / 还款流程开发并入当天联调目标。

**计划动作**

1. 优先联调 `abs.push.order`
2. 其次联调 `abs.token.get`
3. 再联调 `abs.lending.notify`
4. 每个接口都留存：
   - 请求关键字段
   - 响应结果
   - `traceId + bizOrderNo`
   - 状态变化结果

**实际结果**

1. 已完成 `abs.push.order` 真实联调成功
2. 已完成 `abs.token.get` 真实联调，并定位到业务层 `515 会员卡未生效`
3. 已形成按接口拆分的问题清单与日志证据
4. `abs.lending.notify` 尚未完成真实样例验证

**结果判断**

1. 已部分完成，并已实质进入 `M3.1`
2. 当前剩余工作不再是“能不能启动”，而是“怎样收口剩余接口和业务时序问题”

### P1：如果外部条件不到位，固定阻塞影响与时间线

**目标**

- 避免项目状态继续停留在“应该快交付了”的模糊口径里。

**对应里程碑**

- `M2 -> M3 -> M4` 交付路径管理

**原因**

- 当前真正影响交付节奏的是外部确认项。
- 如果不把阻塞与时间线写清楚，后续每天都会重复判断相同问题。

**计划动作**

1. 更新整体里程碑文档中的当前状态。
2. 明确每个未确认项对应影响哪个里程碑：
   - 影响 `M2`
   - 影响 `M3.1`
   - 影响 `M3.2`
   - 影响最终验收与交付
3. 给出“最乐观 / 基准 / 延迟”三档时间线判断。

**实际结果**

1. 更新后的整体里程碑状态
2. 一份当前交付节奏说明
3. 一份可用于对内同步的时间线口径

**结果判断**

1. 已完成
2. 当前时间线已从“是否能启动联调”切换为“剩余外部确认项何时收口”

## 4. 当日执行顺序与实际落点

当天实际是按以下顺序推进的：

1. 先催齐外部确认项，尤其是齐为参数、科技平台地址和回调映射规则。
2. 条件部分到位后，实际先完成齐为协议修正与真实联调。
3. 联调结果落档后，再回填阻塞说明、时间线和部署说明。

对应实际落点：

1. `abs.push.order`、`abs.token.get` 已进入真实验证
2. 齐为协议层问题已被排除
3. 当前剩余问题集中在业务时序与外部确认项

## 5. 当日完成标准与判断

如果 `2026-04-01` 结束前达到以下结果，可以认为当天重点工作达标：

1. 至少完成一轮外部确认项推动，明确哪些已确认、哪些仍阻塞。
2. 若参数和地址齐备，至少完成 `abs.push.order` 的真实联调。
3. 若参数和地址未齐备，整体里程碑与时间线文档已经同步更新，交付风险可被明确表达。

当天实际判断：

1. 第 1 项已完成
2. 第 2 项已完成，且超过“至少完成 `abs.push.order`”这一最低标准
3. 第 3 项也已完成，因为整体里程碑、时间线与阻塞口径均已同步更新

因此：

> 从当天目标角度看，`2026-04-01` 的重点工作已达标；但从项目总交付角度看，`M2` / `M3.1` 仍有剩余外部阻塞未完全收口。

## 6. 当前风险提示

截至 `2026-04-01` 当天结束，当前主要风险点为：

1. 齐为 `abs.token.get` 的生效时序若不明确，`M3.1` 仍无法完全收口。
2. 科技平台若未提供真实用户信息接口和 token 样例，SSO 真联调仍无法闭环。
3. 回调域名和白名单若未确认，`M3.2` 无法排期启动。
4. 科技平台接口今天刚完成一轮对齐，后续仍会继续调整，相关代码和部署文档都需要持续回填。

## 7. 备注

本文件已从“计划文档”回填为“计划 + 当日结果”文档，便于后续回看：

1. 本地开发与验证闭环已经完成。
2. `2026-04-01` 当天已经从“等待能否启动联调”推进到“真实联调已启动并拿到结果”。
3. 当前交付节奏的关键瓶颈已进一步收敛到外部确认项与剩余业务时序问题。
4. 云卡完整交互属于并行但独立的后续开发主线，应单独排期和单独衡量进度。
