# 2026-04-03 重点工作计划

## 1. 计划目标

截至 `2026-04-02` 结束，今天已经完成以下阶段性推进：

1. 齐为三条主动接口已完成真实联调闭环验证
2. `515` 的真实根因已收敛为 `payProtocolNo` 错误，而不是开卡未成功
3. 主流程已完成第一轮“真实协议号优先”改造
4. 真实协议号回写入口已落地
5. 科技平台整条线已明确进入 `HOLD`

因此 `2026-04-03` 的重点，不再是继续重复验证齐为接口本身，而是围绕“生产闭环补齐、上线文档沉淀、剩余外部条件确认”推进三件事：

1. 补齐支付协议号来源阶段性方案文档（当前按通联假设）。
2. 继续细化支付协议号状态管理与主流程取值边界。
3. 跟进公网回调条件，判断 `M3.2` 能否具备启动条件。

说明：

1. 明天主线仍是 **齐为 + 支付协议号来源生产闭环**。
2. 科技平台继续保持 `HOLD`，不进入明天主执行线。

## 2. 明日优先级

### P0：补齐支付协议号来源文档与上线切换口径（当前按通联假设）

**目标**

- 形成一套可直接支撑后续开发、联调、上线切换的文档基线。

**对应里程碑**

- `M3.1 出站接口主流程生产化准备`

**计划动作**

1. 在 `docs/third-part` 下补齐支付协议号来源阶段性文档
2. 明确真实 `payProtocolNo` 的来源、入库入口、状态字段和上线切换步骤
3. 补一份“测试 override 关闭前检查项”清单
4. 更新齐为相关部署说明中的正式环境约束

**预期产出**

1. 一份支付协议号来源阶段性文档
2. 一份上线切换 / 回归检查清单
3. 一份同步后的齐为部署边界说明

### P0：细化支付协议号状态策略与主流程边界

**目标**

- 把当前第一轮实现补到“可持续维护”的程度，避免后续状态语义再次混乱。

**对应里程碑**

- `M3.1 主流程生产可用性增强`

**计划动作**

1. 明确 `ACTIVE / INACTIVE / EXPIRED / REVOKED` 的写入与使用规则
2. 明确回写接口是否只接收 `ACTIVE`，还是同时接收非激活状态
3. 明确主流程在以下场景的行为：
   - 有 `ACTIVE` 协议号
   - 仅有非 `ACTIVE` 协议号
   - 完全无协议号
4. 补齐必要文档或实现说明

**预期产出**

1. 一份状态定义与主流程取值规则说明
2. 若需要，形成下一轮代码改造点清单

### P1：继续跟进公网测试回调条件

**目标**

- 判断 `M3.2` 是否能进入可执行排期。

**对应里程碑**

- `M3.2 入站回调联调`

**计划动作**

1. 继续确认公网测试域名
2. 继续确认回调路径暴露方式
3. 继续确认白名单策略
4. 若条件齐备，输出 `M3.2` 启动建议

**预期产出**

1. 一份最新的公网回调条件状态
2. 若条件满足，补一版 `M3.2` 启动建议

### P0（HOLD）：科技平台事项继续暂停

**目标**

- 保持边界清晰，避免在口径即将整体调整前继续投入实现成本。

**计划动作**

1. 明天不继续推进科技平台代码改造
2. 明天不继续推进科技平台联调执行细节
3. 仅在收到新的确定性口径时更新 `HOLD` 说明

**预期产出**

1. 科技平台继续保持 `HOLD`
2. 避免返工

## 3. 建议执行顺序

建议按以下顺序推进：

1. 先补齐支付协议号来源与上线切换文档。
2. 再细化协议号状态管理与主流程边界。
3. 再跟进网关 / 运维的公网回调条件。
4. 科技平台整条线保持 `HOLD`，不作为明天执行主线。

原因：

1. 当前真正影响生产落地的是“真实协议号来源与切换策略”。
2. 公网回调条件虽然重要，但仍受外部资源节奏影响。
3. 科技平台当前继续推进的收益低于返工风险。

## 4. 明日完成标准

如果 `2026-04-03` 结束前达到以下结果，可以认为当天重点工作达标：

1. `docs/third-part` 下已补齐支付协议号来源阶段性文档。
2. 已明确支付协议号状态定义与主流程取值边界。
3. 公网回调条件至少完成一轮状态更新；若条件齐备，形成 `M3.2` 启动建议。
4. 科技平台继续保持 `HOLD`，无额外返工投入。

## 5. 风险提示

`2026-04-03` 的计划主要受以下因素影响：

1. 若支付渠道最终发生调整，当前按通联假设整理的文档需要同步改版。
2. 若网关 / 运维迟迟不给出公网条件，`M3.2` 仍无法启动。
3. 若外部再变更齐为或支付侧口径，需要及时回填 `docs/third-part` 与 `docs/plan`，避免状态失真。

## 6. 收尾状态回填（截至今日）

### 6.1 已完成

1. 已补齐支付协议号来源阶段性文档，并完成上线切换口径整理。
2. 已细化支付协议号状态定义与主流程取值边界，并补齐下一阶段改造点清单。
3. 已将“通联”为最终渠道的表述降级为阶段性假设，并完成相关文档回填。
4. 已完成 `M3.2` 阻塞状态更新、启动模板和对外短版沟通材料整理。
5. 科技平台今日继续保持 `HOLD`，未新增高返工风险投入。

### 6.2 未完成但已明确阻塞

1. `M3.2` 仍未获得公网测试域名确认。
2. `M3.2` 仍未获得回调路径暴露方式确认。
3. `M3.2` 仍未获得白名单策略确认。

### 6.3 今日结论

1. 今日 `P0` 目标已完成。
2. 今日未完成项主要为外部条件未齐，不属于本地遗漏。
3. 后续应等待外部回复后，再将 `M3.2` 从“准备态”切换到“执行态”。
