# 2026-04-02 发网关/运维确认问题清单

## 1. 文档目的

本文档用于 `2026-04-02` 当天向我方网关 / 运维同步 `M3.2` 公网回调联调前置条件，方便后续直接复制发送，并作为今天的留痕材料。

## 2. 可直接发送的标准版

建议直接发送如下内容：

> 你好，这边正在推进第三方回调联调，当前本地回调入口和签名校验主链已经具备，但 `M3.2` 还不能启动，主要卡在公网暴露条件未确认。  
>  
> 想请协助确认以下 3 项：  
> 1. 是否已有可供第三方访问的测试域名？  
> 2. `/api/callbacks/**` 是否可以直接对外暴露，还是需要通过网关转发？  
> 3. 是否需要配置来源 IP / 域名白名单？如果需要，请明确由谁配置、如何配置。  
>  
> 当前后续会用到的回调路径包括：  
> - `/api/callbacks/grant/forward`  
> - `/api/callbacks/repayment/forward`  
> - `/api/callbacks/exercise-equity`  
> - `/api/callbacks/refund`  
>  
> 若后续还需要扣款类回调，历史口径中还包括：  
> - `/api/callbacks/first-deduction`  
> - `/api/callbacks/fallback-deduction`  
>  
> 麻烦先帮确认公网域名、暴露方式、白名单策略，确认后我们再整理完整回调 URL 给第三方。

## 3. 企业微信短版

如果想发得更短一点，可以直接发：

> 这边第三方回调联调准备启动，但目前还缺公网条件确认，麻烦帮确认：  
> 1. 是否有可供第三方访问的测试域名  
> 2. `/api/callbacks/**` 是直接暴露还是走网关转发  
> 3. 是否要做来源 IP / 域名白名单  
>  
> 这 3 项确认后，我们就能继续推进 `M3.2` 回调联调。

## 4. 当前统一口径

今天对网关 / 运维统一使用以下口径：

1. 本地回调接口已经具备
2. `/api/callbacks/**` 已有签名校验保护
3. 当前不启动 `M3.2` 的原因不是代码未完成
4. 而是公网测试域名、路径暴露方式、白名单策略未定

## 5. 后续收到回复后的动作

### 情况 A：三项都确认

则后续继续做：

1. 生成完整回调 URL 清单
2. 对齐给第三方
3. 启动 `M3.2` 首轮真实联调

### 情况 B：只确认了部分

则后续继续做：

1. 固定未确认项为阻塞说明
2. 不启动 `M3.2`
3. 继续等待剩余条件

## 6. 关联文档

1. `docs/plan/20260402_M3.2公网回调启动条件说明.md`
2. `docs/plan/20260402.md`
3. `docs/plan/20260402_checklist.md`
