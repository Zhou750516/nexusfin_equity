# 2026-04-19 工作计划

## 1. 今日目标

围绕“异步消息补偿机制第一阶段已完成”的现状，继续把方案推进到“可交接、可运维、可联调”的状态，避免只停留在代码和测试通过层面。

## 2. P0：异步消息补偿运维承接说明

1. 补一版面向交付 / 运维 / 联调同学的操作说明，至少覆盖：
   - 任务表、尝试日志表、分片心跳表的作用
   - 任务状态流转说明
   - 重试次数、退避节奏、死任务判定口径
   - 分片 worker 与 supervisor 的职责边界
2. 补一版人工介入说明，至少覆盖：
   - 如何识别积压任务
   - 如何识别死任务
   - 如何确认是外部依赖异常还是本地数据问题
   - 如何决定“重试 / 放弃 / 人工补单”

## 3. P0：控制器级链路补齐验证

1. 继续从用户接口承接角度回看：
   - 权益购买同步失败时的前端返回口径
   - 用信提交同步超时时的前端返回口径
2. 必要时补控制器 / 集成测试，确认：
   - 返回值与当前业务口径一致
   - 不会因为补偿入队而把同步接口直接打成失败
3. 若发现当前实现与已确认口径不一致，优先收口文档与返回语义

## 4. P0：真实运行方式边界说明

1. 明确当前实现属于：
   - 应用内异步补偿骨架 + 本地 MySQL 持久化
2. 明确当前暂不展开：
   - 独立部署进程
   - 多实例调度编排
   - 跨服务统一消息平台
3. 补一版“当前可上线前提 / 当前不可承诺事项”说明，避免协作方误解

## 5. P1：继续维持范围边界

今日仍不进入以下扩面事项：

1. 退款链路异步补偿
2. 绑卡链路异步补偿
3. 权益状态回推异步补偿
4. 小花真实加解密接入
5. 联合登录正式协议接入

## 6. 今日交付物

目标至少补齐以下沉淀物中的一部分：

1. 异步补偿运维 / 人工操作说明文档
2. 控制器级返回口径核对结论
3. 当前实现边界说明
4. 如有新结论，补当日日报 / decision-review

## 7. 收口标准

今日收口以“可交接、可说明、可人工处理”为标准，不以继续横向扩功能为目标。

## 8. 当前执行进展（截至 2026-04-19）

### 8.1 异步补偿运维与人工处理说明已补齐

已新增：

1. `docs/plan/20260419_异步补偿运维与人工处理说明.md`
2. `docs/plan/20260419_异步补偿人工处理SQL与操作模板.md`
3. `docs/plan/20260419_异步补偿自动调度接入建议方案.md`

当前已明确：

1. 任务主表、尝试日志表、分片运行时表职责
2. 任务状态流转
3. worker / supervisor 边界
4. 分片规则
5. 重试退避节奏与死任务判定
6. 积压、死任务、离线分片识别方式
7. 人工介入建议
8. 当前上线前提与不可承诺事项
9. 人工排查 SQL 与操作模板
10. 自动调度接入建议方案

### 8.1.1 自动调度接入已形成开发任务拆解

已新增：

1. `docs/plan/20260419_异步补偿开发任务拆解_调度接入版.md`

当前已进一步明确：

1. 下一步实施单元是“配置层 + 调度协调层 + worker/supervisor scheduler”
2. 默认仍应关闭自动调度，避免污染现有联调与测试
3. 第一阶段仍采用显式 `ownedPartitions`，不进入自动抢占与 rebalance
4. 后续若进入编码，可直接按拆解文档逐任务推进

### 8.2 控制器级返回口径已完成首轮核对

已新增：

1. `docs/plan/20260419_控制器级返回口径核对结论.md`

当前已核对并明确：

1. **权益购买同步超时**
   - 接口当前仍同步失败
   - 但本地权益订单与补偿任务应保留
2. **权益购买成功、云卡明确失败**
   - 当前按 `loan_failed` 承接
   - 同时明确权益已成功
3. **权益购买成功、云卡超时**
   - 当前按“已提交 / 审核中”承接
   - 本地保留映射与补偿任务

### 8.3 已补一处关键事务修复

在控制器级口径核对过程中，补充发现并修复了一处关键实现偏差：

1. 原实现中，`loan/apply` 场景下若齐为权益购买同步超时
2. 虽然代码会尝试写补偿任务
3. 但外层事务会把权益订单和补偿任务一起回滚

现已修复为：

1. 接口仍同步返回 `QW_SYNC_TIMEOUT`
2. 但权益订单和补偿任务会保留，可继续补偿

### 8.4 已完成聚焦验证

已通过：

1. `mvn -q -Dtest=Phase9TaskGroupECompensationIntegrationTest test`
2. `mvn -q -Dtest=Phase9TaskGroupEIntegrationTest,BenefitOrderServiceTest test`

### 8.5 当前一句话判断

> `2026-04-19` 当前已完成“运维说明 + 人工处理说明 + 控制器级口径核对 + 关键事务修复”的第一轮收口，后续可继续补当天执行结果和晚间收尾材料。

补充一句：

> 当前已把“自动调度如何接入”从建议方案进一步沉淀到可执行任务拆解，编码入口已经明确。

### 8.6 自动调度接入已完成第一轮编码落地

本轮已完成：

1. `AsyncCompensationProperties` 补充调度配置项：
   - `enabled`
   - `workerEnabled`
   - `supervisorEnabled`
   - `workerPollIntervalMs`
   - `supervisorIntervalMs`
   - `workerId`
   - `ownedPartitions`
2. `NexusfinEquityApplication` 启用 `@EnableScheduling`
3. 新增调度协调层：
   - `AsyncCompensationSchedulerCoordinator`
   - `AsyncCompensationSchedulerCoordinatorImpl`
4. 新增调度入口：
   - `AsyncCompensationWorkerScheduler`
   - `AsyncCompensationSupervisorScheduler`
5. 默认配置保持关闭自动调度，避免影响现有联调与测试
6. 应用启动时输出调度摘要日志，便于确认实例角色与分片配置

### 8.7 本轮验证结果

已通过以下验证：

1. `mvn -q -Dtest=AsyncCompensationPropertiesTest test`
2. `mvn -q -Dtest=AsyncCompensationWorkerSchedulerTest,AsyncCompensationSupervisorSchedulerTest test`
3. `mvn -q -Dtest=AsyncCompensationPropertiesTest,AsyncCompensationWorkerSchedulerTest,AsyncCompensationSupervisorSchedulerTest,AsyncCompensationWorkerServiceTest,AsyncCompensationSupervisorServiceTest test`
4. `mvn -q -Dtest=NexusfinEquityApplicationTests test`
5. `mvn -q -Dtest=Phase9TaskGroupECompensationIntegrationTest test`

当前可确认：

1. 调度配置默认关闭时，不影响 Spring Boot 启动
2. worker / supervisor scheduler 具备最小定时入口
3. 现有异步补偿主链路语义未被本轮改动打坏
4. 默认关闭调度时，权益购买超时场景只会落 `INIT` 任务，不会自动生成 heartbeat / attempt 留痕
