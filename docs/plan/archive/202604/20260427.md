# 2026-04-27 工作计划

> 今日主线：把 `2026-04-26` 已完成的方案收口与验证结果，转成两条真正可推进的主线：一条是 H5 前后端接口闭环优先级，另一条是 `docs/plan` 新治理方式的稳定落地。

## 1. 今日目标

基于当前已确认事实：

1. Shared Foundation 与 H5 新流程第一批页面改动已经落在当前代码和最新提交中；
2. `docs/plan` 已完成一轮 `archive/` / `topics/` 重组，但 `CURRENT_STATE.md` 仍处于“框架已搭、口径待补”的状态；
3. H5 对接问题已经不是“有没有接口”，而是“哪些接口先做、哪些真实闭环最优先”。

因此 `2026-04-27` 的重点是：

1. 明确 H5 前后端接口第一批实施顺序
2. 把 `CURRENT_STATE.md` 从占位稿补成可直接开工的单点入口
3. 稳定 `docs/plan` 的归档与主题化治理方式
4. 确认 H5 本地联调入口、mock 能力与后续真实对接边界

### 当前已产出

1. `docs/plan/20260427_H5前后端接口对接清单.md`
2. `docs/plan/CURRENT_STATE.md`
3. `scripts/archive_plan.sh`
4. H5 新入口页、路由调整与 mock server 首版支持：
   - `H5/client/src/pages/LandingPage.tsx`
   - `H5/mock-server/index.ts`

## 2. 今日优先级

### P0：收口 H5 前后端接口第一批实施顺序

**目标**

- 不把 `20260427_H5前后端接口对接清单.md` 继续停留在“问题罗列”层，而是压成可直接进入开发的先后顺序。

**计划动作**

1. 把“借款用途 `purpose` 字段上传 / 校验 / 落库”列为第一优先级最小切片；
2. 把“权益开通短信验证闭环”列为第二优先级，先固定契约和状态流，再判断是否直接进入实现；
3. 将 `matching-status`、协议详情、非主链 mock 补全列为后续 backlog，避免今天同时铺开；
4. 为第一优先级项明确 FE / BE / DB / test 改动点与验收标准。

### P0：补齐 `CURRENT_STATE.md` 的真实口径

**目标**

- 让 `docs/plan/CURRENT_STATE.md` 成为每天开工只需先读的一份文档，而不是长期保留占位符的框架稿。

**计划动作**

1. 填掉“当前阶段 / 本周聚焦 / 本周不做”三处占位；
2. 更新五方依赖状态表：
   - 齐为
   - 小花科技
   - 云卡
   - 科技平台
   - 电子签章（法大大）
3. 汇总当前阻塞项，按“等外部回复 / 等本侧收口 / 等商务流程”分类；
4. 把本周已经形成的关键决定回填进“本周核心决定”。

### P1：稳定 `docs/plan` 新治理方式

**目标**

- 让这轮归档不是一次性整理，而是后续每周都能低成本复用的工作流。

**计划动作**

1. 用 `scripts/archive_plan.sh` 做一轮 dry-run 核对；
2. 再确认哪些文件应继续留在 `docs/plan/` 根目录：
   - 当日计划
   - 当日 checklist
   - 当前活跃专题文档
   - `CURRENT_STATE.md`
3. 再确认哪些文件应优先进入：
   - `topics/<主题>/`
   - `archive/<YYYYMM>/`
4. 保持今天不再发起第二轮大规模搬运，只做规则校验与补缺。

### P1：确认 H5 本地联调边界

**目标**

- 保证下一轮 H5 真实接口接入前，本地预览与 mock 调试路径足够清晰，不再反复切换入口。

**计划动作**

1. 确认 `/landing` → `/calculator` → 审批 / 权益 / 还款链路为当前默认入口；
2. 确认 `pnpm dev` + `pnpm dev:mock` 的本地联调方式；
3. 评估是否需要补齐以下 mock 路由：
   - `/api/auth/joint-login`
   - `/api/benefit-dispatch/context/:no`
   - `/api/benefit-dispatch/resolve`
   - `/api/refund/info/:no`
4. 明确这些 mock 补齐项属于“联调便利项”，不和真实主链实现混做。

## 3. 今日完成标准

如果 `2026-04-27` 结束前达到以下结果，可认为当天达标：

1. H5 对接清单的第一批实施顺序已明确，且第一优先级项足够直接进入开发；
2. `CURRENT_STATE.md` 不再保留 `<...>` 占位符；
3. `docs/plan` 的留根目录 / 进 `topics` / 进 `archive` 的边界已明确；
4. H5 本地联调与 mock 的使用方式已固定；
5. 今天没有把工作扩散成“大范围接口同时开工 + 再次大规模文档整理”。

## 4. 风险提示

1. 如果 `20260427_H5前后端接口对接清单.md` 继续只列问题不排优先级，下一轮实现仍会失焦。
2. 如果 `CURRENT_STATE.md` 不尽快补成真实口径，新的归档结构仍会退化成“文件搬走了，但入口没建立”。
3. 如果同时启动 `purpose`、权益短信、资方匹配、协议详情四条实现，工作面会再次过宽。
4. 如果 mock 边界和真实接口边界不分开，H5 联调阶段容易重复返工。

## 5. 一句话判断

> `2026-04-27` 的关键不是继续增加文档数量，而是把“已验证的现状”和“已整理的接口清单”压缩成一个可直接执行的优先级入口。
