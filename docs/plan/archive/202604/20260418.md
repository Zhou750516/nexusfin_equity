# 2026-04-18 工作计划

> 明日主线：在 `2026-04-17` 已完成“联合登录核心入口骨架 + 分发页/退款页/状态回推方案骨架”的基础上，继续把外部依赖项和本侧正式承接设计往“可联调、可实施”方向推进。

## 1. 明日目标

基于 `2026-04-17` 已完成结果：

1. 联合登录本地核心入口骨架已落地
2. `/joint-entry`、`/joint-dispatch`、`/joint-refund-entry` 已具备正式演进起点
3. 分发页 / 退款页 / 状态回推已形成推荐方案骨架
4. 齐为绑卡新增接口与本地还款记录模型都已进入清晰可跟踪状态

因此 `2026-04-18` 的重点是：

1. 继续推进联合登录从“本地骨架”向“真实协议适配准备”靠拢
2. 继续把分发页 / 退款页 / 状态回推收敛成可拆任务的正式方案入口
3. 维持齐为与科技平台外部依赖的跟踪状态，避免遗漏

## 2. 明日优先级

### P0：收口小花科技 2026-04-17 沟通新增事项

**目标**

- 将小花科技最新沟通结论纳入 `2026-04-18` 主线，避免联合登录、还款、用信图片回传各自脱节

**计划动作**

1. 基于 `plan/20260417_小花科技沟通纪要与行动计划.md` 跟踪新增事项
2. 优先确认 `partnerId` 来源方案：
   - 若小花已有内部生成逻辑，采用方案 2
   - 若小花没有内部生成逻辑，采用方案 1
3. 将“还款流程不换卡，不调用绑卡发送验证码接口”同步到还款 / 绑卡边界文档
4. 设计艾博生还款跳转页面地址
5. 将用信图片获取 / 回传拆成后续独立方案入口

### P0：推进小花联合登录真实协议适配准备

**目标**

- 让联合登录从“仅本地骨架”进入“随时可接真实文档”的状态

**计划动作**

1. 回看当前联合登录骨架的已固定边界：
   - 跳转参数
   - 场景分流
   - 本地登录态建立
   - 占位异常处理
2. 若小花正式文档到位，则优先补：
   - 请求 / 响应字段映射
   - token 校验 / 换取链路
   - 失败码与异常页口径
3. 若小花文档仍未到位，则先整理“真实协议适配占位清单”，明确：
   - 哪些类可直接替换
   - 哪些配置项需新增
   - 哪些测试需补充

### P0：把分发页 / 退款页 / 状态回推继续推进到“可拆任务”层级

**目标**

- 让现有方案骨架可以直接继续拆为开发任务或联调任务

**计划动作**

1. 进一步明确分发页的正式承接边界：
   - 订单上下文来源
   - 供应商跳转前检查
   - 跳转流水留痕
2. 进一步明确退款页的正式承接边界：
   - 退款前置信息
   - 退款申请受理口径
   - 退款状态查询口径
3. 进一步明确状态回推的后端能力拆分：
   - 事件触发点
   - 回推客户端
   - 幂等 / 重试 / 日志

### P1：继续维护齐为绑卡新增接口 TODO 与外部资料依赖

**目标**

- 保证绑卡需求随时可以从 TODO 切换到正式设计

**计划动作**

1. 持续等待齐为正式接口文档
2. 若齐为资料到位，则优先对齐：
   - 三个接口的正式路径
   - 字段映射
   - 协议号 / 签约号模型
3. 若资料仍未到位，则维持当前 TODO，不抢跑实现

### P1：继续维持本地还款记录模型的预期管理

**目标**

- 防止后续在验收、对账、查询需求上再次混淆边界

**计划动作**

1. 继续维持“当前不新增本地还款记录表”的口径
2. 若业务开始明确要求强一致，再单独启动模型设计
3. 避免在现有还款主流程中临时补丁式修改 `payment_record`

## 3. 明日完成标准

如果 `2026-04-18` 结束前达到以下结果，可认为当天达标：

1. 小花科技 `2026-04-17` 沟通新增事项已形成可跟踪行动计划
2. 联合登录已明确进入“真实协议适配准备”阶段
3. 分发页 / 退款页 / 状态回推至少有一块进一步沉淀到“可拆任务”层级
4. 齐为绑卡新增接口仍保持在可立即接续的跟踪状态
5. 本地还款记录模型边界未发生回摆

## 4. 风险提示

1. 若小花正式文档继续延迟，联合登录短期内仍只能停留在本地骨架阶段
2. 若齐为供应商跳转或退款文档未回，分发页和退款页只能继续停留在方案层
3. 若提前把状态回推写成最终实现，后续容易因为科技平台契约变动返工
4. 若对本地还款强一致再次口径不稳，容易影响验收判断

## 5. 一句话判断

> `2026-04-18` 的关键不是盲目扩写代码，而是把 `2026-04-17` 已落地的骨架继续推进到“随时能接正式协议、随时能拆正式任务”的状态。

## 6. 提前补充（分发页 / 退款页 / 状态回推实施计划）

已提前补一版正式实施计划：

1. 新增文档：
   - `docs/plan/20260418_分发页退款页状态回推实施计划.md`
2. 已将三块能力拆成可直接执行的 6 个任务：
   - 分发页上下文查询
   - 分发页跳转解析与 H5 联动
   - 退款入口信息查询
   - 退款申请与结果查询
   - 权益状态回推日志与服务骨架
   - 三块能力联调检查清单
3. 已给出建议文件落点、测试命名和执行顺序

### 当前边界

本轮只完成任务拆解，不进入：

1. 真实代码实现
2. 外部协议接入
3. 测试执行

## 7. 提前补充（小花科技沟通纪要与行动计划）

已根据 `2026-04-17` 小花科技沟通内容新增纪要与行动计划：

1. 新增文档：
   - `docs/plan/20260417_小花科技沟通纪要与行动计划.md`
2. 已将新增事项拆为 6 条后续行动：
   - 确认 `partnerId` 来源方案
   - 确认联合登录正式技术方案
   - 评估加解密 demo
   - 梳理用信图片获取与回传方案
   - 设计艾博生还款跳转页面地址
   - 同步还款流程不换卡的边界说明
3. 已明确当前待小花确认项：
   - `partnerId` 是否采用小花内部生成方案
   - 联合登录正式技术方案

### 当前边界

本轮仅做纪要和计划沉淀，不进入：

1. 小花真实接口开发
2. 加解密 demo 代码接入
3. 用信图片接口实现
4. H5 还款跳转页面改造

## 8. 执行进展补充（分发页 / 退款页 / 状态回推第一轮骨架实现）

已按 `docs/plan/20260418_分发页退款页状态回推实施计划.md` 完成第一轮骨架实现：

1. 分发页已完成：
   - `/api/benefit-dispatch/context/{benefitOrderNo}`
   - `/api/benefit-dispatch/resolve`
   - `joint-dispatch.logic.ts`
2. 退款页已完成：
   - `/api/refund/info/{benefitOrderNo}`
   - `/api/refund/apply`
   - `/api/refund/result/{refundId}`
   - `joint-refund.logic.ts`
3. 权益状态回推已完成：
   - `BenefitStatusPushLog`
   - `BenefitStatusPushService`
   - `SkeletonTechPlatformBenefitStatusClient`
   - 主库 / H2 schema 补齐
4. 已完成聚焦验证：
   - `BenefitDispatchControllerIntegrationTest`
   - `BenefitDispatchServiceTest`
   - `joint-dispatch.logic.test.ts`
   - `RefundControllerIntegrationTest`
   - `RefundServiceTest`
   - `joint-refund.logic.test.ts`
   - `BenefitStatusPushServiceTest`
5. 已补三份联调检查清单：
   - `docs/plan/20260418_分发页联调检查清单.md`
   - `docs/plan/20260418_退款页联调检查清单.md`
   - `docs/plan/20260418_权益状态回推联调检查清单.md`

### 当前边界

本轮仍未进入：

1. 齐为真实供应商跳转
2. 齐为真实退款协议接入
3. 科技平台真实状态回推协议接入
4. 分发页 / 退款页完整 UI 编排

## 9. 执行进展补充（退款外部适配占位层）

已继续沿“先留替换点、暂不伪造真实协议”的原则，补齐退款能力的外部适配占位层：

1. 新增退款客户端边界：
   - `src/main/java/com/nexusfin/equity/service/RefundClient.java`
2. 新增本地占位实现：
   - `src/main/java/com/nexusfin/equity/service/impl/SkeletonRefundClient.java`
3. 已将 `RefundServiceImpl` 调整为：
   - `getInfo` 继续走本地订单信息组装
   - `apply` 委托 `RefundClient`
   - `getResult` 委托 `RefundClient`
4. 已完成聚焦验证：
   - `mvn -q -Dtest=RefundServiceTest,RefundControllerIntegrationTest test`
5. 当前收益：
   - 退款入口与结果查询已具备明确的“外部协议替换点”
   - 后续齐为真实退款文档到位后，只需替换 `RefundClient` 实现，不必回改控制器或 H5 接口形态

### 当前边界

本轮仍未进入：

1. 齐为真实退款协议字段映射
2. 齐为真实退款签名 / 加解密接入
3. 退款结果回调或主动轮询策略落地

## 10. 执行进展补充（权益状态回推失败留痕与配置占位）

已继续把权益状态回推从“只有成功路径骨架”推进到“具备失败留痕与配置边界”的状态：

1. `BenefitStatusPushServiceImpl` 已补失败分支：
   - 科技平台回推成功时更新 `SUCCESS`
   - 科技平台回推失败时更新 `FAIL`
   - 失败信息写入 `errorMessage`
2. `SkeletonTechPlatformBenefitStatusClient` 已调整为配置感知占位实现：
   - `enabled=false` 时返回明确禁用错误
   - `mode=MOCK` 时允许本地占位通过
   - `mode=HTTP` 时继续明确提示真实契约尚未接好
3. 已完成聚焦验证：
   - `mvn -q -Dtest=BenefitStatusPushServiceTest,SkeletonTechPlatformBenefitStatusClientTest test`
4. 当前收益：
   - 状态回推失败不再只停留在异常抛出，已有数据库留痕
   - 后续科技平台真实协议接入前，可先使用 `MOCK` 模式维持本地链路

### 当前边界

本轮仍未进入：

1. 科技平台真实状态回推 HTTP 请求实现
2. 真实签名 / 加解密接入
3. 重试调度与补偿任务落地

## 11. 执行进展补充（异步消息补偿机制第一阶段闭环）

已继续按 `docs/plan/20260417_异步消息补偿机制方案与实施计划.md` 推进，并完成第一阶段“本地可持久化、可重试、可监控”的闭环实现：

1. 已完成异步补偿基础能力落地：
   - 异步补偿任务表 / 尝试日志表 / 分片心跳表已补齐
   - 已补 `enqueue`、`worker`、`supervisor` 三层服务
   - 已补重试退避、租约回收、死任务 / 积压任务扫描能力
2. 已完成两类核心补偿执行器：
   - 齐为权益购买补偿执行器
   - 云卡透传小花“用信提交”补偿执行器
3. 已补幂等与重复入队控制：
   - 同一业务键重复入队时可安全忽略
   - 执行前先查业务状态，避免重复发起外部请求
4. 已补本地业务承接逻辑：
   - 权益购买成功后可回写本地业务状态
   - 用信提交同步调用超时或报错时，后端可转入异步补偿链路
   - 对前端口径已统一为“已提交 / 审核中”，而不是简单同步失败
5. 已完成聚焦测试与 MySQL 回归：
   - 聚焦测试：
     - `mvn -q -Dtest=UpstreamTimeoutDetectorTest,AsyncCompensationSchemaIntegrationTest,AsyncCompensationEnqueueServiceTest,AsyncCompensationWorkerServiceTest,AsyncCompensationSupervisorServiceTest,QwBenefitPurchaseCompensationExecutorTest,YunkaLoanApplyCompensationExecutorTest,BenefitOrderServiceTest,Phase9TaskGroupEIntegrationTest test`
   - MySQL 回归：
     - `MYSQL_IT_ENABLED=true MYSQL_IT_DATABASE=nexusfin_equity mvn -Dtest=MySqlRoundTripIntegrationTest,MySqlCallbackFlowIntegrationTest test`
     - `MYSQL_IT_ENABLED=true MYSQL_IT_DATABASE=nexusfin_equity mvn -Dtest=MySqlAsyncCompensationIntegrationTest test`
     - `MYSQL_IT_ENABLED=true MYSQL_IT_DATABASE=nexusfin_equity mvn -Dtest=MySqlRoundTripIntegrationTest,MySqlCallbackFlowIntegrationTest,MySqlAsyncCompensationIntegrationTest test`
   - 结果：均已通过，`BUILD SUCCESS`

### 当前边界

本轮已明确暂不扩展到以下范围：

1. 退款链路异步补偿
2. 绑卡链路异步补偿
3. 权益状态回推异步补偿
4. 真实常驻部署编排（仅完成应用内 supervisor / worker 机制与数据库模型）

## 12. 今日日报、决策总结与 Decision Review

### 12.1 今日日报

今日主要围绕“异步消息补偿机制第一阶段闭环 + MySQL 真库回归”推进，收口结果如下：

1. 已完成异步补偿第一阶段骨架到可验证实现的闭环：
   - 任务持久化
   - 分片 worker
   - supervisor 监控
   - 重试退避
   - 租约回收
   - 死任务 / 积压任务识别
2. 已完成两条当前最关键补偿链路：
   - 齐为权益购买失败 / 超时补偿
   - 云卡透传小花“用信提交”失败 / 超时补偿
3. 已按确认口径收口前端承接方式：
   - 用信提交同步调用如超时，艾博生后端进入异步补偿
   - 前端页面展示“已提交”或“审核中”
4. 已补齐 MySQL 集成测试覆盖：
   - 异步任务入队持久化
   - worker 成功执行
   - 尝试日志留痕
   - supervisor 心跳持久化
   - 过期租约回收
   - 积压 / 死任务扫描
5. 已完成整组 MySQL 回归并通过：
   - `MySqlRoundTripIntegrationTest`
   - `MySqlCallbackFlowIntegrationTest`
   - `MySqlAsyncCompensationIntegrationTest`

### 12.2 今日决策总结

今日新增并已落地的关键决策如下：

1. 异步消息补偿采用方案 A：
   - 同步调用异常时，业务先给前端稳定承接口径
   - 后端再通过异步任务补偿外部调用
2. 用信提交链路前端口径调整为：
   - 若后端内部调用云卡超时，前端不再简单展示失败
   - 统一展示“已提交 / 审核中”
3. 异步补偿第一阶段只覆盖最关键主链路：
   - 权益购买
   - 用信提交
4. 补偿执行必须带幂等保护：
   - 重复入队不重复执行
   - 执行前先查业务状态，避免重复调用外部
5. 真实部署形态暂不在本轮展开：
   - 先完成本地 MySQL 数据模型、worker、supervisor 和验证
   - 常驻进程 / 多实例编排后续单独展开

### 12.3 Decision Review

对今天异步补偿相关决策回看，结论如下：

1. **方案 A：同步承接 + 异步补偿**
   - 结论：成立
   - 结果：兼顾了用户前端体验与后端补偿能力，避免“接口超时即流程断裂”
2. **先覆盖权益购买与用信提交两条主链路**
   - 结论：成立
   - 结果：范围可控，能先解决当前最容易影响主流程成交与放款的风险点
3. **补偿执行必须建立在本地持久化和幂等前提上**
   - 结论：成立
   - 结果：当前实现已具备任务留痕、重复入队保护和执行前状态校验
4. **先完成 MySQL 真库回归，再讨论扩面**
   - 结论：成立
   - 结果：当前不是只停留在单测层，已经在真实 MySQL 上验证关键链路
5. **常驻部署与多分片编排后置**
   - 结论：成立
   - 结果：避免在真实运行方式尚未确认时，把当前任务扩大成运维改造项目

### 12.4 今日风险与边界

1. 当前补偿能力仍是第一阶段：
   - 已有应用内 worker / supervisor 机制
   - 但尚未落到正式常驻进程部署方案
2. 当前只覆盖两条主链路：
   - 权益购买
   - 用信提交
3. 真实外部环境仍依赖：
   - 云卡真实服务完成并可调用
   - 小花真实协议 / 加解密 / 联合登录进一步确认

### 12.5 收口判断

> 今天可以判断为：异步消息补偿机制第一阶段已经完成“方案确认 → 代码落地 → MySQL 真库验证 → 文档沉淀”的闭环；明天可以进入“运维承接说明、人工操作手册、控制器级链路补齐验证”的下一步推进。
