# 2026-04-13 重点工作计划

> 今日主线：正式启动 `Phase 9` 后端实现，优先完成任务组 A，并为云卡 gateway 基础设施搭好骨架。

## 1. 计划目标

截至 `2026-04-12`，`Phase 9` 已经完成：

1. 云卡 gateway 执行边界定版
2. `Phase 9` 任务拆解与实施顺序确认
3. 现有实现差异分析与可执行开发清单输出

因此 `2026-04-13` 的工作重点，不再是继续讨论方案，而是围绕“先最小闭环、再基础设施”推进三件事：

1. 正式落 H5 专用接口层骨架：`LoanController`、`BenefitsController`、`RepaymentController`
2. 优先完成任务组 A 的 3 个接口：
   - `GET /api/loan/calculator-config`
   - `GET /api/benefits/card-detail`
   - `POST /api/benefits/activate`
3. 同时为任务组 B 预埋云卡 gateway 配置与客户端骨架，但不提前展开完整借还款链路

说明：

1. 今天默认仍按“云卡服务由其他同学并行开发”的前提推进。
2. 今天的目标是把 `Phase 9` 从“分析完成”推进到“代码已启动且已有最小接口闭环”。

## 2. 今日优先级

### P0：搭好 H5 专用接口层骨架

**目标**

- 将 H5 `Phase 9` 接口与当前 `/api/equity/*` 旧接口正式隔离。

**计划动作**

1. 新增 `LoanController`
2. 新增 `BenefitsController`
3. 新增 `RepaymentController`
4. 新增对应 Service 接口与实现骨架
5. 新增任务组 A 所需 DTO

**预期产出**

1. H5 专用接口路径正式落地
2. 后续开发不再混入旧接口职责

### P0：完成任务组 A

**目标**

- 先打通不依赖云卡真实环境的最小接口闭环。

**计划动作**

1. 实现 `GET /api/loan/calculator-config`
   - 使用配置项承载金额范围、默认金额、期数、收款账户
   - 固定返回 3 期
2. 实现 `GET /api/benefits/card-detail`
   - 优先复用现有 `BenefitProduct` 与权益服务能力
   - 不足展示字段优先走本地配置补齐
3. 实现 `POST /api/benefits/activate`
   - 通过 H5 适配层复用现有 `BenefitOrderService.createOrder()` 主链
4. 为以上接口补充请求 / 响应 DTO 与必要校验

**预期产出**

1. 3 个任务组 A 接口具备本地开发与自测条件
2. H5 首页与权益页具备先行联调基础

### P0：补云卡 gateway 基础设施骨架

**目标**

- 为后续借款 / 还款 7 个接口提前准备统一转发基础设施。

**计划动作**

1. 新增云卡配置类
2. 抽取云卡统一 gateway URI
3. 抽取 `/loan/trail` 等路径配置
4. 新增云卡客户端接口与实现骨架

**预期产出**

1. 任务组 B 有稳定起点
2. 后续所有云卡接口都能复用同一封装方式

### P1：为 `applicationId` 映射设计预留模型

**目标**

- 避免任务组 C / E 启动时再回头补基础建模。

**计划动作**

1. 明确 `applicationId` 映射需要保存的最小字段
2. 判断是扩展现有表还是新增映射表
3. 若时间允许，完成实体和仓储骨架

**预期产出**

1. 任务组 C / E 的持久化设计方向明确

### P1：完成一轮最小验证

**目标**

- 确保今天启动的代码不是只停留在文件创建层。

**计划动作**

1. 针对新增接口补充最小单测或集成测试
2. 若时间允许，运行最小测试集
3. 检查新增接口是否纳入鉴权路径与统一返回风格

**预期产出**

1. 任务组 A 实现具备初步可信度

## 3. 建议执行顺序

建议按以下顺序推进：

1. 先搭 H5 专用接口层骨架
2. 再实现 `calculator-config`
3. 再实现 `benefits/card-detail`
4. 再实现 `benefits/activate`
5. 再补云卡 gateway 配置与客户端骨架
6. 最后处理 `applicationId` 映射预留与最小测试

原因：

1. 先把接口边界立住，再填单个接口，返工最少。
2. `calculator-config` 最简单，可以先形成第一个可运行样板。
3. 两个 `benefits` 接口最容易复用现有权益能力，适合作为启动阶段的主实现面。
4. 云卡基础设施今天只需起骨架，不必过早扩展到完整借还款接口。

## 4. 今日完成标准

如果 `2026-04-13` 结束前达到以下结果，可以认为当天工作达标：

1. H5 专用接口层骨架已落地
2. 任务组 A 的 3 个接口至少完成大部分实现
3. 云卡 gateway 配置和客户端骨架已建立
4. 至少完成一轮最小测试或本地校验

## 5. 风险提示

`2026-04-13` 的计划主要受以下因素影响：

1. `benefits/card-detail` 所需展示字段若现有库表不足，需接受“部分字段先配置化承载”的实现方式。
2. `benefits/activate` 的 H5 入参与现有权益下单主链不一致，今天需要尽快确定适配映射。
3. 云卡服务虽未就绪，但今天不应因此阻塞本地客户端和配置骨架的搭建。

## 6. 当前进展更新（截至 2026-04-13）

基于今日已完成的推进，当前状态更新如下：

### 6.1 已完成事项

1. `Phase 9` 任务组 A 已完成主体实现：
   - `GET /api/loan/calculator-config`
   - `GET /api/benefits/card-detail`
   - `POST /api/benefits/activate`
2. H5 专用接口层骨架已落地：
   - `LoanController`
   - `BenefitsController`
   - `RepaymentController`
3. 云卡 gateway 基础设施骨架已预埋：
   - 云卡配置类
   - 统一 gateway URI 封装
   - `path + data` 请求模型
   - 云卡客户端接口与实现骨架
4. `applicationId` 本地映射的基础模型已预留
5. 任务组 A 已完成一轮最小测试验证

### 6.2 新增已完成设计事项

除 `Phase 9` 主线外，今日新增的 `H5` 多语言支持方案也已完成设计沉淀，结论如下：

1. 支持 `zh-CN`、`zh-TW`、`en-US`、`vi-VN`
2. 进入页面先按浏览器语言自动匹配
3. 允许手动切换并持久化用户选择
4. 覆盖 `H5 + 后端 + 协议/富文本内容`
5. 采用“方案 C：统一文案资源中心”

对应文档：

- `plan/20260413_H5多语言支持方案设计.md`

### 6.3 后续执行焦点

在任务组 A 完成后，后续执行顺序建议更新为：

1. 继续推进任务组 B：统一云卡 gateway 转发基础设施完善
2. 紧接任务组 C：优先实现 `loan/calculate`、`approval-status`、`approval-result`
3. 再推进任务组 D：还款链路接口
4. 最后收口任务组 E：`loan/apply` 编排链路

### 6.4 当前一句话判断

> `2026-04-13` 的工作已从“启动实现”推进到“任务组 A 完成、云卡骨架完成、可继续进入任务组 B/C”的状态。

## 7. 任务组 B/C 推进更新（截至 2026-04-13）

基于确认后的继续执行，当前已进一步完成：

### 7.1 任务组 B：云卡 gateway 基础设施增强

1. 已复用云卡统一 gateway 客户端骨架
2. 已在 `loan/calculate` 中按配置读取 `/loan/trail`
3. 已在审批查询中按配置读取 `/loan/query`
4. 已形成 `requestId + path + bizOrderNo + data` 的统一请求封装实践

### 7.2 任务组 C：贷款查询类接口

已完成以下 3 个接口的主体实现与测试覆盖：

1. `POST /api/loan/calculate`
   - 转发云卡 gateway
   - 默认科技平台 path：`/loan/trail`
   - 将 H5 金额转换为科技平台所需金额单位后转发
   - 将云卡返回的还款计划映射为 H5 试算结果
2. `GET /api/loan/approval-status/{applicationId}`
   - 根据本地 `applicationId` 映射查询上游 `loanId`
   - 转发云卡 gateway `/loan/query`
   - 映射为“审批中”页面所需状态与步骤模型
3. `GET /api/loan/approval-result/{applicationId}`
   - 与审批状态查询共用 `/loan/query`
   - 映射为“审批结果”页面所需结果模型
   - 放款成功时返回 `loanId`

### 7.3 本轮验证

已完成验证：

1. `mvn -q -Dtest=Phase9TaskGroupCIntegrationTest test`
2. `mvn -q -Dtest=Phase9TaskGroupAIntegrationTest,Phase9TaskGroupCIntegrationTest test`
3. `mvn -q checkstyle:check`

验证结果：

> 以上命令均已通过。

### 7.4 下一步建议

后续建议进入任务组 D：

1. `GET /api/repayment/info/{loanId}` → 云卡 `/repay/trial`
2. `POST /api/repayment/submit` → 云卡 `/repay/apply`
3. `GET /api/repayment/result/{repaymentId}` → 云卡 `/repay/query`

## 8. 任务组 D 推进更新（截至 2026-04-13）

基于继续执行，任务组 D 已完成主体实现与测试覆盖。

### 8.1 已完成接口

1. `GET /api/repayment/info/{loanId}`
   - 转发云卡 gateway
   - 默认科技平台 path：`/repay/trial`
   - 将云卡返回的 `repayAmount` 映射为 H5 还款金额
   - 返回 H5 所需银行卡展示信息与还款提示
2. `POST /api/repayment/submit`
   - 转发云卡 gateway
   - 默认科技平台 path：`/repay/apply`
   - 将 H5 金额转换为科技平台所需金额单位后转发
   - 将云卡返回的 `swiftNumber` 映射为 H5 `repaymentId`
3. `GET /api/repayment/result/{repaymentId}`
   - 转发云卡 gateway
   - 默认科技平台 path：`/repay/query`
   - 按已确认口径，不由艾博生本地生成 `repaymentId`
   - 以科技平台 / 云卡返回的还款结果映射 H5 成功页模型

### 8.2 本轮验证

已完成验证：

1. `mvn -q -Dtest=Phase9TaskGroupDIntegrationTest test`
2. `mvn -q -Dtest=Phase9TaskGroupAIntegrationTest,Phase9TaskGroupCIntegrationTest,Phase9TaskGroupDIntegrationTest test`
3. `mvn -q checkstyle:check`

验证结果：

> 以上命令均已通过。

### 8.3 下一步建议

后续建议进入任务组 E：

1. `POST /api/loan/apply`
2. 先调用齐为购买权益
3. 权益购买成功后再调用云卡 `/loan/apply`
4. 若“权益成功、云卡失败”，对 H5 返回借款 / 放款失败，同时明确权益购买成功

## 9. 任务组 E 推进更新（截至 2026-04-13）

基于继续执行，`Phase 9` 最后一组任务已完成主体实现与测试覆盖。

### 9.1 已完成接口

1. `POST /api/loan/apply`
   - 先调用齐为权益购买主链
   - 权益购买成功后再调用云卡 gateway `/loan/apply`
   - 艾博生本地生成 `applicationId`
   - 同步生成本地 `loanId` 参与云卡请求
   - 云卡成功后，保存 `applicationId -> loanId` 映射

### 9.2 部分成功场景处理

已按确认决策实现以下口径：

1. 若齐为权益购买成功、云卡借款申请失败
2. 对 H5 返回借款失败状态
3. 同时明确标识权益已购买成功
4. 不落本地 `applicationId -> loanId` 成功映射

### 9.3 本轮验证

已完成验证：

1. `mvn -q -Dtest=Phase9TaskGroupEIntegrationTest test`
2. `mvn -q -Dtest=Phase9TaskGroupAIntegrationTest,Phase9TaskGroupCIntegrationTest,Phase9TaskGroupDIntegrationTest,Phase9TaskGroupEIntegrationTest test`
3. `mvn -q checkstyle:check`

验证结果：

> 以上命令均已通过。

### 9.4 当前阶段结论

截至当前，`Phase 9` 已完成：

1. 任务组 A：本地配置与齐为直连能力
2. 任务组 B：云卡 gateway 基础设施
3. 任务组 C：贷款试算与审批查询
4. 任务组 D：还款链路
5. 任务组 E：借款申请编排

一句话结论：

> `Phase 9` 的 10 个 H5 后端接口已全部完成主体实现，并已通过当前阶段的集成测试回归与代码规范检查。

## 10. H5 多语言执行更新（截至 2026-04-13）

基于已确认的 `H5` 多语言方案与实施计划，当前已继续完成以下落地内容。

### 10.1 前端已完成事项

1. 已新增 H5 多语言基础设施：
   - `locale.ts`
   - `messages.ts`
   - `content.ts`
   - `I18nProvider.tsx`
   - `LanguageSwitcher.tsx`
2. 已在 `App.tsx` 接入全局 `I18nProvider`
3. 已在 `MobileLayout.tsx` 接入语言切换入口
4. 已完成 6 个主页面的静态文案替换：
   - `CalculatorPage`
   - `ApprovalPendingPage`
   - `BenefitsCardPage`
   - `ApprovalResultPage`
   - `ConfirmRepaymentPage`
   - `RepaymentSuccessPage`
5. 已支持：
   - 浏览器语言自动识别
   - 手动切换
   - 本地持久化

### 10.2 后端已完成事项

1. 已新增 `Accept-Language` 解析骨架：
   - `H5Locale`
   - `H5LocaleContext`
   - `H5LocaleFilter`
2. 已新增统一后端文案资源入口：
   - `H5I18nService`
   - `H5I18nServiceImpl`
3. 已将以下 H5 响应展示字段接入语言资源骨架：
   - `GET /api/benefits/card-detail`
   - `GET /api/loan/calculator-config`
   - `GET /api/loan/approval-status/{applicationId}`
   - `GET /api/loan/approval-result/{applicationId}`
   - `GET /api/repayment/info/{loanId}`
   - `GET /api/repayment/result/{repaymentId}`
4. 已让后端响应按请求语言返回 `Content-Language`
5. 已覆盖协议标题、权益提示、审批步骤文案、还款提示等首批长文案/富文本类字段

### 10.3 本轮验证

已新增并通过多语言相关回归验证：

1. `Phase9TaskGroupAIntegrationTest`
   - 新增英文 `benefits/card-detail` 返回断言
2. `Phase9TaskGroupDIntegrationTest`
   - 新增英文 `repayment/info` 返回断言
3. 已通过验证命令：
   - `mvn -q -Dtest=Phase9TaskGroupAIntegrationTest,Phase9TaskGroupDIntegrationTest test`
   - `mvn -q -Dtest=Phase9TaskGroupAIntegrationTest,Phase9TaskGroupCIntegrationTest,Phase9TaskGroupDIntegrationTest,Phase9TaskGroupEIntegrationTest test`
   - `mvn -q checkstyle:check`
   - `cd H5 && ./node_modules/.bin/tsc --noEmit`

验证结果：

> 后端多语言骨架、现有 `Phase 9` 主链回归、以及 H5 多语言代码的类型校验均已通过。

### 10.4 当前遗留与阻塞

1. H5 本地依赖已补齐：
   - 已通过 `npm exec --yes pnpm@10.4.1 -- install --frozen-lockfile` 完成安装
   - 无需依赖全局 `pnpm`
2. H5 `TypeScript` 实际命令校验已完成：
   - `cd H5 && ./node_modules/.bin/tsc --noEmit`
3. 当前剩余工作已从“环境阻塞”切换为“真实接口联调与页面映射补验”

### 10.5 当前一句话判断

> `H5 + 后端 + 协议/富文本` 的多语言第一阶段实现已完成，当前可直接进入真实 API 联调与页面字段补验。

## 11. H5 页面接口联通更新（截至 2026-04-13）

基于已完成的 `Phase 9` 后端接口与 H5 多语言基础设施，当前已继续完成 H5 主页面到真实接口的首轮接入。

### 11.1 已完成接入范围

1. 已新增 H5 统一请求层与类型化 API：
   - `H5/client/src/lib/api.ts`
   - `H5/client/src/lib/loan-api.ts`
2. 已新增 H5 跨页流程状态：
   - `H5/client/src/contexts/LoanContext.tsx`
3. 已新增辅助能力：
   - 金额/日期/银行卡格式化
   - 查询参数拼接与读取
   - 通用加载/错误反馈组件
   - 审批步骤卡片组件
4. 已完成以下页面真实接口替换：
   - `CalculatorPage` → `GET /api/loan/calculator-config`、`POST /api/loan/calculate`、`POST /api/loan/apply`
   - `ApprovalPendingPage` → `GET /api/loan/approval-status/{applicationId}`
   - `BenefitsCardPage` → `GET /api/benefits/card-detail`、`POST /api/benefits/activate`
   - `ApprovalResultPage` → `GET /api/loan/approval-result/{applicationId}`
   - `ConfirmRepaymentPage` → `GET /api/repayment/info/{loanId}`、`POST /api/repayment/submit`
   - `RepaymentSuccessPage` → `GET /api/repayment/result/{repaymentId}`

### 11.2 本轮实现口径

1. H5 请求默认带上：
   - `withCredentials`
   - 当前手动切换后的 `Accept-Language`
2. 流程关键标识已在前端统一维护并支持恢复：
   - `applicationId`
   - `loanId`
   - `repaymentId`
3. 为兼容页面刷新与跳转回流，已支持通过查询参数恢复关键标识：
   - `/approval-pending?applicationId=...`
   - `/approval-result?applicationId=...`
   - `/confirm-repayment?loanId=...`
   - `/repayment-success?repaymentId=...`
4. 已兼容“权益购买成功但借款申请失败”的前端展示口径：
   - `loan/apply` 返回 `loan_failed` 时，H5 进入结果页并展示失败结论与权益已成功的提示信息

### 11.3 已完成验证

1. H5 类型检查通过：
   - `cd H5 && ./node_modules/.bin/tsc --noEmit`
2. 结合此前已通过的后端回归，当前状态为：
   - `Phase 9` 后端接口主体已完成
   - H5 主流程页面已完成首轮接口接入
   - H5 多语言能力仍保持生效

### 11.4 当前剩余工作

1. 真实联调环境下补做页面走查：
   - SSO 登录态 / Cookie 注入
   - 页面跳转与轮询链路
   - 云卡联调返回字段与前端映射核验
2. 如需进一步收口，可继续补做：
   - `pnpm build` 构建验证
   - 本地浏览器端到端走查

### 11.5 当前一句话判断

> H5 六个主页面已完成对 `Phase 9` 接口的首轮联通，当前可以进入真实环境联调和页面级走查。

## 12. 今日日报、决策总结与 Decision Review

### 12.1 今日日报

今日围绕 `Phase 9 + H5` 主线，完成了从“后端接口完成”到“H5 页面接入、构建收口”的推进，主要结果如下：

1. `Phase 9` 的 10 个 H5 后端接口已全部完成，并已通过集成测试与 `checkstyle`
2. H5 多语言第一阶段已完成，覆盖：
   - `zh-CN`
   - `zh-TW`
   - `en-US`
   - `vi-VN`
3. H5 已新增统一请求层、流程状态管理、格式化与路由恢复能力
4. H5 六个主页面已完成首轮真实接口接入：
   - 试算页
   - 审批中页
   - 权益详情页
   - 审批结果页
   - 确认还款页
   - 还款成功页
5. 已完成 H5 代码校验：
   - `cd H5 && ./node_modules/.bin/tsc --noEmit`
   - `cd H5 && npm exec --yes pnpm@10.4.1 -- build`
6. 已完成 `Phase 0` 残留清理：
   - 移除 analytics script
   - 移除 `vite-plugin-manus-runtime`
   - 移除 debug collector
   - 删除 `H5/client/public/__manus__`
   - 补齐 `/api` → `http://localhost:8080` 代理
7. 已完成本地可访问性验证：
   - `H5 dev server` 可正常启动
   - 首页可打开
   - 当前阻塞已明确为“后端 `8080` 未启动导致代理 `ECONNREFUSED`”

### 12.2 今日决策总结

今日已沉淀并实际执行的关键决策如下：

1. H5 请求层采用统一封装：
   - 默认 `withCredentials`
   - 透传当前多语言 `Accept-Language`
   - 保留 `Authorization: Bearer` 扩展位
2. H5 流程状态由前端统一维护并使用 `sessionStorage` 持久化：
   - `applicationId`
   - `loanId`
   - `repaymentId`
   - `benefitsCardActivated`
3. 页面刷新与跨页回流统一通过查询参数恢复关键标识：
   - `/approval-pending?applicationId=...`
   - `/approval-result?applicationId=...`
   - `/confirm-repayment?loanId=...`
   - `/repayment-success?repaymentId=...`
4. “权益成功、借款失败”继续按既定口径处理：
   - H5 返回借款失败
   - 同时明确权益已购买成功
5. H5 构建收口优先于联调走查：
   - 先消除 `Phase 0` 残留告警
   - 再进入真实环境联调
6. 当前联调前置条件已明确：
   - Java 后端需启动在 `localhost:8080`
   - 需具备有效 `NEXUSFIN_AUTH` 登录态 Cookie

### 12.3 Decision Review

对今天关键决策进行回看，结论如下：

1. **多语言方案 C（统一文案资源中心）**
   - 结论：成立
   - 结果：前后端多语言骨架已经落地，页面和接口展示字段已能统一跟随语言切换
2. **H5 使用统一请求层 + 统一流程状态**
   - 结论：成立
   - 结果：减少了页面各自维护状态的复杂度，便于跨页恢复 `applicationId` / `loanId` / `repaymentId`
3. **先收口构建与工程残留，再做真实联调**
   - 结论：成立
   - 结果：已消除 analytics / Manus 残留告警，构建输出已稳定，后续联调定位问题更简单
4. **通过查询参数恢复关键标识**
   - 结论：成立
   - 结果：兼容刷新与页面直达，降低联调过程中“上下文丢失”的概率
5. **“权益成功、借款失败”由 H5 结果页显式承接**
   - 结论：成立
   - 结果：已与既定业务口径一致，前端实现也已完成兼容
6. **当前最大阻塞不在 H5，而在联调环境前置条件**
   - 结论：成立
   - 结果：本地验证已证明当前 500 实际来自 `localhost:8080` 未启动，而非 H5 构建或路由问题

### 12.4 今日风险与阻塞

1. 当前并非 H5 路由或构建问题，主要阻塞是：
   - `localhost:8080` 未启动
2. 即使后端启动，真实联调仍依赖：
   - SSO / Cookie 注入路径
   - 云卡侧联调窗口与可用响应

### 12.5 收口判断

> 今天已经完成“代码实现 + 类型检查 + 生产构建 + Phase 0 清理 + 本地可访问性验证”，明天可以直接进入真实联调与页面走查。
