# 2026-04-04 重点工作计划

## 1. 计划目标

截至 `2026-04-03` 结束，当前项目状态已经进一步收敛为：

1. 齐为主流程第一轮生产化结构已经成立，且保持有效。
2. 支付协议号相关设计已从“通联最终方案”降级为“当前按通联假设的阶段性基线”。
3. `M3.2` 入站回调联调的本地准备已完成，当前阻塞明确落在公网测试域名、路径暴露方式、白名单三项外部条件。
4. 电子签章服务商已明确由上上签切换为法大大，但法大大正式接入文档尚未启动。
5. 科技平台整条线继续 `HOLD`，暂不进入主执行线。

因此 `2026-04-04` 的重点，不再是继续泛化铺开新内容，而是围绕“等待外部条件落定、把可切换材料准备完整、保持主线稳定”推进三件事：

1. 继续跟进支付渠道最终确认，并把阶段性方案与正式切换动作之间的差异边界说清楚。
2. 继续跟进 `M3.2` 三项公网条件，若外部回复到位，则立即切换为执行态。
3. 视信息成熟度，启动法大大接入文档的第一轮承接准备。

说明：

1. 明天主线仍是 **齐为主流程生产化收尾 + 外部条件确认**。
2. 科技平台继续保持 `HOLD`，不进入明天主执行线。

## 2. 明日优先级

### P0：继续收口支付渠道待定后的执行边界

**目标**

- 确保后续一旦支付渠道最终拍板，可以最小成本完成文档与实现切换。

**对应里程碑**

- `M3.1 出站接口主流程生产化准备`

**计划动作**

1. 回看当前支付协议号相关文档的阶段性口径是否仍一致：
   - `docs/third-part/20260402_通联真实payProtocolNo接入整体设计方案.md`
   - `docs/third-part/20260403_通联服务对接流程与上线切换说明.md`
   - `docs/decision-review/20260403_支付渠道待定口径调整决策记录.md`
2. 若当天拿到新的支付渠道确认信息，优先回填：
   - 正式渠道名
   - `provider_code` 最终口径
   - 真实协议号来源说明
   - 上线切换步骤
3. 若仍未拿到最终结论，则补一版“阶段性基线 -> 正式切换”的差异清单。

**预期产出**

1. 一份最新的支付渠道状态结论
2. 若条件具备，完成阶段性方案向正式方案的第一轮切换
3. 若条件不具备，至少补齐差异清单，避免后续临时重梳理

### P0：继续跟进 `M3.2` 公网回调启动条件

**目标**

- 明确 `M3.2` 是否能够从“准备态”切换到“执行态”。

**对应里程碑**

- `M3.2 入站回调联调`

**计划动作**

1. 继续向网关 / 运维确认：
   - 公网测试域名
   - 回调路径暴露方式
   - 白名单策略
2. 若外部回复到位：
   - 补齐正式回调 URL 清单
   - 更新启动建议
   - 准备进入首轮入站回调联调
3. 若外部仍未回复：
   - 继续保持阻塞状态文档为最新
   - 不虚假标记 `M3.2` 已启动

**预期产出**

1. 一份最新的 `M3.2` 状态判断
2. 若条件满足，形成可执行的启动清单
3. 若条件未满足，形成最新阻塞结论

### P1：启动法大大接入文档的第一轮准备

**目标**

- 把电子签章服务商切换从“结论”推进到“可承接的文档准备”。

**对应里程碑**

- `M4 验收闭环准备`

**计划动作**

1. 回看当前与签章切换相关的已有文档：
   - `docs/decision-review/20260403_电子签章服务商由上上签切换为法大大决策记录.md`
   - `docs/删除记录_20260402_【上上签】新CA合规接口调用流程.pdf.md`
2. 梳理法大大正式接入文档第一版需要承接的结构：
   - 接入目标
   - 关键接口
   - 回调要求
   - 配置项
   - 上线切换注意点
3. 若当天没有更多法大大输入，则先整理目录和文档框架，不提前编造实现细节。

**预期产出**

1. 一份法大大文档启动判断
2. 若条件允许，产出法大大接入说明第一版框架

### P0（HOLD）：科技平台事项继续暂停

**目标**

- 保持边界稳定，避免在整体调整前继续投入返工成本。

**计划动作**

1. 明天不继续推进科技平台代码改造
2. 明天不继续推进科技平台联调执行细节
3. 仅在收到新的确定性口径时更新 `HOLD` 说明

**预期产出**

1. 科技平台继续保持 `HOLD`
2. 不新增无效投入

## 3. 建议执行顺序

建议按以下顺序推进：

1. 先确认支付渠道是否有新增结论。
2. 再确认 `M3.2` 三项公网条件是否有外部回复。
3. 再视当天信息成熟度，决定是否启动法大大文档第一版。
4. 科技平台整条线继续保持 `HOLD`。

原因：

1. 当前最有可能触发全局口径变化的是支付渠道最终确认。
2. `M3.2` 是否能启动，仍完全依赖外部条件齐备与否。
3. 法大大文档可先准备，但不应在信息不足时写成伪定稿。

## 4. 明日完成标准

如果 `2026-04-04` 结束前达到以下结果，可以认为当天重点工作达标：

1. 已获得支付渠道新增结论，或至少补齐“阶段性基线 -> 正式切换”的差异清单。
2. 已获得 `M3.2` 三项公网条件的新增状态；若条件满足，形成正式启动清单。
3. 法大大文档至少完成启动判断；若条件允许，产出第一版框架。
4. 科技平台继续保持 `HOLD`，无额外返工投入。

## 5. 风险提示

`2026-04-04` 的计划主要受以下因素影响：

1. 若支付渠道继续待定，则相关文档仍只能维持阶段性基线状态。
2. 若网关 / 运维继续未回复，`M3.2` 仍无法切换到执行态。
3. 若法大大仍无进一步输入，电子签章只能停留在“切换结论”层，不能贸然展开实现细节。
4. 若科技平台口径突然恢复推进，需要先重新校正基线后再进入执行。
