# 2026-04-02 开发任务安排

## 1. 今日工作主线

今天的工作不再以“继续扩功能”为主，而是围绕 **齐为联调收口、科技平台外部确认、公网回调条件梳理** 三条线推进。

统一原则：

1. **先收口最接近完成的链路**，即齐为 `M3.1`
2. **外部未确认前不做过度改造**
3. **每拿到一条新结论，优先回填文档**

## 2. 任务拆分

### A 线：齐为联调主执行

#### A1. 复盘 `abs.token.get` 当前证据链

**目标**

- 把“当前到底用什么参数请求、齐为到底返回了什么、还差什么确认”说清楚。

**涉及文件**

- `src/main/java/com/nexusfin/equity/thirdparty/qw/QwBenefitClientImpl.java`
- `docs/bug/20260401_齐为504数据协议非法分析.md`
- `docs/third-part/齐为/20260401_齐为线上部署说明.md`

**动作**

1. 回看现有真实日志中的 `partnerOrderNo`、`orderNo`、`cardNo`
2. 确认 `abs.token.get` 当前是否仅使用我方订单号
3. 输出“当前实现 / 齐为返回字段 / 待确认字段”的对照结果

**完成标准**

- 能明确说出下一次 `abs.token.get` 重试应该沿用什么参数，或等待什么确认

#### A2. 准备 `abs.token.get` 二次验证

**目标**

- 今天一旦拿到齐为新口径，可以立即验证，不再重复准备工作。

**动作**

1. 预备延时重试方案
2. 预备按 `orderNo / cardNo` 调整的代码入口
3. 保证真实调用日志能够完整落证

**完成标准**

- 新反馈一到即可真实调用验证

#### A3. 推进 `abs.lending.notify`

**目标**

- 尽量把齐为第三个出站接口从“待启动”推进到“已准备好验证”。

**动作**

1. 复核现有代码入口和日志埋点
2. 列出仍缺的成功 / 失败样例
3. 若样例齐备，直接启动首轮真实验证

**完成标准**

- 至少形成一份明确的“可验证 / 仍阻塞”判断

### B 线：科技平台并行跟进

> `2026-04-02` 最新口径：科技平台事项 **整体 HOLD**。今天不继续推进代码实现、不继续推进联调细节，只保留状态记录与待确认项。

#### B1. HOLD 状态下保留最小待确认项

**目标**

- 今天不继续执行，只保留最小待确认项，供后续恢复时接续。

**动作**

1. `/api/users/me` 真实测试地址
2. Bearer Token 样例或获取方式
3. 真实返回字段结构

**完成标准**

- 后续恢复推进时，能够直接从这三项重新接续

#### B2. HOLD 期间执行原则

**目标**

- 在接口仍会继续变化、且后续会整体调整的前提下，今天严格不动科技平台代码。

**动作**

1. 今天不继续推进科技平台代码改造
2. 今天不继续推进科技平台联调执行细节
3. 等整体口径定稿后再集中恢复

**完成标准**

- 明确今天不会误推进科技平台事项

### C 线：公网回调准备

#### C1. 固定 `M3.2` 启动条件

**目标**

- 让后续与网关 / 运维同步时，能直接给出明确问题清单。

**动作**

1. 明确公网测试回调域名
2. 明确回调路径暴露方式
3. 明确白名单策略

**完成标准**

- 若未确认，也能明确说明为什么 `M3.2` 还不能启动

## 3. 今日建议执行顺序

1. 先做 A1，避免今天继续在齐为问题上信息不全
2. 再准备 A2 / A3，把随时可验证的动作预热好
3. 中午前后并行推进 B1
4. 下午推进 C1，并同步 B2
5. 下班前统一回填所有文档

## 4. 今日交付物

今天建议至少沉淀以下交付物：

1. 一份更清晰的齐为 `abs.token.get` 阻塞判断
2. 一份 `abs.lending.notify` 当前可验证性结论
3. 一份科技平台最小闭环输入状态更新
4. 一份公网回调启动条件状态更新
5. 更新后的 plan / checklist / third-part / bug 文档

## 5. 注意事项

1. 今天若要删除任何 `docs/` 下文件，必须先创建删除记录文档。
2. 外部未确认前，优先记录差异，不做过度实现。
3. 真联调日志、接口返回和结论必须成对留痕，避免晚上回填时再丢上下文。
