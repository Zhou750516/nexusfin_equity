# 2026-03-30 提交说明草稿

## 1. 本轮改动主线

本轮工作围绕以下五条主线收口：

1. 幂等与审计
2. 跨域认证
3. 齐为接入
4. 安全整改
5. 命名收敛

对应核心提交：

- `140a0d9 Fix order idempotency and callback audit handling`
- `48e9ad6 feat: implement cross-domain auth flow`
- `8e8e2e2 feat: integrate qw upstream and harden auth flows`

## 2. 已完成并验证通过

以下事项已完成，并且当前仓库已通过 2026-03-30 当天重新执行的 `mvn -q test` 验证：

1. 创建权益订单补齐 `requestId` 级幂等，重复请求可回放历史订单结果。
2. 通知回调统一收敛为 `RECEIVED -> PROCESSED/FAILED` 审计状态，缺单或异常场景不会误记成功。
3. 新增 `GET /api/auth/sso-callback` 与 `GET /api/users/me`，建立本地 JWT Cookie 登录态。
4. `/api/equity/**` 业务接口改为从本地认证上下文解析会员，不再接收显式 `memberId`。
5. 下线旧的 `/api/users/register` 静默注册入口，主链切换为 auth-first。
6. 接入齐为协议适配层，完成会员同步、联登 URL、放款通知 3 个主动调用能力的代码接线。
7. 补齐 SSO 建档敏感字段的入站解码、格式校验、AES-GCM 加密与 hash 索引。
8. 收敛状态字段与 Provider 命名，代码、API 输出与新库初始化脚本统一使用中性命名。

本次执行结果：

- `mvn -q test`
- 结果：`tests=61 failures=0 errors=0 skipped=2`

## 3. 已落代码但仍需联调验证

以下事项已经有代码实现，但是否可真实上线仍取决于外部联调结果：

1. 齐为 3 个主动调用接口在 `HTTP` 模式下的真实请求报文、密钥、AES 配置是否与对方测试环境完全匹配。
2. 科技平台用户信息接口的真实测试地址、token 获取方式、字段完整性是否与当前解析逻辑一致。
3. 齐为扣款通知只有一个业务定义，而我方本地拆成 `first-deduction` 和 `fallback-deduction` 两个入口，联调时还需要确认路由规则。
4. 齐为行权、退款、还款等回调字段是否需要进一步扩展 DTO 以便完整留痕和对账。

## 4. 明确属于后续增强

以下内容不属于本轮提交闭环，建议单独排期：

1. `TechPlatformUserClientImpl` 的 retry 机制和更细粒度失败可观测日志。
2. `BenefitOrderController` 中产品页、查单、行权链接访问日志的进一步补齐。
3. 历史评审/设计文档中旧文件名与旧表述的持续清理。
4. 齐为真实回调协议与当前内部 DTO 之间的完整字段映射增强。

## 5. 文档齐套性核对

当前与本轮改动直接相关的文档已经具备：

- 周报：`docs/周报/20260323-20260328.md`
- bug 说明：`docs/bug/20260329.md`
- 验收记录：`docs/third-part/齐为/齐为接入与认证整改验收记录_20260326.md`
- 修改说明：`docs/CODE_MODIFY_20260323.md`

## 6. 可直接复用的提交说明

### 建议标题

```text
feat: close auth-first delivery docs and integration prep
```

### 建议正文

```text
- sync cross-domain auth tasks with actual repo status
- refresh auth-first baseline contract and delivery notes
- summarize current change boundary across idempotency, auth, qw integration, security, and naming cleanup
- add QW pre-integration checklist and internal/external dependency split
```
