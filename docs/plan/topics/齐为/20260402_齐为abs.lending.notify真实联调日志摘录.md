# 2026-04-02 齐为 `abs.lending.notify` 真实联调日志摘录

## 1. 联调前提

根据齐为补充的正确联调步骤：

1. 先调用 `abs.push.order` 完成会员同步与开卡
2. 齐为发起第一次扣款
3. **如果第一次扣款失败**，则在**放款成功后**由我方调用 `abs.lending.notify`

因此本次真实联调选用的不是 mock 协议号开卡成功订单，而是此前真实出现过：

- `515 会员卡未生效`

的那一笔订单，对应：

- `uniqueId=debug-user-20260401-002`
- `partnerOrderNo=debug-ord-20260401-002`

本次请求使用：

- `status=1`，表示放款成功

## 2. 真实请求日志

日志源文件：

- `/tmp/qw_real_lending_notify_20260402.log`

关键日志摘录如下：

```text
abs.lending.notify_BEGIN ts=2026-04-02T16:39:12.688642+08:00 traceId=trace-qw-real-20260402-lending-001 endpoint=https://t-api.test.qweimobile.com/api/abs/method partnerNo=abs partnerOrderNo=debug-ord-20260401-002 uniqueId=debug-user-20260401-002 status=1
abs.lending.notify_REQUEST_HEAD timestamp=1775119152636 sign=957be2512e0b4588d91c0902e554f8bf
abs.lending.notify_REQUEST_BODY {"requestHead":{"partnerNo":"abs","timestamp":1775119152636,"method":"abs.lending.notify","version":"v1.0","sign":"957be2512e0b4588d91c0902e554f8bf"},"requestBody":"8f9eb018e8aa51fb551a5cdfdd3e903f9b25ac76b967614346f0068839f1ce0d796a1cc3be7bc2ed156a56f2dd06a4ec662b8a998af2402be3dffe7212d8cbf47ca6bf6c4003432a094b4acd5b9fbbb126f6ab55c245b7f40a0d16751d4494df"}
abs.lending.notify_RESULT httpStatus=200
abs.lending.notify_RESPONSE_RAW {"code":200,"msg":"OK","data":"8c82f7602d98cbb7a6b8a9beaacffadd5cebf0a221a82696ec7435de571c1b7f"}
QW_LENDING_NOTIFY_RESPONSE_DECRYPTED {"orderNo":"94260401229898"}
```

## 3. 结果判断

本轮真实联调结果可以确认：

1. `abs.lending.notify` 已真实打到齐为测试环境
2. 齐为返回 `HTTP 200`
3. 业务返回：
   - `code=200`
   - `msg=OK`
4. `data` 解密后得到：
   - `{"orderNo":"94260401229898"}`

## 4. 阶段性结论

截至 `2026-04-02`，齐为主动接口三条线当前状态为：

1. `abs.push.order`：已真实联调
2. `abs.token.get`：已真实联调
3. `abs.lending.notify`：已真实联调

即：

> 齐为侧 **3 个主动调用接口**，当前都已经完成至少一轮真实联调验证。
