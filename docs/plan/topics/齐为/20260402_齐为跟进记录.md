# 2026-04-02 齐为跟进记录

## 1. 文档目的

本文档用于记录 `2026-04-02` 当天已发给齐为的确认问题、待回复项、收到回复后的处理动作，方便后续持续回复和逐项 check。

## 2. 当前状态

### 2.1 已完成动作

今天已完成以下动作：

1. 已基于真实联调结果整理问题清单
2. 已将企业微信短版消息发给齐为
3. 当前进入 **等待齐为回复** 状态

相关参考文档：

1. `docs/plan/20260402_发齐为确认问题清单.md`
2. `docs/plan/20260402_齐为abs.token.get证据链结论.md`
3. `docs/plan/20260402_齐为abs.lending.notify可验证性结论.md`

### 2.2 当前统一口径

当前对齐为的统一口径应固定为：

1. `abs.push.order` 已真实成功
2. `abs.token.get` 昨晚与今天上午重试均返回 `515 会员卡未生效`
3. 当前待确认重点为：
   - 卡生效时序
   - `openFlag=0` 含义
   - `abs.token.get` 是否必须使用 `orderNo / cardNo`
   - `abs.lending.notify` 成功 / 失败样例与 `status` 字段说明

## 3. 已发送内容摘要

本次已发给齐为的核心问题如下：

### 3.1 `abs.token.get`

- [ ] `abs.push.order` 成功后，会员卡通常多久生效
- [ ] 是否存在固定等待窗口
- [ ] `openFlag=0` 的具体含义
- [ ] `abs.token.get` 是否允许只传 `uniqueId + partnerOrderNo`
- [ ] 是否必须改用 `orderNo`
- [ ] 是否必须改用 `cardNo`
- [ ] 若需要调整，请明确最小必填字段组合

### 3.2 `abs.lending.notify`

- [ ] 成功样例
- [ ] 失败样例
- [ ] `status` 字段取值含义
- [ ] 是否还需要 `orderNo / cardNo` 或其他额外字段

## 4. 回复记录区

后续收到齐为回复时，按以下模板补充，避免消息记录散落在聊天里。

### 4.1 第一轮回复

**回复时间**

- `2026-04-02`（具体聊天时间待补充）

**回复原文摘要**

1. 会员同步后开卡即成功
2. `515 会员卡未生效` 不是等待时间问题，而是扣款失败
3. 扣款失败原因是：`payProtocolNo`（支付协议号）错误
4. `openFlag=0` 表示开卡成功
5. `abs.token.get` 只传 `uniqueId + partnerOrderNo` 可以
6. `abs.token.get` 不需要额外传 `orderNo / cardNo`
7. `abs.lending.notify` 的 `status` 口径为：`0=失败，1=成功`

**落地结论**

1. `abs.token.get` 当前阻塞点已从“时序 / 参数待确认”进一步收敛为：**前置扣款失败导致卡未生效**
2. `openFlag=0` 的含义已经确认，不再是待确认项
3. `abs.token.get` 当前参数口径没有问题，继续使用 `uniqueId + partnerOrderNo` 即可
4. 当前最需要排查的已不是 `orderNo / cardNo`，而是 **我方传给 `abs.push.order` 的 `payProtocolNo` 为什么错误**
5. `abs.lending.notify` 的 `status` 字段口径已确认，但成功 / 失败样例仍未拿到

**对应动作**

1. 回填 `abs.token.get` 结论文档
2. 回填齐为问题分析与部署说明
3. 回查当前项目中 `payProtocolNo` 的来源与赋值逻辑
4. 后续优先确认“真实支付协议号”应该来自哪里
5. 继续向齐为补要 `abs.lending.notify` 成功 / 失败样例

### 4.2 第二轮回复：`payProtocolNo` 来源确认

**回复时间**

- `2026-04-02`（具体聊天时间待补充）

**回复原文摘要**

1. 真实支付协议号来自艾博生与第三方支付公司 **通联** 签约后获取的协议号
2. 一个用户一个协议号
3. 当前项目还没有和通联完成真实对接
4. 齐为下午会先提供一个 **mock 协议号**，方便继续联调
5. 后续需要在 `third-part` 部分补充 **通联服务对接流程**，以便真实获取支付协议号

**落地结论**

1. 当前项目内不存在真实可用的 `payProtocolNo` 来源
2. `agreementNo` 和 `proto-` 前缀值都不能继续视为真实支付协议号
3. 下午如果拿到 mock 协议号，可以先用于齐为联调验证
4. 长期方案必须补通联服务对接，才能真正获取用户级支付协议号

**对应动作**

1. 等待齐为提供 mock 协议号
2. 拿到后设计最小联调改造方案
3. 后续在 `docs/third-part` 下增加通联服务对接流程文档

### 4.3 第三轮回复：mock 协议号与 `abs.lending.notify` 样例

**回复时间**

- `2026-04-02`（具体聊天时间待补充）

**回复原文摘要**

1. 齐为已提供 mock 协议号：
   - `AIP211926033187CF73483`
2. `abs.lending.notify` 成功返回：
   - 原始响应：`{code:200,msg:"OK",data:"8c82f7602d98cbb7a6b8a9beaacffadd5cebf0a221a82696ec7435de571c1b7f"}`
   - `data` 解密后：`{"orderNo":"94260401229898"}`
3. `abs.lending.notify` 失败返回：
   - `{code:510,msg:"合作方订单不存在",data:null}`

**落地结论**

1. 齐为联调继续推进所需的 mock 协议号已经具备
2. `abs.lending.notify` 的成功 / 失败样例已经具备
3. 当前齐为这条线，已从“等待关键外部输入”推进到“可以继续真实联调验证”

**对应动作**

1. 使用 mock 协议号设计最小联调改造方案
2. 准备重新真实联调 `abs.push.order` + `abs.token.get`
3. 若条件允许，补做 `abs.lending.notify` 首轮真实验证

### 4.4 第四轮处理结果：mock 协议号真实联调通过

**处理时间**

- `2026-04-02`

**处理摘要**

1. 代码已新增测试环境 override 配置：
   - `QW_MEMBER_SYNC_PAY_PROTOCOL_NO_OVERRIDE`
2. 当前 `abs.push.order` 在配置该值时，会优先透传 mock 协议号：
   - `AIP211926033187CF73483`
3. 真实联调日志保存在：
   - `/tmp/qw_real_push_and_token_20260401.log`
4. 成功日志摘录已固化到：
   - `docs/plan/20260402_齐为mock协议号真实联调日志摘录.md`
5. 本轮实际现象：
   - 首次发送 `abs.push.order` 时，出现一次传输层 `EOF reached while reading`
   - 随后重试同一单据，`abs.push.order` 返回 `530 合作方订单号已经存在`
   - 继续调用 `abs.token.get`，返回 `200 / OK`
   - `data` 解密后拿到：
     - `token=9b0848b8-17b0-4fda-a713-86f98d9707c0`
     - 可用 `redirectUrl`

**落地结论**

1. mock 协议号已经验证齐为会员卡可进入生效状态
2. `515 会员卡未生效` 的问题已被实际消除
3. `openFlag=0`、`uniqueId + partnerOrderNo` 的原参数口径继续成立
4. 线上剩余核心事项已收敛为：
   - 正式接入通联，获取真实 `payProtocolNo`
   - 继续补做 `abs.lending.notify` 首轮真实验证

**对应动作**

1. 回填 bug / 部署 / checklist 文档
2. 将 mock 协议号方案明确标注为“仅联调使用”
3. 后续补通联接入流程文档与 `abs.lending.notify` 真实验证记录

### 4.5 第五轮处理结果：`abs.lending.notify` 真实联调通过

**处理时间**

- `2026-04-02 16:39 +08:00`

**处理摘要**

1. 根据齐为补充流程，`abs.lending.notify` 应使用“首扣失败后、且已放款成功”的订单验证
2. 本轮选用订单：
   - `uniqueId=debug-user-20260401-002`
   - `partnerOrderNo=debug-ord-20260401-002`
   - `status=1`
3. 真实联调日志：
   - `/tmp/qw_real_lending_notify_20260402.log`
4. 固化摘录文档：
   - `docs/plan/20260402_齐为abs.lending.notify真实联调日志摘录.md`

**真实结果**

1. `abs.lending.notify` 返回 `HTTP 200`
2. 原始响应：
   - `{"code":200,"msg":"OK","data":"8c82f7602d98cbb7a6b8a9beaacffadd5cebf0a221a82696ec7435de571c1b7f"}`
3. `data` 解密后：
   - `{"orderNo":"94260401229898"}`

**落地结论**

1. 齐为主动接口三条线当前都已完成至少一轮真实联调：
   - `abs.push.order`
   - `abs.token.get`
   - `abs.lending.notify`
2. `abs.lending.notify` 的成功样例已从“对方提供样例”变成“我方真实调用成功样例”
3. 当前后续主流程改造边界已经明确：
   - 接口接线本身可保留在主流程
   - mock 协议号与 override 只能保留为测试联调配置
   - 正式上线前仍需补通联真实协议号来源

## 5. 收到回复后的处理规则

### 情况 A：确认只需要等待

则勾选并执行：

- [ ] 记录对方给出的等待窗口
- [ ] 按等待窗口重新真实重试 `abs.token.get`
- [ ] 保存新日志
- [ ] 更新 `docs/bug/20260401_齐为504数据协议非法分析.md`
- [ ] 更新 `docs/third-part/齐为/20260401_齐为线上部署说明.md`

### 情况 B：确认必须带 `orderNo / cardNo`

则勾选并执行：

- [ ] 记录最小必填字段组合
- [ ] 设计最小代码改造方案
- [ ] 评估是否需要补库表字段
- [ ] 完成代码修改后再做真实联调
- [ ] 回填 bug / 部署 / plan 文档

### 情况 C：补齐 `abs.lending.notify` 样例

则勾选并执行：

- [ ] 保存成功样例
- [ ] 保存失败样例
- [ ] 确认 `status` 字段取值含义
- [ ] 判断当前请求模型是否足够
- [ ] 若字段齐备，启动真实验证

### 情况 D：确认根因是 `payProtocolNo` 错误

则勾选并执行：

- [x] 记录根因：扣款失败导致卡未生效
- [x] 确认 `openFlag=0` 表示开卡成功
- [x] 确认 `abs.token.get` 继续使用 `uniqueId + partnerOrderNo`
- [x] 确认 `abs.token.get` 不需要 `orderNo / cardNo`
- [ ] 排查当前 `payProtocolNo` 的真实来源
- [ ] 判断 `agreementNo` 是否被误当成支付协议号
- [ ] 确认真正可用的支付协议号来自哪个系统 / 字段
- [ ] 若定位完成，再制定最小修复方案

### 情况 E：确认真实 `payProtocolNo` 来自通联，当前先用 mock 协议号联调

则勾选并执行：

- [x] 确认真实 `payProtocolNo` 来自通联签约协议号
- [x] 确认当前项目尚未对接通联
- [x] 确认短期方案是等待齐为提供 mock 协议号
- [x] 收到 mock 协议号后开始最小改造与真实联调
- [x] 完成 mock 协议号下的真实联调验证
- [ ] 后续补充通联服务对接流程文档

### 情况 F：收到 `abs.lending.notify` 成功 / 失败样例

则勾选并执行：

- [x] 保存成功样例
- [x] 保存失败样例
- [x] 确认成功响应解密后包含 `orderNo`
- [ ] 若条件具备，启动 `abs.lending.notify` 首轮真实验证

## 6. 后续回复建议

如果齐为只回复一部分内容，后续跟进建议按以下顺序继续追：

1. 先用 mock 协议号继续真实联调
2. 再补 `abs.lending.notify` 首轮真实验证
3. 后续再补通联服务对接流程

原因：

1. `abs.token.get` 参数口径、协议号来源、mock 协议号都已明确
2. `abs.lending.notify` 样例现已具备
3. 当前最有价值的是继续把真实联调往前推

## 7. 一句话状态

> 今天已收到齐为第一轮回复，当前齐为问题已收敛为“`payProtocolNo` 错误导致扣款失败，从而会员卡未生效”。
