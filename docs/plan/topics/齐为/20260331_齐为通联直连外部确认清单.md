# 2026-03-31 齐为通联直连外部确认清单

## 1. 目的

本文档用于基于当前《齐为联调方案设计文档（通联证书直连版）》整理一份可直接发给齐为 / 通联的外部确认清单。

目标是尽快确认：

1. 本轮联调是否以通联 `ProcessServlet` 直连方式为准。
2. 三个现有业务动作如何映射到通联直连接口。
3. 证书、签名、验签、报文格式、测试样例等关键协议细节。

## 2. 当前我方已知信息

### 2.1 已拿到的环境信息

| 类别 | 当前信息 | 来源 |
| --- | --- | --- |
| 测试地址 | `https://tlt-test.allinpay.com/aipg/ProcessServlet` | `证书信息.txt` |
| 商户号 | `200000000007804` | `证书信息.txt` |
| 用户名 | `20000000000780404` | `证书信息.txt` |
| 用户密码 | `111111` | `证书信息.txt` |
| P12 密码 | `111111` | `证书信息.txt` |
| 支付账户 | `200000000007804` | `艾博生-齐为测试环境.txt` |
| 渠道编码 | `allinpay-1` | `艾博生-齐为测试环境.txt` |

### 2.2 我方当前业务入口

| 业务动作 | 当前本地入口 | 说明 |
| --- | --- | --- |
| 会员 / 订单同步 | `BenefitOrderServiceImpl.createOrder()` | 下单后同步会员、产品、支付协议等信息 |
| 获取联登链接 | `BenefitOrderServiceImpl.getExerciseUrl()` | 获取用户跳转所需链接 |
| 放款结果通知 | `NotificationServiceImpl.handleGrant()` | 放款处理成功后通知下游 |

## 3. 需齐为 / 通联确认的核心事项

### 3.1 联调链路边界确认

| 确认项 | 当前判断 | 需对方确认内容 | 不确认的影响 |
| --- | --- | --- | --- |
| 本轮联调是否以通联直连为准 | 我方按 `ProcessServlet` 直连方案设计 | 请明确本轮是否不再走 `qweimobile` 聚合网关，而是统一走通联直连 | 若边界不清，开发会走错协议实现 |
| 三个业务动作是否继续保留 | 我方保留现有业务入口不变 | 请确认“下单同步 / 联登链接 / 放款通知”三类动作在直连模式下仍然成立 | 若业务动作定义变化，上层服务编排需调整 |

### 3.2 接口映射确认

| 我方业务动作 | 当前抽象接口 | 需对方确认内容 | 期望对方提供 |
| --- | --- | --- | --- |
| 下单后同步会员与订单 | `QwBenefitClient.syncMemberOrder()` | 对应哪个通联直连接口 / serviceCode | 接口名称、serviceCode、请求样例、响应样例 |
| 获取联登链接 | `QwBenefitClient.getExerciseUrl()` | 对应哪个通联直连接口 / serviceCode | 接口名称、serviceCode、请求样例、响应样例 |
| 放款结果通知 | `QwBenefitClient.notifyLending()` | 对应哪个通联直连接口 / serviceCode | 接口名称、serviceCode、请求样例、响应样例 |

### 3.3 报文协议确认

| 确认项 | 需对方确认内容 | 期望对方提供 |
| --- | --- | --- |
| 请求格式 | 是 XML、表单报文，还是其他固定协议格式 | 完整请求报文模板 |
| 响应格式 | 返回报文结构、成功码、失败码、业务码定义 | 完整响应报文模板 |
| 公共字段 | 商户号、用户名、版本号、时间戳、签名字段等的固定位置和命名 | 字段说明表 |
| 字符编码 | UTF-8、GBK 或其他要求 | 编码要求说明 |
| 时间格式 | 时间戳或日期字段的具体格式 | 格式说明与示例 |

### 3.4 签名 / 验签 / 证书确认

| 确认项 | 当前情况 | 需对方确认内容 | 不确认的影响 |
| --- | --- | --- | --- |
| `user-rsa.p12` 用途 | 我方已拿到证书包和密码 | 请确认该证书用于报文签名、客户端证书认证，还是两者同时使用 | 无法确定 HTTP 客户端和签名实现方式 |
| `public-rsa.cer` 用途 | 我方可正常解析，证书有效期为 `2024-05-06` 至 `2034-05-04` | 请确认是否为当前有效响应验签证书 | 验签可能使用错误证书 |
| `allinpay-pds.cer` 用途 | 我方解析到该证书有效期为 `2012-07-24` 至 `2014-07-24` | 请确认该文件是否仍有用途，是否应废弃 | 若误用历史证书，会导致联调误判 |
| 签名规则 | 当前未知 | 请提供签名串拼接顺序、摘要算法、签名字段名称 | 无法实现正确签名 |
| 验签规则 | 当前未知 | 请提供验签字段范围、算法、验签失败判定规则 | 无法判断响应是否可信 |
| 是否需要双向 TLS | 当前未知 | 请确认是否必须使用客户端证书建立 HTTPS 连接 | 无法确定 HTTP 客户端装载方式 |

### 3.5 账号与权限确认

| 确认项 | 当前信息 | 需对方确认内容 |
| --- | --- | --- |
| 商户号可用性 | `200000000007804` | 请确认该商户号当前可用于本轮联调 |
| 用户名可用性 | `20000000000780404` | 请确认该用户名与商户号匹配且可正常调用接口 |
| 用户密码可用性 | `111111` | 请确认当前未失效 |
| 支付账户 / 业务账户 | `200000000007804` | 请确认该账户在本轮业务链路中的具体字段位置与用途 |
| 渠道编码 | `allinpay-1` | 请确认是否需要在请求报文中显式传递 |

### 3.6 样例数据确认

| 场景 | 需对方提供内容 | 目的 |
| --- | --- | --- |
| 下单成功 | 完整请求样例 + 完整响应样例 | 验证建单主链 |
| 下单失败 | 失败码样例 + 失败原因说明 | 验证错误分类 |
| 联登成功 | 完整请求样例 + 返回链接或 token 样例 | 验证跳转链路 |
| 联登失败 | 失败码样例 | 验证异常分支 |
| 放款通知成功 | 完整请求样例 + 成功响应样例 | 验证放款通知链路 |
| 放款通知失败 | 完整失败样例 | 验证失败处理与审计 |

## 4. 建议发给齐为 / 通联的最小问题清单

以下问题可以直接对外同步：

1. 请确认本轮联调是否统一以 `https://tlt-test.allinpay.com/aipg/ProcessServlet` 为测试地址，不再走 `qweimobile` 聚合网关。
2. 请确认以下三个业务动作在通联直连模式下分别对应哪个接口或 `serviceCode`：
   - 下单后同步会员与订单
   - 获取联登链接
   - 放款结果通知
3. 请提供上述三个接口的完整请求 / 响应报文样例，以及成功码、失败码说明。
4. 请确认 `user-rsa.p12` 的具体用途：仅用于报文签名、仅用于客户端证书认证，还是两者同时使用。
5. 请确认 `public-rsa.cer` 是否为当前有效的响应验签证书。
6. 请确认 `allinpay-pds.cer` 是否仍需使用；若不使用，请明确作废。
7. 请提供请求签名规则、响应验签规则、签名字段顺序和算法要求。
8. 请确认是否需要双向 TLS，以及若需要，客户端证书装载要求是什么。
9. 请确认商户号 `200000000007804`、用户名 `20000000000780404`、密码 `111111` 当前是否仍可用于联调。
10. 请提供至少一套完整的成功样例和一套失败样例，覆盖建单、联登、放款通知三个场景。

## 5. 我方建议的对齐结论格式

为了减少反复，建议齐为 / 通联按以下格式反馈：

| 项目 | 反馈内容 |
| --- | --- |
| 接口名称 / serviceCode | 明确填写 |
| 请求报文模板 | 附完整样例 |
| 响应报文模板 | 附完整样例 |
| 签名算法 | 明确填写 |
| 验签证书 | 明确是哪个文件 |
| 双向 TLS 要求 | 是 / 否 |
| 账号是否可用 | 是 / 否 |
| 备注 | 需补充说明的限制条件 |

## 6. 当前结论

当前我方已经具备：

1. 现有业务入口与业务编排
2. 通联测试地址、账号信息、证书材料
3. 基于通联直连改造的技术方案设计

当前仍阻塞进入开发和真实联调的关键点不是代码结构，而是：

**齐为 / 通联尚未明确给出通联直连接口定义、报文规范、签名验签规则，以及当前有效证书用途说明。**

因此，建议先以本清单完成外部对齐，再进入通联直连 client 的正式开发与联调。
