# 2026-04-02 发齐为确认问题清单

## 1. 使用说明

本文档用于 `2026-04-02` 当天向齐为联调同学同步当前真实联调结果，并一次性把最关键待确认问题问清楚，避免来回反复沟通。

建议发送原则：

1. 先给事实，再提问题
2. 只问会直接影响今天推进的问题
3. 每个问题都尽量让对方能给出“明确字段 / 明确样例 / 明确等待时间”

## 2. 可直接转发的消息版本

建议直接发送如下内容：

> 你好，我们这边已基于你们测试环境完成一轮真实联调，想和你们确认几个关键口径，方便今天继续收口。  
>  
> 当前事实如下：  
> 1. 我方已成功调用测试地址 `https://t-api.test.qweimobile.com/api/abs/method`  
> 2. `abs.push.order` 已真实成功，并返回：  
>    - `orderNo=94260401229898`  
>    - `cardNo=98260401557343`  
>    - `openFlag=0`  
> 3. 随后我们用同一组 `uniqueId + partnerOrderNo` 调用 `abs.token.get`，返回：  
>    - `code=515`  
>    - `msg=会员卡未生效`  
> 4. 我们已在 `2026-04-02 10:44:02 +08:00` 再次重试，结果仍然是 `515 会员卡未生效`  
>  
> 想请你们协助确认以下问题：  
>  
> **一、关于 `abs.token.get`**  
> 1. `abs.push.order` 成功后，会员卡通常需要多久生效？是否存在固定等待窗口？  
> 2. `openFlag=0` 在你们这边具体代表什么状态？是否表示“已开卡但未生效”？  
> 3. `abs.token.get` 是否允许只传 `uniqueId + partnerOrderNo`？  
> 4. `abs.token.get` 是否必须改用你们侧的 `orderNo` 或 `cardNo`？如果需要，请明确最小必填字段组合。  
>  
> **二、关于 `abs.lending.notify`**  
> 5. 请提供 `abs.lending.notify` 的成功样例  
> 6. 请提供 `abs.lending.notify` 的失败样例  
> 7. `status` 字段的取值含义是否就是 `1=成功，0=失败`？  
> 8. `abs.lending.notify` 是否还需要额外字段，比如 `orderNo`、`cardNo` 或其他字段？  
>  
> 如果方便的话，请尽量直接给：  
> - 字段说明  
> - 一条成功请求/响应样例  
> - 一条失败请求/响应样例  
>  
> 我们这边拿到口径后，今天就能继续验证并同步结果。

## 3. 极简版消息

如果对方时间很紧，也可以直接发简版：

> 我方 `abs.push.order` 已成功，但 `abs.token.get` 跨到今天上午重试仍返回 `515 会员卡未生效`。  
> 请帮忙确认：  
> 1. 卡生效是否有等待窗口  
> 2. `openFlag=0` 的具体含义  
> 3. `abs.token.get` 是否必须用 `orderNo / cardNo`  
> 4. `abs.lending.notify` 的成功/失败样例和 `status` 字段说明

## 4. 这份清单的目标

今天发这份清单，不是为了泛泛催办，而是为了尽快拿到两类决定性信息：

1. `abs.token.get` 当前卡住是 **时序问题** 还是 **参数口径问题**
2. `abs.lending.notify` 今天是否具备首轮真实验证条件

## 5. 对方回复后的处理建议

### 情况 A：确认只需要等待

则今天继续做：

1. 按对方给出的等待窗口再重试
2. 保留日志
3. 更新 `bug / thirdpart / plan`

### 情况 B：确认必须带 `orderNo / cardNo`

则今天继续做：

1. 设计最小字段改造方案
2. 补齐落库与读取逻辑
3. 再做真实联调

### 情况 C：补齐 `abs.lending.notify` 样例

则今天继续做：

1. 构造真实请求
2. 留存原始响应
3. 更新部署说明和问题清单
