# 2026-04-01 M2 外部确认与 M3.1 推进跟踪

## 1. 文档目的

本文档用于承接 `docs/plan/20260401.md` 的执行动作，把今天需要推动的外部确认项、`M3.1` 启动条件、阻塞影响和下一动作固定下来，避免当天结束后仍停留在“已催办但没有状态口径”的模糊状态。

说明：

1. 本文档只覆盖 **齐为联调主线**。
2. **云卡完整页面交互、借款确认、还款页编排** 不纳入本文件跟踪范围。
3. 当前状态判断基于仓库内截至 `2026-04-01` 已存在的计划、清单和联调说明；若当天收到新反馈，应直接在本文件更新状态。
4. 本文件已结合 `2026-04-01` 当天齐为真实联调结果做过一次状态回填。

## 2. 外部确认项状态总览

| 序号 | 待确认项 | 来源方 | 影响里程碑 | 当前状态 | 当前依据 | 下一动作 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 齐为测试环境 `partnerNo / signKey / aesKey` | 齐为 | `M2`、`M3.1` | 已确认（测试环境） | 已基于 `docs/third-part/齐为/艾博生-齐为测试环境.txt` 和当日真实联调跑通 `abs.push.order` | 保留当前测试口径；后续上线前再确认生产参数 |
| 2 | 齐为测试网关最终地址与版本口径 | 齐为 | `M2`、`M3.1` | 已确认（测试环境） | 已按 `https://t-api.test.qweimobile.com/api/abs/method`、`v1.0` 完成真实联调 | 后续上线前再确认生产网关、`methodPath` 与版本 |
| 3 | 科技平台 `/api/users/me` 真实测试地址 | 科技平台 | `M2`、`M4` | 待确认 | 待确认清单中仍要求补真实 `baseUrl` | 向科技平台索取真实测试地址 |
| 4 | 科技平台 Bearer Token 样例 | 科技平台 | `M2`、`M4` | 待确认 | 现有文档仍要求提供 token 样例或获取方式 | 向科技平台索取 token 样例或获取口径 |
| 5 | 科技平台真实返回字段结构 | 科技平台 | `M2`、`M4` | 待确认 | 当前仅有预期字段口径，未见真实返回样例 | 向科技平台索取 `/api/users/me` 实际响应样例 |
| 6 | 扣款回调区分首扣 / 兜底代扣的规则 | 齐为 + 我方业务 | `M2`、`M3.2` | 待确认 | 当前仅明确该项必须单独确认，未见判定规则落地 | 单独催确认字段定义、判定规则与样例报文 |
| 7 | 公网测试回调域名 | 我方网关 / 运维 | `M2`、`M3.2` | 待确认 | 现有文档仅列出需确认测试域名 | 向我方网关 / 运维确认可暴露的测试域名 |
| 8 | 回调路径暴露方式 | 我方网关 / 运维 | `M2`、`M3.2` | 待确认 | 文档已列出候选回调路径，未见暴露方式确认 | 确认网关路径、转发方式和可达性 |
| 9 | 白名单策略 | 我方网关 / 运维 | `M2`、`M3.2`、`M5` | 待确认 | 当前仍仅停留在“是否需白名单”的确认项 | 确认来源 IP / 域名白名单是否必需以及配置方式 |
| 10 | 3 个出站接口与关键回调样例 | 齐为 | `M2`、`M3.1`、`M3.2` | 部分确认 | `abs.push.order`、`abs.token.get` 已有真实调用样例；`abs.lending.notify` 与回调样例仍未补齐 | 继续补 `abs.lending.notify` 的成功 / 失败样例，并索取关键回调样例 |

## 3. 按责任方拆分的催办视图

### 3.1 齐为

**今日需要推动的确认项**

1. `abs.token.get` 返回 `515 会员卡未生效` 的生效时序与额外前置条件
2. `abs.lending.notify` 成功 / 失败样例
3. 扣款通知区分首扣 / 兜底代扣的字段与判定规则
4. 行权、退款、还款回调的最小样例报文
5. 生产环境正式参数与测试环境差异

**若未确认的直接影响**

1. `M3.1` 虽已进入真实 `HTTP` 联调，但无法完全收口 `abs.token.get` 与 `abs.lending.notify`
2. `M3.2` 仍无法稳定设计入站回调联调路径
3. 当天即使继续本地开发，也无法替代齐为对业务时序和回调口径的确认

### 3.2 科技平台

**今日需要推动的确认项**

1. 测试环境 `baseUrl`
2. `/api/users/me` 真实地址
3. Bearer Token 获取方式或可直接使用的 token 样例
4. 实际返回字段结构与样例

**若未确认的直接影响**

1. `M2` 不能被判定为外部条件确认完成
2. SSO 真实链路无法闭环
3. `M4` 验收阶段的真实登录态与用户信息链路仍无法完成闭环验证

### 3.3 我方网关 / 运维

**今日需要推动的确认项**

1. 公网测试回调域名
2. 回调路径的对外暴露方式
3. 白名单策略

**若未确认的直接影响**

1. `M3.2` 无法排期启动
2. 齐为无法回调我方测试环境
3. 入站回调联调证据无法形成

## 4. `M3.1` 启动条件核对

### 4.0 本地已具备项复核

从当前代码实现看，`M3.1` 所需的本地入口和齐为调用主链已经落位：

1. `BenefitOrderServiceImpl.createOrder()` 已承接建单主链。
2. `BenefitOrderServiceImpl.getExerciseUrl()` 已承接联登链接获取主链。
3. `NotificationServiceImpl.handleGrant()` 已承接放款通知处理主链。
4. `QwBenefitClientImpl` 已固定 `abs.push.order`、`abs.token.get`、`abs.lending.notify` 三个 method 调用。

同时也需要注意：

1. `QwProperties` 中当前仍保留 `partnerNo`、`signKey`、`aesKeyBase64`、`methodPath` 的默认占位值。
2. 因此即使代码入口已就绪，真实 `HTTP` 联调仍必须先拿到外部正式测试参数，并通过运行环境配置覆盖默认值。

### 4.1 `abs.push.order`

**启动前必须齐备**

1. 齐为真实参数已确认
2. 齐为最终测试网关已确认
3. 联调样例与最小请求字段口径已确认
4. 我方日志可留存 `traceId + bizOrderNo`

**当前判断**

1. 测试环境参数、测试网关和最小请求口径已经通过真实联调验证。
2. 截至当前，`abs.push.order` 已满足启动条件并已真实联调成功。

### 4.2 `abs.token.get`

**启动前必须齐备**

1. 已有有效 `benefitOrderNo`
2. 上一步齐为订单与会员上下文已打通
3. 齐为真实联登返回字段口径已确认

**当前判断**

1. `abs.push.order` 已真实成功，前置上下文已验证可建立。
2. `abs.token.get` 已完成真实调用，但当前被齐为业务层以 `515 会员卡未生效` 拒绝。
3. 因此当前问题已不再是“无法启动”，而是“业务生效时序待确认”。

### 4.3 `abs.lending.notify`

**启动前必须齐备**

1. 已存在有效订单
2. 齐为放款通知接口口径已确认
3. 出站请求与回参样例已确认

**当前判断**

1. 当前代码入口已经具备，协议口径也已完成第一轮真实验证。
2. 但本轮尚未完成 `abs.lending.notify` 的真实成功 / 失败样例联调。
3. 因此它仍属于 `M3.1` 的剩余待收口事项。

## 5. 当日 `M2` 状态判断

截至 `2026-04-01` 当前仓库内可见信息，建议统一使用以下判断口径：

1. `M2` **整体尚未完成**。
2. 当前更准确的表述应为：**齐为出站测试环境已基本打通，但整体外部条件仍未全部确认完成**。
3. 齐为侧已不再是“完全无法启动”的状态，但科技平台真实地址 / token、回调映射规则和公网回调地址仍未完全确认。

## 6. 阻塞影响与时间线判断

### 6.1 当前阻塞影响

| 阻塞项 | 影响里程碑 | 影响说明 |
| --- | --- | --- |
| 齐为 `abs.token.get` 生效时序未确认 | `M3.1` | `abs.push.order` 已成功，但联登链路仍卡在 `515 会员卡未生效` |
| 齐为 `abs.lending.notify` 样例未确认 | `M3.1` | 出站三接口尚未全部收口 |
| 科技平台真实地址与 token 未确认 | `M2`、`M4` | SSO 真联调和真实验收闭环受阻 |
| 扣款回调映射规则未确认 | `M2`、`M3.2` | 入站联调无法稳定排期 |
| 公网测试回调域名与白名单未确认 | `M2`、`M3.2`、`M5` | 回调链路无法建立，后续上线准备也受影响 |

### 6.2 当前时间线口径

1. **最乐观**：齐为当天补齐 `abs.token.get` 时序说明和 `abs.lending.notify` 样例，则 `M3.1` 可继续快速收口。
2. **基准**：齐为协议层已通，但仍需 1 个工作日左右继续补业务时序与剩余接口样例；同时科技平台与网关确认项继续并行推进。
3. **延迟**：若齐为业务时序、科技平台真实地址 / token、回调公网条件持续不到位，则 `M3.1`、`M3.2`、`M4` 继续整体顺延。

## 7. 今日建议输出

若当天收到新的关键外部确认或真实联调结果，建议至少输出以下结果：

1. 一份“已确认项 / 仍阻塞项 / 下一动作”更新结果
2. 一份按责任方拆分的催办视图
3. 一份 `M2` 当前状态与交付时间线判断
4. 一份联调环境切换与配置手册
5. 一份可本地复制的环境变量样板
6. 一份按接口沉淀的真实联调证据与问题清单

## 8. 已补充的本地落地材料

截至当前，仓库内已补充以下可直接使用的材料：

1. `docs/plan/20260401_对外同步口径.md`
2. `docs/plan/20260401_M3.1联调记录模板.md`
3. `docs/plan/20260401_M3.1环境切换与配置手册.md`
4. `docs/plan/20260401_联调环境变量样板.sh`
5. `docs/bug/20260401_齐为504数据协议非法分析.md`
6. `docs/third-part/齐为/20260401_齐为线上部署说明.md`
