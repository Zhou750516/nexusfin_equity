# 2026-04-26 工作计划

> 今日主线：承接 `2026-04-24` 差异分析、任务拆解与 `2026-04-25` 周报收口结果，把“下阶段先做什么、哪些先不做、哪些依赖外部确认”进一步落成可执行计划，避免继续停留在总结层。

## 1. 今日目标

基于当前已确认结论：

1. 本地第一版主链并非空白，而是需要按小花 `4.22` 与新借款链路重新对齐；
2. `specs/004-benefit-loan-flow/tasks.md` 未完成任务已经不适合作为当前直接执行主清单；
3. 当前项目主要阻塞集中在云卡状态机、小花 `4.22` 细节、齐为正式文档、法大大资料与商务审核。

因此 `2026-04-26` 的重点是：

1. 继续细化“用信新链路改造方案”
2. 继续细化“云卡状态查询与 H5 状态映射方案”
3. 继续细化“H5 自动埋点方案”
4. 明确 Shared Foundation 第一批可落地项
5. 把外部待确认项与本侧可执行项进一步拆开

### 当前已产出

1. `docs/plan/20260426_用信新链路改造方案.md`
2. `docs/plan/20260426_云卡状态查询与H5状态映射方案.md`
3. `docs/plan/20260426_H5自动埋点方案.md`
4. `docs/plan/20260426_Shared Foundation第一批落地项.md`
5. `docs/plan/20260426_外部待确认项与本侧可执行项拆分.md`

## 2. 今日优先级

### P0：细化用信新链路改造方案

**目标**

- 把“云卡先建放款订单、权益完成后再放款”的主链拆成更清晰的后端责任边界和状态节点。

**计划动作**

1. 基于 `docs/plan/20260423_小花云卡齐为联动会议纪要与行动计划.md` 再次整理新旧链路差异；
2. 细化以下关键节点：
   - 创建云卡放款订单
   - 权益购买与短信签约
   - loading 承接
   - 触发放款
   - 状态查询
   - 失败 / 超时 / 放弃权益 / 关单分支
3. 明确本地最少需要维护的核心标识与状态；
4. 明确哪些节点必须等待云卡接口定版。

### P0：细化云卡状态查询与 H5 状态映射方案

**目标**

- 避免审批中、审批结果、拒绝落地和分发场景继续混用“云卡状态”和“小花结果”。

**计划动作**

1. 整理建议状态枚举；
2. 建立状态到页面阶段的映射表；
3. 明确 `approval-status`、`approval-result`、分发上下文接口需要补充的字段；
4. 区分本地维护字段、云卡返回字段、小花最终结果字段。

### P0：细化 H5 自动埋点方案

**目标**

- 在不直接启动埋点代码改造的前提下，先把页面、事件、参数与隐私边界讲清楚。

**计划动作**

1. 梳理关键页面与关键事件；
2. 明确事件命名、公参、业务主键；
3. 标注脱敏要求与隐私边界；
4. 明确失败重试、降级与埋点缺失的处理原则。

### P1：明确 Shared Foundation 第一批落地项

**目标**

- 把“共享基础收口”从概念层推进到可以开始执行的第一批任务。

**计划动作**

1. 从 `docs/plan/20260424_小花4.22开发任务拆解.md` 中抽出第一批最小落地项：
   - Yunka 路径配置补齐
   - typed DTO 范围定义
   - `XiaohuaGatewayService` facade 入口设计
   - 放款 / 还款回调 DTO 基础模型
2. 明确哪些可以立即开工，哪些依赖外部字段确认；
3. 形成一版“先做什么、后做什么”的清单。

### P1：继续控制边界

**目标**

- 保持本轮工作停留在“方案 + 任务入口 + 依赖澄清”层，不误入高返工实现。

**计划动作**

1. 不直接大改 `/api/loan/apply` 主链逻辑；
2. 不抢跑云卡状态机真实接口开发；
3. 不抢跑法大大真实签章接入；
4. 不直接启动 H5 埋点全量代码改造。

## 3. 今日完成标准

如果 `2026-04-26` 结束前达到以下结果，可认为当天达标：

1. 用信新链路改造方案较前一版进一步细化；
2. 云卡状态查询与 H5 状态映射方案形成更明确的状态表；
3. H5 自动埋点方案完成事件与字段层梳理；
4. Shared Foundation 第一批落地项已经明确；
5. 外部依赖项与本侧可执行项拆分更清晰；
6. 今天仍未误启动高返工主链改造。

## 4. 风险提示

1. 若三类方案继续停留在“只知道要做、但未细化”的状态，下周实现仍容易失焦；
2. 若 Shared Foundation 第一批落地项不先明确，后续权益 / 借款 / 还款对齐会继续各自为战；
3. 若外部待确认项不和本侧可执行项拆开，团队容易在等待中失去推进节奏；
4. 商务合同审核、云卡状态机、小花 `4.22` 细节、齐为与法大大资料仍是当前主阻塞。

## 5. 一句话判断

> `2026-04-26` 的关键不是新增更多总结，而是把本周形成的结论进一步压缩成“可执行的下一步”，为下周 Shared Foundation 与主链路对齐工作建立真正的落地入口。

## 6. 当天执行结果

截至 `2026-04-26` 收口时，今天计划已实际完成：

1. 已产出 5 份方案 / 边界文档：
   - `docs/plan/20260426_用信新链路改造方案.md`
   - `docs/plan/20260426_云卡状态查询与H5状态映射方案.md`
   - `docs/plan/20260426_H5自动埋点方案.md`
   - `docs/plan/20260426_Shared Foundation第一批落地项.md`
   - `docs/plan/20260426_外部待确认项与本侧可执行项拆分.md`
2. 已核对 `2026-04-25` 开发完成度，确认昨日没有新增开发实现任务。
3. 已对照当前工作区改动确认 Shared Foundation 与权益 / 借款 / 还款第一批对齐实现已基本落地。
4. 已同步实现契约与验证文档：
   - `specs/004-benefit-loan-flow/contracts/page-flow-api.md`
   - `specs/004-benefit-loan-flow/contracts/tech-platform-outbound.md`
   - `specs/004-benefit-loan-flow/quickstart.md`
5. 已完成完整验证：
   - `mvn -q test`
   - `mvn -q clean package -DskipTests`
   - `mvn -q checkstyle:check`
6. 已清理 4 个测试文件中的无用导入，消除本轮 `checkstyle` 阻塞。
