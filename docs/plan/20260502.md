# 2026-05-02 工作计划

> 今日主线：在还款线 3 个关键问题已闭合的基础上，继续收掉 `20260506` 联调方案里剩余的 `pending case`，不再回头反复刷已通过链路。

## 0. 当天起点

`2026-05-01` 已形成以下真实结论：

1. 联调边界已正式收紧到艾博生直接边界。
2. `docs/test/20260506_联调case设计方案.md` 已改为当前可执行版本。
3. `repayment submit` 当前最关键的 3 个真实问题都已闭合：
   - 超时不再掉通用 `500`
   - 超额还款会在本地拦住
   - 重复还款不会重复打 `/repay/apply`
4. `benefiturl` 已接入权益订单信息同步主链，但字段语义风险仍需外部继续确认。

因此，`2026-05-02` 的重点不再是继续修 repayment，而是：

1. 推进剩余 `pending case`
2. 继续收集联调证据
3. 只在出现新的真实 bug 时，再拉开发会话介入

## 1. 今日目标

1. 把联调覆盖矩阵里的剩余 `pending case` 继续收掉。
2. 继续保持“按批次推进、每批留证、发现真 bug 先停”的节奏。
3. 不把已经通过的 repayment case 再当主任务重复刷。
4. 为 `2026-05-07` 对外联调继续沉淀可直接复用的请求、响应和日志证据。

## 2. 今日优先级

### P0：签约主链与签约异常

**目标**

- 收掉签约主链和签约异常相关的剩余 `pending case`。

**计划动作**

1. 跑 `M-B.5` 签约 query
2. 跑 `M-B.6` 签约 apply
3. 跑 `M-B.7` 签约 confirm
4. 跑 `EX-C.1 ~ EX-C.5`

**通过标准**

- 能给出请求、响应、关键日志、实际出站 path / 次数；
- 若失败，能明确是本地 bug、stub 问题还是外部 blocker。

### P0：借款主链与审批结果

**目标**

- 收掉借款申请、审批结果和相关异常线的剩余 `pending case`。

**计划动作**

1. 跑 `M-B.9` 借款申请
2. 跑 `M-B.10` 审批结果
3. 跑 `EX-D.1 ~ EX-D.4`

**通过标准**

- 区分 `processing / rejected / controlled error`；
- 不把“处理中”误判成 bug；
- 继续留足 `requestBodyJson / responseBodyJson / errorNo / errorMsg` 证据。

### P1：还款主链样本补证据

**目标**

- 不再重复刷 `EX-E.1 / EX-E.3 / EX-E.4`，只补一条完整、干净、可留证的还款主链样本。

**计划动作**

1. 跑 `M-B.11` 还款全链
2. 重点补 `repay/info -> repay/submit` 的完整样本

### P1：最小边界复核

**目标**

- 对仍可能受 stub 限制的 case 做最小必要验证，不编造结论。

**计划动作**

1. 跑 `EX-A.3 token 篡改`
2. 复核 `EX-F.3`

**通过标准**

- 能测就给结论；
- 不能测就明确写 `BLOCKED`，并说明缺什么前置。

## 3. 今日完成标准

如果达到以下结果，可认为当天计划达标：

1. 签约和借款两大块的剩余 `pending case` 有明显收缩；
2. 还款主链有一条完整、可复用的正向样本证据；
3. `EX-A.3`、`EX-F.3` 的状态更清楚，不再模糊悬着；
4. 联调结果文档和覆盖矩阵已同步更新到最新真值。

## 4. 风险提示

1. `benefiturl` 字段语义风险今天仍不会自然消失，它主要依赖外部继续确认。
2. `EX-A.3` 和 `EX-F.3` 仍可能受 stub fault 注入能力限制，不应硬跑。
3. 如果签约或借款线出现新的高价值真实 bug，今天应优先停在当前批次修 bug，而不是继续扩后面的 case。

## 5. 一句话判断

> `2026-05-02` 的任务重点是“把剩余 pending case 往前推”，不是回头重复证明已经通过的 repayment 问题；继续按证据驱动推进联调准备。
