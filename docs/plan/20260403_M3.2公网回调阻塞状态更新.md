# 2026-04-03 `M3.2` 公网回调阻塞状态更新

## 1. 文档目的

本文档用于记录 `2026-04-03` 当天对 `M3.2 入站回调联调` 的最新判断，明确：

1. 当前哪些准备工作已经完成
2. 当前为什么仍不能正式启动
3. 一旦外部回复到位，下一步如何立即接上

## 2. 今日已完成准备

截至今天，`M3.2` 相关的本地和文档准备已经推进到以下状态：

### 2.1 本地回调主链已具备

当前项目已经具备：

1. `/api/callbacks/**` 回调入口
2. 签名校验
3. 参数校验
4. 幂等处理
5. 订单状态更新主链

因此当前阻塞不在“本地接口不存在”。

### 2.2 启动条件已整理清楚

当前已经有以下材料可直接复用：

1. 启动条件说明：
   - `docs/plan/20260402_M3.2公网回调启动条件说明.md`
2. 启动建议模板：
   - `docs/plan/20260403_M3.2启动建议模板.md`
3. 发网关 / 运维短版消息：
   - `docs/plan/20260403_发网关运维确认问题清单_短版.md`

### 2.3 启动后验证顺序已固定

今天已经把启动后的建议顺序固定为：

1. 先验证网络可达和签名通过
2. 再验证轻链路：
   - `refund`
   - `exercise-equity`
3. 再验证主业务链：
   - `grant/forward`
   - `repayment/forward`
4. 若需要，再验证扣款类回调：
   - `first-deduction`
   - `fallback-deduction`

## 3. 当前仍阻塞的原因

截至 `2026-04-03` 当前时点，`M3.2` 仍不能正式启动，原因仍是以下三项外部条件尚未确认：

### 3.1 公网测试域名未确认

尚未明确：

1. 是否已有可供第三方访问的测试域名
2. 最终应使用哪个域名

### 3.2 回调路径暴露方式未确认

尚未明确：

1. `/api/callbacks/**` 是否直接暴露
2. 是否走网关转发
3. 是否存在 rewrite

### 3.3 白名单策略未确认

尚未明确：

1. 是否需要来源 IP 白名单
2. 是否需要来源域名白名单
3. 由谁配置、何时生效

## 4. 当前统一判断

今天建议统一使用以下判断：

1. `M3.2` **尚未具备启动条件**
2. 原因不是本地代码未完成
3. 原因是公网域名、路径暴露方式、白名单三项外部条件未齐

一句话概括：

> 当前 `M3.2` 的状态是“本地已准备，外部网络暴露条件待确认”。

## 5. 外部一旦回复后的即时动作

只要网关 / 运维回复补齐上述三项，后续即可立即执行：

1. 补齐正式测试域名
2. 生成完整回调 URL 清单
3. 明确最终回调路径映射
4. 对齐给第三方
5. 启动首轮 `M3.2` 入站回调联调

## 6. 当前建议

今天这条线建议停在以下状态最合理：

1. 不虚假宣称 `M3.2` 已可启动
2. 也不再重复整理相同材料
3. 先把“阻塞原因 + 可发送消息 + 启动模板”全部准备好
4. 等外部回复后立即切换为执行态

## 7. 一句话结论

> `M3.2` 今天已经从“还没梳理清楚为什么不能启动”推进到“材料已准备完毕，只差外部确认即可启动”的状态。
