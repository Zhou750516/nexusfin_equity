# 2026-04-02 齐为 `abs.lending.notify` 可验证性结论

## 1. 目的

本文档用于承接 `2026-04-02` 当天 A3 任务，判断当前项目中的 `abs.lending.notify` 是否已经具备真实验证条件，以及还缺什么外部输入。

## 2. 当前代码状态

### 2.1 请求模型已具备最小入口

当前 `abs.lending.notify` 请求 DTO 为：

1. `uniqueId`
2. `partnerOrderNo`
3. `status`

对应文件：

- `src/main/java/com/nexusfin/equity/thirdparty/qw/QwLendingNotifyRequest.java`

响应 DTO 当前只接：

1. `orderNo`

对应文件：

- `src/main/java/com/nexusfin/equity/thirdparty/qw/QwLendingNotifyResponse.java`

### 2.2 业务触发入口已接主链

当前 `NotificationServiceImpl.handleGrant()` 的处理顺序是：

1. 幂等判断
2. 落接收日志
3. 更新本地放款结果状态
4. 必要时触发兜底代扣
5. 调用 `qwBenefitClient.notifyLending(...)`

当前映射关系为：

1. `uniqueId = order.getExternalUserId()`
2. `partnerOrderNo = order.getBenefitOrderNo()`
3. `status = success ? 1 : 0`

对应文件：

- `src/main/java/com/nexusfin/equity/service/impl/NotificationServiceImpl.java`

结论：

> 从代码接线角度看，`abs.lending.notify` 已经具备“收到我方放款通知后即可主动出站”的最小能力。

## 3. 当前缺口

### 3.1 成功 / 失败样例已补齐

截至 `2026-04-02` 当前，仓库内已确认：

1. `abs.push.order` 有真实成功样例
2. `abs.token.get` 有真实失败样例（`515 会员卡未生效`）
3. `abs.lending.notify` 已补到成功 / 失败样例

当前已确认：

1. `status=0` 表示失败
2. `status=1` 表示成功
3. 成功返回：
   - 原始响应：`{code:200,msg:"OK",data:"8c82f7602d98cbb7a6b8a9beaacffadd5cebf0a221a82696ec7435de571c1b7f"}`
   - `data` 解密后：`{"orderNo":"94260401229898"}`
4. 失败返回：
   - `{code:510,msg:"合作方订单不存在",data:null}`

### 3.2 测试材料未给更细口径

当前测试材料 `docs/third-part/齐为/艾博生-齐为测试环境.txt` 只给出了：

1. 测试地址
2. `partnerNo`
3. method 名称
4. 密钥

并未给出：

1. `abs.lending.notify` 请求字段说明
2. 成功样例
3. 失败样例
4. `status` 枚举说明

因此当前 `abs.lending.notify` 已不再缺最小样例口径。

## 4. 当前可验证性判断

截至 `2026-04-02` 上午，建议统一使用如下判断：

1. **代码入口已具备**
2. **协议层口径已具备参考基础**，因为同一 client 已真实跑通 `abs.push.order`
3. **业务样例已具备**
4. 因此 `abs.lending.notify` 当前状态应定义为：

> **已具备首轮真实验证条件**

原因是：

1. `status` 口径已明确
2. 成功 / 失败返回已具备
3. 成功返回解密后的最小业务字段已明确为 `orderNo`

## 5. 今日最小确认清单

今天若要继续向齐为追问，建议只追最小闭环问题：

1. `abs.lending.notify` 成功样例
2. `abs.lending.notify` 失败样例
3. `status` 字段的取值含义
4. 成功返回解密后最小字段

截至 `2026-04-02` 当前已全部拿到：

1. 成功样例
2. 失败样例
3. `status=0/1` 口径
4. 成功解密后字段 `orderNo`

## 6. 对开发动作的影响

### 情况 A：齐为确认当前字段足够

则今天可继续做：

1. 构造一条真实放款成功通知
2. 保留原始请求 / 原始响应
3. 更新部署说明和 bug / 跟踪文档

### 情况 B：齐为确认字段不足

则后续最小改动会是：

1. 扩展 `QwLendingNotifyRequest`
2. 调整 `NotificationServiceImpl.handleGrant()` 映射字段
3. 如需新增持久化字段，再补库表和实体

## 7. 阶段性结论

一句话总结：

> `abs.lending.notify` 现在已经补齐成功 / 失败样例与 `status` 口径，已具备首轮真实验证条件。
