# 2026-03-30 重点工作计划

## 1. 计划目标

2026-03-30 的工作重点，不是继续分散扩需求，而是围绕“收口、对齐、可联调”推进三件最重要的事：

1. 同步任务清单与仓库真实状态，消除项目管理视图偏差。
2. 收敛当前这轮改动的提交与说明边界，确保 Review 和后续协作可读。
3. 为齐为真实联调做好接口、环境、验证项三方面准备。

## 2. 明日优先级

### P0：同步真实进度，补齐任务状态

**目标**

- 把 `specs/002-cross-domain-auth/tasks.md` 与当前代码事实、测试结果、文档现状对齐。

**原因**

- 当前 `tasks.md` 大量任务仍未勾选，但认证主链、旧注册入口下线、鉴权边界拆分、验证闭环实际上已完成。
- 如果不先回填，后续 Review、排期和交接都会继续依赖错误状态。

**计划动作**

1. 逐项核对 `specs/002-cross-domain-auth/tasks.md` 中与认证改造相关的任务。
2. 结合当前代码、测试、文档和提交记录，判断哪些任务已经完成，哪些只是部分完成，哪些仍未做。
3. 更新任务勾选状态，并在必要处补充简短备注，说明“已完成但通过手工实现落地”的背景。

**预期产出**

1. 更新后的 `specs/002-cross-domain-auth/tasks.md`
2. 一份与仓库事实一致的任务状态视图

### P0：整理本轮改动的提交说明和收口边界

**目标**

- 明确当前仓库中“已完成事项、未完成事项、后续增强项”的边界，避免多主题改动继续混在一起。

**原因**

- 这轮改动同时涉及幂等、审计、跨域认证、齐为接入、安全整改、命名收敛。
- 如果不主动收口，后续提交说明和代码 Review 成本会持续升高。

**计划动作**

1. 以现有文档为基础，整理当前这轮改动的边界：
   - 已完成并验证通过的部分
   - 已落代码但仍需联调验证的部分
   - 明确属于后续增强的部分
2. 检查当前新增文档是否齐全，重点确认：
   - 周报
   - bug 处理说明
   - 验收记录
   - 修改说明
3. 输出一版简明的提交说明草稿，方便后续提交时直接复用。

**预期产出**

1. 一份可直接复用的提交说明草稿
2. 一套清晰的变更边界描述

### P1：推进齐为真实联调准备

**目标**

- 把“已完成代码接入”推进到“具备真实联调前置条件”。

**原因**

- 当前齐为接入已经进入真实代码主链，但周报和验收记录都明确指出，下一阶段风险在于外部测试环境和联调资源打通。
- 如果不先把联调清单和接口前置项核清，后续排障会在外部依赖上反复消耗时间。

**计划动作**

1. 重新核对 `docs/third-part/齐为/齐为权益接口分析与联调清单_20260326.md` 与当前 `thirdparty/qw` 实现。
2. 梳理齐为联调所需的最小前置条件：
   - 测试地址
   - 商户号 / 密钥 / AES 配置
   - 回调地址
   - 联调样例数据
   - 成功 / 失败 / 异常场景验证项
3. 输出一版“联调前检查清单”，明确哪些是内部已具备、哪些需要外部协同。

**预期产出**

1. 一份齐为联调前检查清单
2. 一份内部与外部待协同项列表

## 3. 建议执行顺序

建议按以下顺序推进：

1. 先处理 `tasks.md` 对齐，解决项目状态失真问题。
2. 再整理提交说明和变更边界，解决改动收口问题。
3. 最后推进齐为联调准备，解决下一阶段落地问题。

原因：

1. 任务状态不准，会直接影响当天所有判断。
2. 提交边界不清，会影响后续 Review 和交付说明。
3. 联调准备虽然重要，但应建立在前两项已经收口的基础上。

## 4. 明日完成标准

如果 2026-03-30 结束前完成以下结果，可以认为当天重点工作达标：

1. `specs/002-cross-domain-auth/tasks.md` 已与当前仓库事实同步。
2. 本轮改动的提交说明和边界描述已整理完成。
3. 齐为联调前检查项已形成清单，外部依赖项已明确。

## 5. 风险提示

明日计划可能受以下因素影响：

1. 如果齐为外部联调参数或测试环境信息不完整，联调准备只能先完成内部侧检查清单。
2. 如果当前工作区还有新的未整理改动进入，提交边界收口工作量会增加。
3. 如果发现 `tasks.md` 与代码事实偏差比预期更大，任务回填会占用较多时间。

## 6. 备注

本计划基于截至 2026-03-29 已有周报、bug 处理说明、验收文档和任务清单状态整理而成。若 2026-03-30 开始前又新增需求或新增代码变更，应先调整优先级再执行。
