# 2026-04-03 发网关/运维确认问题清单（短版）

## 1. 文档目的

本文档用于沉淀一版比 `20260402` 更短、适合企业微信直接发送的公网回调条件确认消息，方便今天继续推进 `P1`。

## 2. 企业微信超短版

可直接发送：

> 这边准备启动第三方回调联调，当前本地 `/api/callbacks/**` 主链已具备。  
> 麻烦帮确认 3 项：  
> 1. 是否有可供第三方访问的测试域名  
> 2. `/api/callbacks/**` 是直接暴露还是走网关转发  
> 3. 是否需要来源 IP / 域名白名单  
>  
> 这 3 项确认后，我们就能整理完整回调 URL 并启动 `M3.2`。

## 2.1 一段式复制即发版

如果你想直接发一段、不分条，可以用这版：

> 这边准备启动第三方回调联调，当前本地 `/api/callbacks/**` 主链已经具备，麻烦帮确认下是否已有可供第三方访问的测试域名、`/api/callbacks/**` 是直接暴露还是走网关转发、以及是否需要来源 IP / 域名白名单；这 3 项确认后，我们就能整理完整回调 URL 并启动 `M3.2`。

## 3. 稍正式一点的短版

如果对方需要更完整一点，可直接发送：

> 你好，这边在准备第三方回调联调，当前应用本地回调入口和签名校验已经具备。  
> 为启动 `M3.2`，还需要协助确认以下 3 项：  
> 1. 可供第三方访问的测试域名  
> 2. `/api/callbacks/**` 的对外暴露方式（直出 / 网关转发 / 是否 rewrite）  
> 3. 是否需要来源 IP / 域名白名单，以及配置方式  
>  
> 这 3 项确认后，我们会整理完整回调 URL 给第三方继续联调。

## 4. 当前统一口径

今天统一建议使用以下说法：

1. 本地代码不是当前阻塞点
2. 当前阻塞点是公网回调条件未齐
3. 最关键的就是：
   - 域名
   - 暴露方式
   - 白名单

## 5. 关联文档

1. `docs/plan/20260402_M3.2公网回调启动条件说明.md`
2. `docs/plan/20260402_发网关运维确认问题清单.md`
3. `docs/plan/20260403_M3.2启动建议模板.md`
