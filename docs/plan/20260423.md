# 2026-04-23 工作计划

> 今日主线：基于 `2026-04-22` 已冻结的稳定快照，继续补齐后端日志专项中剩余的异步补偿日志缺口，形成“同步主链路 + 异步补偿链路”完整排障闭环。

## 1. 今日目标

基于 `2026-04-22` 已完成结果：

1. 当前仓库已形成稳定快照 `e06dea2`
2. 完整后端测试已通过，可在稳定基线上继续增量开发
3. 借款、还款、绑卡、云卡客户端、统一异常日志已基本收口

因此 `2026-04-23` 的重点是：

1. 补齐异步补偿入队与 worker 执行日志
2. 统一异步补偿失败日志的 `errorNo / errorMsg` 口径
3. 用测试验证这部分日志字段已经可检索、可排障
4. 将结论回填到日志专项文档与联调检查清单
5. 如验证通过，形成一笔独立增量提交并推送远端

## 2. 今日优先级

### P0：补异步补偿入队日志

**目标**

- 让同步超时进入补偿后的第一跳就有清晰日志证据

**计划动作**

1. 在异步补偿入队成功时打印：
   - `traceId`
   - `bizOrderNo`
   - `taskId`
   - `taskType`
   - `bizKey`
   - `partitionNo`
   - `targetCode`
   - `requestPath`
2. 对重复入队场景补清晰日志
3. 统一重复入队错误码

### P0：补异步补偿 worker 执行日志

**目标**

- 让异步补偿从“入队”到“执行结果”可完整追踪

**计划动作**

1. 补 worker 认领日志
2. 补执行成功日志
3. 补失败待重试日志
4. 补死任务日志
5. 统一失败日志中的 `errorNo / errorMsg`

### P0：补针对性验证

**目标**

- 确认新增日志不是只写了代码，而是真的有测试保护

**计划动作**

1. 先写失败测试锁定期望日志字段
2. 再补最小代码使测试转绿
3. 运行：
   - `AsyncCompensationEnqueueServiceTest`
   - `AsyncCompensationWorkerServiceTest`
4. 追加 `checkstyle` 校验

### P1：回填文档并收口提交

**目标**

- 把本轮异步补偿日志补齐沉淀为可复用的排障材料

**计划动作**

1. 更新日志专项开发任务拆解文档
2. 更新联调与排障检查清单
3. 若验证通过，形成单独提交
4. 推送远端 `main`

## 3. 今日完成标准

如果 `2026-04-23` 结束前达到以下结果，可认为当天达标：

1. 异步补偿入队日志已补齐
2. 异步补偿 worker 认领 / 成功 / 失败 / 死任务日志已补齐
3. 失败日志统一输出 `errorNo / errorMsg`
4. 对应测试通过
5. `checkstyle` 通过
6. 文档已回填
7. 形成独立提交并推送

## 4. 风险提示

1. 若异步补偿日志仍不闭环，`loan apply` 超时后的排障链路会中断
2. 若仍保留旧的 `error=` 混用写法，联调阶段容易出现错误口径不一致
3. 若不补测试，后续日志字段很容易再次被无意改坏

## 5. 一句话判断

> `2026-04-23` 的关键不是再扩日志范围，而是把异步补偿链路最后一段补齐，让“同步调用 + 异步补偿”真正形成可排障闭环。
