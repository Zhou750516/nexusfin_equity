# 2026-04-30 工作计划

> 今日主线：在不破坏现有基线的前提下，收口 Layer B 的剩余语义边界，并决定是直接进入 `B-P0-3`，还是先补第二个隔离用户把 `B-P0-2` 的更窄证明做完整。

## 0. 当天结果补记

`2026-04-30` 到目前为止，已经完成三件关键事：

1. `1abcf4a` 修复了 clean 用户 `QW_SIGN` 前置被固定 `mock-agreement-1234` 撞唯一键的问题，clean 用户现已具备独立 `ACTIVE QW_SIGN`。
2. clean 用户下的 `B-P0-1` 已 fresh 命中新的 `YUNKA_UPSTREAM_TIMEOUT`，并成功新增 `YUNKA_LOAN_APPLY_RETRY`。
3. clean 用户下的 `B-P0-2` 运行时已证明：
   - 没有第二笔 `benefit_order`
   - 没有第二条 `loan_application_mapping`
   - 没有第二次 `Yunka /loan/apply`
4. `896c4c2` 已新增第二个隔离 clean 用户 `mock-tech-token-clean-2 / tech-user-local-clean-002`，并确认：
   - 可正常登录
   - 默认无历史 `PENDING_REVIEW`
   - 已可补出独立 `ACTIVE QW_SIGN`
5. 第二个 clean 用户下的隔离版 `B-P0-2` 已最终打实：
   - 第一步 fresh 命中新的 `YUNKA_UPSTREAM_TIMEOUT`
   - 第二步立即重试复用第一步刚生成的 `applicationId / benefitOrderNo`
   - 补偿任务保留且未被破坏

当前剩下的已经不是 `B-P0-2` 语义争议，而是是否进入下一条 `B-P0-3` 回调幂等线。

## 1. 今日目标

基于当前已确认事实：

1. 本地 fullflow 与 Layer A 已闭合；
2. Layer B 的 `B-P0-1` 已 fresh 通过；
3. `B-P0-2` 的宽语义已通过，但窄语义证明仍受执行顺序影响；
4. 云卡公网与电子签章仍是独立挂起项。

因此今天的重点是：

1. 正式收口 `B-P0-2`，确认宽语义与窄语义都已具备运行时证据；
2. 准备进入 `B-P0-3` 回调幂等线；
3. 继续保持“单 case 打实后再扩”的节奏；
4. 同步回填日报、decision-review、plan、checklist，避免结论在会话之间漂移。

## 2. 今日优先级

### P0：收口 `B-P0-2` 验收边界

**目标**

- 避免在“真实问题已修好”和“证明方式还想更严格”之间反复消耗。

**计划动作**

1. 对照当前测试证据确认：
   - `B-P0-1` 已 fresh 通过
   - `B-P0-2` 已证明不会新建第二套业务对象
   - `B-P0-2` 也已证明“第一步 fresh timeout、第二步复用它”
2. 将 `B-P0-2` 从“待讨论边界”转为“已收口项”
3. 不再为这条语义继续补用户或重跑

### P1：继续准备 `B-P0-3`

**目标**

- 让 Layer B 不停在边界讨论，而是继续往下一条真实异常链推进。

**计划动作**

1. 基于当前已闭合的 loan/apply timeout 线，整理进入 `B-P0-3` 的测试前提
2. 保持“单条场景打实后再扩”的节奏

### P1：同步文档入口

**目标**

- 让 `2026-04-30` 之后的会话看到的是最新判断，而不是继续引用 `2026-04-29` 的旧入口。

**计划动作**

1. 补 `20260429` 日报与 decision-review
2. 更新 `20260428_checklist.md` 的最终完成态说明
3. 生成 `20260430` plan 与 checklist
4. 视情况更新 `CURRENT_STATE.md` 与 `README.md`

## 3. 今日完成标准

如果 `2026-04-30` 结束前达到以下结果，可认为当天达标：

1. `B-P0-2` 的宽语义与窄语义都已明确，不再反复讨论
2. `B-P0-3` 已具备进入条件
3. 日报、decision-review、plan、checklist 已同步回填

## 4. 风险提示

1. `B-P0-3` 将开始触碰回调幂等与终态保护，验证时要避免复用不合适的旧业务键。
2. 若为了后续更窄证明继续改 `loan/apply` 复用逻辑，容易把已收口的测试问题重新改成业务问题。
3. 云卡公网 `KJ_REQUEST_ENCODE_ERROR` 与电子签章接入问题今天仍不能与 Layer B 结论混用。

## 5. 一句话判断

> `2026-04-30` 的关键已经从“证明 `B-P0-2`”切到“基于已闭合的 loan/apply timeout 线，继续进入 `B-P0-3` 回调幂等验证”。
