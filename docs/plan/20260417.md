# 2026-04-17 工作计划

> 明日主线：把今天完成的“边界澄清与问题收集”进一步转成可推进的正式方案入口，优先推进联合登录与页面承接方案。

## 1. 明日目标

基于 `2026-04-16` 已完成结果：

1. H5 主动还款成功与本地 `payment_record` 的边界已明确
2. 小花联合登录对外短版待确认清单已可直接使用
3. 分发页 / 退款页 / 状态回推的设计问题已初步收集
4. 齐为绑卡 3 个新增接口已继续沉淀为 TODO

因此 `2026-04-17` 的重点是：

1. 继续把外部依赖项推进成“可对齐、可催办”的资料
2. 在不抢跑开发的前提下，把本侧正式方案入口再往前推一步
3. 为后续设计 / 开发留出更清晰的拆解边界

## 2. 明日优先级

### P0：推进小花联合登录正式方案输入

**目标**

- 让联合登录从“待确认问题”进入“可落方案准备”状态

**计划动作**

1. 以 `plan/20260416_发小花联合登录待确认短版.md` 为外发基线
2. 梳理本侧收到正式文档后需要立即落地的设计项：
   - 跳转参数规范
   - token 校验 / 换取链路
   - 本地登录态建立方式
   - 异常页与回跳口径
3. 若外部文档仍未返回，则整理一版“联合登录本侧方案框架”

### P0：把分发页 / 退款页 / 状态回推问题清单进一步整理成方案骨架

**目标**

- 将问题清单收敛成后续可继续扩展的方案文档结构

**计划动作**

1. 按三块拆分边界：
   - 分发页
   - 退款页
   - 状态回推
2. 先写清每块的：
   - 页面职责
   - 入参来源
   - 后端接口边界
   - 外部依赖
3. 保持“未确认项”显式列出，不抢写最终实现

### P1：继续维护齐为绑卡新增接口待办

**目标**

- 保持绑卡需求处于随时可接续状态

**计划动作**

1. 继续等待齐为正式接口资料
2. 预留后续需要补齐的设计维度：
   - 页面流程
   - 后端接口
   - 协议号映射
   - 回写表设计

### P1：评估是否需要单独拉起“本地还款记录模型”设计待办

**目标**

- 防止后续对账或验收时再次出现“页面成功 ≠ 本地账务成功”的歧义

**计划动作**

1. 继续维持当前短期口径不变
2. 若业务侧继续追问强一致能力，则补一份专门的待确认项清单：
   - 是否需要本地还款申请流水
   - 是否需要还款结果回写
   - 是否需要本地对账 / 查询能力

## 3. 明日完成标准

如果 `2026-04-17` 结束前达到以下结果，可认为当天达标：

1. 小花联合登录已形成更明确的本侧方案承接入口
2. 分发页 / 退款页 / 状态回推已从问题清单升级为方案骨架或设计提纲
3. 齐为绑卡新增接口仍保持在清晰可跟踪状态
4. 如有需要，本地还款记录模型的后续确认项已被显式沉淀

## 4. 风险提示

1. 若小花联合登录文档继续延迟，本侧方案推进仍会停留在半成品阶段
2. 若提前把分发页 / 退款页拆开独立推进，后续容易因登录态与参数口径不统一返工
3. 若不提前管理“本地还款强一致”预期，后续验收口径还会反复

## 5. 一句话判断

> `2026-04-17` 应从“问题清单整理”继续推进到“正式方案骨架搭建”，但仍坚持不在外部契约缺失时抢跑实现。

## 6. 执行进展补充（联合登录核心入口）

已提前完成“小花联合登录核心入口（本地骨架）”第一轮落地，当前结果如下：

1. 已新增后端接口：`POST /api/auth/joint-login`
2. 已新增后端骨架：
   - `JointLoginController`
   - `JointLoginService`
   - `JointLoginServiceImpl`
   - `XiaohuaAuthClient`
   - `SkeletonXiaohuaAuthClient`
   - `JointLoginTargetPageResolver`
3. 已新增 H5 页面：
   - `/joint-entry`
   - `/joint-dispatch`
   - `/joint-refund-entry`
4. 已完成聚焦测试：
   - `JointLoginControllerIntegrationTest`
   - `JointLoginServiceTest`
   - `joint-entry.logic.test.ts`
   - `H5` TypeScript 类型检查
5. 已补联调检查清单：
   - `plan/20260417_小花联合登录联调检查清单.md`

### 当前边界

本轮仅完成“核心入口骨架”，尚未进入：

1. 小花真实协议适配
2. 齐为真实分发页跳转
3. 正式退款流程
4. 权益状态回推

## 7. 执行进展补充（分发页 / 退款页 / 状态回推）

已完成“分发页 / 退款页 / 状态回推”方案骨架沉淀：

1. 新增文档：
   - `docs/plan/20260417_分发页退款页状态回推方案骨架.md`
2. 已将设计问题清单进一步收敛为三块正式骨架：
   - 分发页骨架
   - 退款入口骨架
   - 权益状态回推骨架
3. 已与当前联合登录核心入口实现对齐：
   - `/joint-entry`
   - `/joint-dispatch`
   - `/joint-refund-entry`
4. 明确当前阶段只固化：
   - 页面职责
   - 场景路由
   - 后端接口边界建议
   - 外部依赖与未确认项

### 当前边界

本轮仍未进入以下实现：

1. 齐为真实供应商跳转
2. 正式退款申请 / 查询
3. 科技平台状态回推实现

## 8. 执行进展补充（齐为绑卡新增接口 TODO）

已完成对 `plan/20260415_齐为绑卡流程新增接口TODO.md` 的当日复核与补充，当前结果如下：

1. 已再次确认 3 个接口仍处于“待齐为正式接口资料”阶段
2. 已明确当前继续不进入开发，保持为可跟踪 TODO
3. 已提前预留三类本侧设计入口：
   - 页面流程入口
   - 后端接口映射入口
   - 协议模型入口
4. 已补 `2026-04-17` 跟踪结论，后续可在齐为文档到位后直接续写

### 当前边界

本轮仅做跟踪收口，不进入以下内容：

1. H5 绑卡页面实现
2. 后端绑卡接口实现
3. 协议表结构落库设计

## 9. 执行进展补充（本地还款记录模型预期管理）

已补一版“本地还款记录模型待确认项”文档：

1. 新增文档：
   - `docs/plan/20260417_本地还款记录模型待确认项.md`
2. 已将短期口径再次固化：
   - 当前不要求新增本地还款记录表
   - 当前不改造 `payment_record`
   - 当前不把页面主流程成功与本地账务强一致绑定
3. 已把后续若要强一致时需要确认的维度显式列出：
   - 是否新增独立模型
   - 是否覆盖申请 / 查询 / 回调全链路
   - 外部流水与本地单号映射
   - 查询、对账、补偿能力

### 当前边界

本轮仅做预期管理与待确认项沉淀，不进入：

1. `repayment_record` 表设计
2. 还款记录接口开发
3. 本地账务强一致改造

## 10. 执行进展补充（联合登录真实协议适配准备）

已补一版“联合登录真实协议适配占位清单”：

1. 新增文档：
   - `docs/plan/20260417_小花联合登录真实协议适配占位清单.md`
2. 已明确当前真实协议接入时优先保持稳定的边界：
   - H5 入口页与后端入口 DTO
   - `JointLoginServiceImpl` 主流程
   - 目标页分流逻辑
3. 已明确优先替换点：
   - `SkeletonXiaohuaAuthClient`
   - 小花配置项
   - 错误码归一化
   - 联调日志与测试补位
4. 已明确真实协议文档到位后的推荐接入顺序：
   - 字段映射
   - 配置补齐
   - 真实客户端实现
   - 测试联调

### 当前边界

本轮仅做真实协议适配准备，不进入：

1. 小花真实 HTTP 接口实现
2. 配置项正式落地
3. 新一轮后端 / H5 测试改造
