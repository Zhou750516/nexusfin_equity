# 2026-03-31 重点工作计划

## 1. 计划目标

结合 [20260329_项目开发到上线整体计划.md](/Users/lixiaokun/Projects/nexusFin/nexusfin-equity/docs/plan/20260329_项目开发到上线整体计划.md) 的里程碑定义，以及 2026-03-30 已完成的任务状态回填、提交边界整理和齐为联调前检查清单，今天的工作重点应从“收口整理”切换到“联调推进前的最后准备与阻塞清理”。

2026-03-31 的核心目标有三项：

1. 继续完成 `M1 开发完成` 的剩余收尾项，重点收口 `T021` 和 `T026`。
2. 把 `M2 联调准备完成` 从“文档已梳理”推进到“参数、路由、环境口径可执行”。
3. 为 `M3 联调执行` 做最小启动准备，优先锁定出站接口联调条件。

## 2. 今日优先级

### P0：收口剩余开发项，补齐联调期最需要的日志与韧性

**目标**

- 完成当前仍未彻底收口的开发侧任务，避免带着明显观测和调用短板进入真实联调。

**对应里程碑**

- `M1 开发完成`

**原因**

- 昨天虽然已经完成 `tasks.md` 状态回填，但仍明确保留了两项未完全完成任务：
  - `T021`：访问链路 trace 日志仍未完全覆盖
  - `T026`：上游用户校验调用的 retry 与更细粒度失败可观测性仍未补齐
- 如果这两项继续拖到联调阶段，排障效率会明显下降。

**计划动作**

1. 核对 `AuthController`、`BenefitOrderController` 当前已具备的日志点，明确还缺哪些用户访问关键路径日志。
2. 优先补齐以下接口或路径的访问日志：
   - 产品页访问
   - 查单
   - 行权链接获取
3. 核对 `TechPlatformUserClientImpl` 当前超时、异常映射和调用日志实现，判断最小可落地的 retry 与失败留痕方案。
4. 若当天无法完整补齐 retry 机制，至少先把失败日志、traceId、关键参数上下文补齐，并把剩余增强边界写清。

**预期产出**

1. `T021` 明显收口，业务访问日志覆盖更完整
2. `T026` 至少达到“联调可观测、失败可定位”的最低标准
3. 若仍有缺口，形成明确遗留说明，而不是模糊悬而未决

### P0：把齐为联调前检查清单变成可执行清单

**目标**

- 不停留在“已经列清单”，而是推动关键外部依赖项拿到明确反馈。

**对应里程碑**

- `M2 联调准备完成`

**原因**

- 昨天已经输出 [20260330_齐为联调前检查清单.md](/Users/lixiaokun/Projects/nexusFin/nexusfin-equity/docs/plan/20260330_齐为联调前检查清单.md)，但其中关键外部项仍全部待确认：
  - `partnerNo / signKey / aesKeyBase64`
  - 科技平台真实测试地址与 token 样例
  - 扣款回调映射规则
  - 我方公网测试回调地址
- 如果今天不推动这些项从“待确认”进入“已确认 / 已催办 / 有阻塞说明”，项目会停留在伪准备状态。

**计划动作**

1. 基于昨天的联调前检查清单，整理一版“待外部确认项”最小清单，便于直接对外同步。
2. 明确每个外部依赖项的负责人、来源方和阻塞影响：
   - 齐为
   - 科技平台
   - 我方网关/运维
3. 把“齐为扣款回调 -> 我方 `first-deduction / fallback-deduction`” 的映射问题单独列为高优先级确认项。
4. 如当天无法拿到全部真实参数，至少形成一份对外同步口径，避免后续重复解释。

**预期产出**

1. 一份可直接发给外部或协同方的待确认项清单
2. 一份内部阻塞项说明，明确当前缺的不是代码而是哪些外部条件
3. `M2` 从“文档准备”推进到“协同准备”

### P1：准备联调执行顺序，优先瞄准 M3.1 出站接口

**目标**

- 把联调执行顺序提前排出来，避免外部条件一到位后仍然临场决定怎么联。

**对应里程碑**

- `M3 联调完成` 的启动准备

**原因**

- 整体里程碑已经明确建议联调先做出站接口，再做入站回调，最后补异常场景。
- 目前最适合先打通的是 3 个我方主动调用齐为的接口，因为边界最清楚、依赖最集中。

**计划动作**

1. 按以下顺序定义联调执行优先级：
   - `abs.push.order`
   - `abs.token.get`
   - `abs.lending.notify`
2. 明确每个接口的联调输入、成功判定、失败判定和需要保留的证据。
3. 预先列出联调当天最可能需要观察的日志点和状态字段。
4. 若条件允许，补一版“出站接口联调执行说明”草稿，方便联调时直接照单执行。

**预期产出**

1. 一份出站接口联调执行顺序
2. 每个接口的最小通过判定标准
3. 联调执行时的观察点清单

## 3. 建议执行顺序

今天建议按以下顺序推进：

1. 先收口 `T021`、`T026`，把联调期最影响排障效率的问题先处理掉。
2. 再推动外部待确认项，让 `M2` 不再停留在纸面准备。
3. 最后把 `M3.1` 的联调执行顺序和判定标准整理出来。

原因：

1. 没有日志和失败可观测性，联调即使开始也会低效。
2. 没有真实参数和环境口径，联调无法真正启动。
3. 没有执行顺序和通过标准，外部条件就绪后仍会浪费时间临场决策。

## 4. 今日完成标准

如果 2026-03-31 结束前完成以下结果，可以认为今天的重点工作达标：

1. `T021`、`T026` 至少有一项彻底收口，另一项有明确落地方案或实际进展。
2. 昨天联调前检查清单中的外部待确认项，已经形成可执行的对外同步口径。
3. 已明确 `M3.1` 出站接口联调的先后顺序、成功标准和观察点。

## 5. 风险提示

今天的计划可能受以下因素影响：

1. 如果外部协同方无法在今天提供真实参数，`M2` 仍然只能部分推进。
2. 如果 `T026` 的 retry 方案涉及面超出预期，今天更适合先补可观测性而不是一次性做大改。
3. 如果当前工作区又引入新的功能性改动，会干扰从 `M1` 平滑过渡到 `M2/M3` 的节奏。

## 6. 备注

本计划基于以下事实整理：

1. 昨天 `2026-03-30` 已完成任务状态回填、提交说明草稿和齐为联调前检查清单。
2. 当前项目在整体里程碑上已接近完成 `M1`，今天应重点推进 `M2`，并开始为 `M3.1` 做启动准备。
3. 若今天拿到新的外部联调信息，应优先更新联调清单和执行顺序，而不是先扩展新功能。
