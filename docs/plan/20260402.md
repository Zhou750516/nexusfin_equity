# 2026-04-02 重点工作计划

## 1. 计划目标

截至 `2026-04-01` 结束，项目已经完成以下阶段性推进：

1. 齐为标准 HTTP 协议已打通
2. `abs.push.order` 已在测试环境真实成功
3. `abs.token.get` 已进入业务层，但当前返回 `515 会员卡未生效`
4. 科技平台阶段性部署文档已落地，但真实联调条件仍未齐备
5. `20260401` 的计划、checklist 和推进跟踪已完成回填

因此 `2026-04-02` 的重点，不再是重新证明“联调能不能启动”，而是围绕“收口剩余阻塞、推进剩余接口、继续拿外部确认”推进三件事：

1. 优先收口齐为 `abs.token.get` 的生效时序问题，并尽量推进 `abs.lending.notify` 的真实样例验证。
2. 继续推动科技平台真实地址、token 样例和返回结构确认，缩小 `M2` 剩余阻塞面。
3. 继续推动我方公网测试回调条件确认，为 `M3.2` 做排期准备。

说明：

1. 明天仍以 **齐为联调主线** 为第一优先级。
2. 科技平台完整借款 / 还款页面编排仍不纳入当天主执行线。

## 2. 明日优先级

### P0：收口齐为 `abs.token.get` 与 `abs.lending.notify`

**目标**

- 尽量把 `M3.1` 从“已启动”继续推进到“剩余问题更明确”或“进一步收口”。

**对应里程碑**

- `M3.1 出站接口联调完成`

**原因**

- 当前齐为协议层问题已经排除，最值得继续推进的是剩余业务时序和最后一个出站接口样例问题。
- 如果明天还能拿到齐为额外说明或样例，`M3.1` 有机会进一步收口。

**计划动作**

1. 优先确认 `abs.token.get` 的业务时序要求：
   - 是否存在固定等待窗口
   - 是否需要额外状态
   - 是否必须使用齐为侧 `orderNo / cardNo`
2. 视外部反馈情况，补一次延时重试或按新口径重试 `abs.token.get`
3. 尽量拿到 `abs.lending.notify` 成功 / 失败样例
4. 若条件具备，启动 `abs.lending.notify` 的真实验证

**预期产出**

1. 一份更明确的 `abs.token.get` 阻塞结论
2. 若样例齐备，完成 `abs.lending.notify` 首轮真实验证
3. 一份补充到 bug / 部署文档中的新增联调结论

### P0（HOLD）：科技平台事项整体暂停推进

**目标**

- 今天不继续推进科技平台代码、联调和细化实现，统一进入等待后续整体调整状态。

**对应里程碑**

- `M2 联调准备完成`
- `M4 验收闭环准备`

**原因**

- 用户已明确：科技平台后续还会整体调整。
- 因此今天继续推进科技平台代码或细化联调方案，沉没成本高且容易返工。

**计划动作**

1. 今天不继续推进科技平台代码改造
2. 今天不继续扩展科技平台联调执行细节
3. 仅保留“阶段性基线版 + 待确认项 + HOLD 说明”
4. 等后续整体调整口径明确后，再集中恢复推进

**预期产出**

1. 一份明确的 HOLD 说明
2. 避免今天误把科技平台继续当成主执行线

### P1：推动我方公网测试回调条件确认

**目标**

- 为 `M3.2` 提前清理外部环境障碍。

**对应里程碑**

- `M3.2 入站回调联调`

**原因**

- 即使齐为出站侧继续推进，若公网回调域名、暴露方式和白名单策略迟迟不明确，`M3.2` 依旧无法启动。

**计划动作**

1. 向我方网关 / 运维确认：
   - 公网测试回调域名
   - 回调路径暴露方式
   - 白名单口径
2. 若拿到结果，补齐回调准备材料
3. 若仍未拿到，继续固定阻塞影响与时间线判断

**预期产出**

1. 一份更明确的 `M3.2` 启动条件状态
2. 若条件具备，形成下一步回调联调排期建议

## 3. 建议执行顺序

建议按以下顺序推进：

1. 先追齐为 `abs.token.get` 的时序与 `abs.lending.notify` 样例。
2. 科技平台整条线今天保持 `HOLD`，不再作为主执行线。
3. 再向我方网关 / 运维确认公网测试回调条件。
4. 若当天拿到任何新增外部确认，优先回填文档再继续执行。

原因：

1. 齐为是当前最接近收口的一段，继续推进收益最高。
2. 科技平台虽仍是外部阻塞项，但今天已明确 `HOLD`，不继续投入实现成本。
3. 文档若不及时更新，第二天状态又会失真。

## 4. 今日开发任务安排

为避免今天继续停留在“方向正确但动作分散”的状态，建议把当天工作明确拆成 **主执行任务**、**等待外部反馈的并行任务**、**收到反馈后的即刻处理任务** 三层。

### 4.1 主执行任务：齐为联调收口

**任务 1：复核 `abs.token.get` 当前入参与证据链**

1. 回看当前真实调用日志，重新整理以下字段：
   - 我方 `partnerOrderNo`
   - 齐为返回 `orderNo`
   - 齐为返回 `cardNo`
   - 当前 `abs.token.get` 实际使用的查询参数
2. 对照 `QwBenefitClientImpl` 与 `demo` 分析文档，确认当前代码是否仍只使用我方订单号。
3. 输出一份“当前参数口径 vs 待确认口径”的差异清单，作为今天继续追齐为的依据。

**任务 2：准备 `abs.token.get` 二次验证方案**

1. 预先准备两套重试口径：
   - 方案 A：按当前实现延时重试
   - 方案 B：若齐为确认需要，改为带 `orderNo / cardNo` 的查询口径
2. 若今天拿到齐为新反馈，优先按反馈口径修改并验证，不再重复泛化排查。
3. 每次验证都要保留：
   - 调用时间
   - 入参摘要
   - 原始响应
   - 结论

**任务 3：推进 `abs.lending.notify` 首轮真实验证准备**

1. 先确认当前代码入口、请求字段和日志埋点是否已满足真实验证需要。
2. 整理缺失项清单：
   - 成功样例
   - 失败样例
   - 状态码与业务含义
3. 若当天拿到样例，直接进入真实调用；若拿不到，则把缺口同步回联调文档。

### 4.2 并行任务：科技平台外部确认与本地准备

**任务 4：固定科技平台待确认口径**

1. 今日对外只追最小闭环输入，不扩大范围：
   - `/api/users/me` 真实测试地址
   - Bearer Token 样例或获取方式
   - 真实返回字段结构
2. 若拿到新增信息，第一时间更新阶段性部署文档。
3. 若拿不到，也要明确记录“今天继续阻塞”的结论，避免晚上再重复确认。

**任务 5：同步检查当前科技平台代码是否需要等待更新**

1. 复核当前实现与昨天文档是否一致。
2. 若今天仅有接口口径更新、但代码条件仍未齐备，则不提前做大改。
3. 只保留必要的差异记录，等待最新接口定稿后再落代码。

### 4.3 并行任务：公网回调准备

**任务 6：把 `M3.2` 启动前置条件说清楚**

1. 固定今天需确认的三项：
   - 公网测试回调域名
   - 回调路径暴露方式
   - 白名单策略
2. 若仍无结果，补充一版“缺少哪些条件就不能启动 `M3.2`”的阻塞说明。
3. 让后续对网关 / 运维同步时，可以直接复用这份口径。

### 4.4 收尾任务：当日结果回填

**任务 7：当天所有新增结果必须当日回填**

1. 若齐为有新结论，更新：
   - `docs/bug/20260401_齐为504数据协议非法分析.md`
   - `docs/third-part/齐为/20260401_齐为线上部署说明.md`
2. 若科技平台有新结论，更新：
   - `docs/third-part/科技平台/20260401_科技平台线上部署说明.md`
3. 若计划状态变化，更新：
   - `docs/plan/20260402.md`
   - `docs/plan/20260402_checklist.md`

## 5. 今日建议时间块

为了减少任务切换，建议按以下时间块执行：

1. **上午第一段**：先把齐为 `abs.token.get` 当前证据链复盘清楚。
2. **上午第二段**：整理 `abs.lending.notify` 缺失项和今天要追的样例口径。
3. **下午第一段**：跟进科技平台真实地址 / token / 返回结构。
4. **下午第二段**：跟进公网回调条件，并同步更新阻塞状态。
5. **下班前**：统一回填 bug、部署文档、plan、checklist。

## 6. 明日完成标准

如果 `2026-04-02` 结束前达到以下结果，可以认为当天重点工作达标：

1. 至少把齐为 `abs.token.get` 的阻塞原因再向前推进一层。
2. 若样例齐备，至少启动或完成 `abs.lending.notify` 的首轮真实验证。
3. 至少完成一轮公网回调条件状态更新；科技平台保持 `HOLD` 状态记录即可。
4. 若仓库内新增了外部确认信息，相关文档已同步回填。

## 7. 风险提示

`2026-04-02` 的计划主要受以下因素影响：

1. 齐为若仍无法确认 `abs.token.get` 的时序要求，`M3.1` 只能部分收口。
2. `abs.lending.notify` 若拿不到样例，最后一个出站接口验证仍会滞后。
3. 科技平台当前已整体 `HOLD`，待后续整体调整口径明确后再恢复推进。
4. 我方网关 / 运维若未确认公网回调条件，`M3.2` 仍无法排期。

## 8. 备注

本计划基于 `2026-04-01` 的真实结果整理：

1. 项目当前已从“能否启动联调”进入“如何收口剩余联调问题”的阶段。
2. 当前最重要的不是继续泛化扩功能，而是把剩余外部阻塞项继续压缩。
3. 科技平台接口后续仍会整体调整，今天统一不继续推进代码和联调动作。
