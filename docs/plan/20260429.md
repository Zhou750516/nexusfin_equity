# 2026-04-29 工作计划

> 今日主线：确认 `2026-04-28` 日计划已完整执行，完成后续阶段结果复核，并把项目入口文档切到“重构阶段已完成、进入收口与稳态维护”的最新口径。

## 0. 当前已知新增问题

在 `2026-04-29` 事后补跑 `MySqlAsyncCompensationIntegrationTest` 时，发现当前存在一个新的收口缺陷：

- `AllinpayDirectQwBenefitClient` 存在两个带 `@Autowired` 标记的构造器
- 导致 MySQL IT context 初始化失败
- 影响结论：
  - `mvn test` 仍通过
  - H5 `tsc` 仍通过
  - 但可选 MySQL IT 目前不通过

因此，今天的计划应以“结果复核 + 缺陷归档 + 文档收口”为主，而不是继续宣称所有验证全绿。

## 1. 今日目标

基于当前已确认事实：

1. `docs/plan/20260428.md` 对应的日计划已经执行完成；
2. `Phase 0 / 1A / 1B / 2A / 2B / 2C / 2D / 2E / 5 / 3 / 4` 已全部推进并有提交与验证结果；
3. 当前剩余问题已经不再是“继续拆分主线代码”，而是“如何把文档、状态入口和验证口径收成一致的最终结论，并归档新暴露的 MySQL IT 缺陷”。

因此 `2026-04-29` 的重点是：

1. 审查并确认 `2026-04-28` 日计划可视为完成
2. 复核后续阶段的验证证据与结构性指标
3. 更新 `CURRENT_STATE.md`、日计划和 checklist 的最终状态口径
4. 明确当前还剩哪些真正未完成项，哪些已经不应继续当作待办
5. 将 MySQL IT 失败归类为新的待修缺陷，而不是回滚整轮阶段完成结论

## 2. 今日优先级

### P0：确认 2026-04-28 日计划完成态

**目标**

- 把“20260428 做了很多事”收口成“这份日计划已经完成”的明确结论，避免后续继续误判。

**计划动作**

1. 对照 `docs/plan/20260428.md`
2. 确认以下事项都有证据：
   - 双分支收口完成
   - `2026-04-27` 未收口事项已重新归类
   - `Phase 1A ~ 2E` 已完成
   - 后续阶段顺序已固定
3. 在 `20260428.md` 中补执行结果说明，明确“日计划完成”与“总工程完成”的区别

### P0：复核后续阶段结果

**目标**

- 确认 `Phase 0 / 5 / 3 / 4` 的结果不是口头完成，而是有测试、编译和结构证据支撑。

**计划动作**

1. 复核 `Phase 5`：
   - `@SpringBootTest` 数量变化
   - controller slice 增量
   - `mvn test` 耗时变化
2. 复核 `Phase 3`：
   - `Allinpay*` 文件数
   - `QwProperties.java` 行数
   - `Skeleton*` 清理结果
3. 复核 `Phase 4`：
   - `CalculatorPage.tsx` 行数
   - i18n 拆分结果
   - `components/ui` 文件数
   - H5 `tsc` 与逻辑测试结果
4. 复核最终后端基线：
   - `mvn test`
5. 复核可选 MySQL IT：
   - `MySqlAsyncCompensationIntegrationTest`
   - 如失败，则记录直接原因与影响边界

### P1：更新入口文档

**目标**

- 让后续任何会话开工时，不会再把已经完成的重构阶段当成待做项。

**计划动作**

1. 更新 `docs/plan/CURRENT_STATE.md`
2. 更新 `docs/plan/20260428.md`
3. 如有必要，更新 `docs/plan/20260428_checklist.md`
4. 明确当前入口文件中：
   - 哪些阶段已完成
   - 哪些属于可选补充验证
   - 哪些只是既有未提交文档状态，不是代码未完成

## 3. 今日完成标准

如果 `2026-04-29` 结束前达到以下结果，可认为当天达标：

1. `20260428.md` 已被明确标注为“日计划完成”
2. `Phase 0 / 5 / 3 / 4` 的结果均有独立复核证据
3. `CURRENT_STATE.md` 与 `20260428.md`、`20260428_checklist.md` 结论一致
4. 后续没有再把已完成阶段误列为待办
5. `MySqlAsyncCompensationIntegrationTest` 的失败已被明确定义为单独缺陷，而不是文档口径遗漏

## 4. 风险提示

1. 如果只把提交做完、不更新入口文档，后续会话仍会把已完成重构误判为未做。
2. 如果把“日计划完成”和“总工程结束”混为一谈，会造成文档阅读歧义。
3. 当前仍有一些你明确要求不要动的既有脏状态，它们不能被误读为本轮主实现未提交。
4. 如果不把 MySQL IT 的失败单独记为缺陷，后续很容易误读成“重构阶段根本没有完成”，这也不准确。

## 5. 一句话判断

> `2026-04-29` 的关键不是再继续扩代码，而是把 `2026-04-28` 已完成的重构结果收成一套不会误导后续会话的入口口径。
