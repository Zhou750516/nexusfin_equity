# 2026-05-01 工作计划

> 今日主线：以夜间自动化联调 Round 12 结果为准，收口“本地主链稳定 + 联调前硬化”，把剩余风险压缩到外部阻塞和低优先级质量项。

## 0. 当天结果补记

`2026-05-01` 已形成以下真实结论：

1. 夜间自动化联调已跑到 Round 12，且未新增 `P0 / P1 blocker`。
2. 本地主链已稳定：
   - `joint-login`
   - `POST /api/auth/redrect_benefit_url`
   - `POST /api/loan/calculate`
   - `GET /api/benefits/card-detail`
   - `POST /api/benefits/activate`
   - `repayment` 代表性链路
3. invalid token 运行态语义已闭环：
   - `JOINT_LOGIN_TOKEN_INVALID`
   - `JOINT_LOGIN_UPSTREAM_FAILED`
   - `JOINT_LOGIN_UPSTREAM_TIMEOUT`
   的分支都已有运行态证据。
4. `benefiturl` 主链运行态证据已闭合：
   - 已拿到 `benefits sync benefiturl generated`
   - 已拿到 `path=/benefit/sync requestBodyJson`
   - payload 中真实包含 `benefiturl`
5. `4a8e5b9` 已完成联调前硬化收尾：
   - Yunka stub 契约测试防漂移
   - `benefiturl` 语义注释 / 日志硬化
   - H5 关键表单 `id/name` 第一轮补齐

因此，`2026-05-01` 后续不再把重点放在“本地主链修 bug”，而是转为：

1. 保持当前稳定基线
2. 收敛剩余语义风险
3. 准备 2026-05-07 对外联调的输入与口径

## 1. 今日目标

1. 固定当前夜间回归结论，避免后续会话继续沿用旧 blocker 口径。
2. 将“本地主链稳定”和“字段语义是否完全确认”拆开管理。
3. 为齐为 / 云卡 / 科技平台联调准备最小风险清单，而不是继续在本地重复打转。

## 2. 今日优先级

### P0：固定本地稳定基线

**目标**

- 不再让本地 stub path 漂移或旧问题状态回填错误重新制造噪音。

**计划动作**

1. 以 `docs/test/20260501_夜间自动化联调测试结果.md`、问题清单、运行记录为当前真值。
2. 保持 `QW=MOCK + Yunka local stub + tech-user stub` 基线。
3. 在文档中明确：
   - 先前 Round 1 的本地 blocker 已在 Round 2 修复并回归通过
   - 当前不再存在本地主链级 `P1 blocker`

### P0：收口联调前硬化

**目标**

- 把已经完成的硬化结果纳入正式基线，而不是只停留在单次开发提交。

**计划动作**

1. 记录 `4a8e5b9` 的三项硬化内容：
   - Yunka stub path 契约测试
   - `benefiturl` 语义说明硬化
   - H5 表单 `id/name` 第一轮补齐
2. 保留“硬化已完成，但语义风险未归零”的真实口径。

### P1：保留剩余风险

**目标**

- 防止对外联调前的口径过度乐观。

**计划动作**

1. 明确保留 `benefiturl` 的字段语义风险：
   - 当前 runtime source 更接近 QW exercise redirect URL
   - 不能自动扩写成“所有权益场景通用联登 URL”
2. 将 H5 剩余浏览器 `id/name` issue 继续作为 `P2` 质量项，而不是主链 blocker。
3. 将公网 Yunka `KJ_NOT_READY / kj.private-key` 和电子签章待定项继续隔离为外部阻塞。

## 3. 今日完成标准

如果达到以下结果，可认为当天计划已达标：

1. 本地主链已明确标记为稳定
2. 夜间回归旧 blocker 已全部回填到最终状态
3. 剩余风险只保留：
   - 外部联调阻塞
   - `benefiturl` 字段语义风险
   - H5 低优先级页面质量项

## 4. 风险提示

1. 当前 `benefiturl` 的真实来源仍是 QW exercise redirect URL，这只是“当前 runtime source 已明确”，不等于协议语义已被三方完全确认。
2. 公网 Yunka 的真实阻塞依然是 `KJ_NOT_READY / kj.private-key 配置无效`，不能用本地全绿掩盖。
3. H5 这轮只是做了关键页面第一轮 `id/name` 补齐，未来新增原生控件仍可能再引入浏览器 issue。

## 5. 一句话判断

> `2026-05-01` 的本地联调工作已经从“修主链”转成“固定稳定基线并收口剩余风险”；下一步更重要的是准备对外联调输入，而不是继续在本地重复证明已经通过的主链。
