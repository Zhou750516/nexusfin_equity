# 2026-04-24 工作计划

> 今日主线：承接 `2026-04-23` 已完成的日志专项收口与会议纪要，优先输出“用信新链路改造方案”“云卡状态查询与 H5 状态映射方案”“H5 自动埋点方案”，并同步重排 `004-benefit-loan-flow` 未完成任务，避免在新边界未定前直接进入高返工代码改造。

## 1. 今日目标

基于 `2026-04-23` 已完成结果：

1. 异步补偿日志链路已经收口，当前后端主线具备稳定快照
2. `plan/20260423_小花云卡齐为联动会议纪要与行动计划.md` 已沉淀一批新的流程结论与后续行动项
3. `specs/004-benefit-loan-flow/tasks.md` 仍有 `T013-T033` 未完成，但其中部分任务已受到会议新结论影响

因此 `2026-04-24` 的重点是：

1. 输出“用信新链路改造方案”，把“云卡先建放款订单、权益成功后再放款”链路讲清楚
2. 输出“云卡状态查询与 H5 状态映射方案”，区分云卡状态与小花结果
3. 输出“H5 自动埋点方案”，先把页面、事件、上报口径和边界固定
4. 把 `004-benefit-loan-flow` 未完成任务按“可继续 / 需改写 / 待外部接口”重新分类
5. 不直接启动真实接口大改，先把方案、边界和任务顺序重新收口

### 当前已产出

1. `third-part/科技平台/科技平台-小花接口文档-艾博生4.22_整理分析.md`
   - 已完成小花 `4.22` 文档 Markdown 化与方向修正
2. `plan/20260424_小花4.22现有实现差异分析与开发计划.md`
   - 已完成当前代码与小花 `4.22` 的差异盘点
3. `plan/20260424_小花4.22开发任务拆解.md`
   - 已完成面向后续开发的任务分组与分配建议

## 2. 今日优先级

### P0：输出用信新链路改造方案

**目标**

- 把原“艾博生直接提交流程”改造成“云卡先建单、权益 / 签约完成后再触发放款”的正式方案口径

**计划动作**

1. 对照 `plan/20260423_小花云卡齐为联动会议纪要与行动计划.md` 梳理旧链路与新链路差异
2. 明确新链路关键节点：
   - 云卡创建放款订单
   - 艾博生完成权益购买
   - 齐为短信签约
   - 审批 loading 承接
   - 云卡触发放款
   - 结果查询与异常分支
3. 明确本地需要新增或调整的核心标识：
   - 云卡放款订单号
   - 本地权益订单号
   - 签约状态
   - 放款状态
4. 标注失败 / 超时 / 放弃权益 / 关闭订单的触发点
5. 输出一版后端视角流程方案文档

### P0：输出云卡状态查询与 H5 状态映射方案

**目标**

- 让审批中、审批结果、拒绝落地、行权分发等页面的状态来源清晰，不再把云卡状态和小花状态混在一起

**计划动作**

1. 基于会议纪要整理建议状态枚举：
   - 订单已创建
   - 待权益购买
   - 待签约
   - 待放款
   - 放款中
   - 成功
   - 拒绝
   - 关闭
2. 明确每个状态对应的 H5 页面或页面阶段
3. 明确 `approval-status`、`approval-result`、分发页上下文接口需要返回的字段
4. 标注哪些字段由艾博生本地维护，哪些必须等待云卡接口
5. 输出一版“状态映射 + 接口建议”文档

### P0：输出 H5 自动埋点方案

**目标**

- 在不启动真实埋点开发的前提下，先固定页面、事件、参数、隐私边界和失败处理策略

**计划动作**

1. 梳理关键页面：
   - 试算页
   - 权益页
   - 签约页
   - loading 页
   - 审批结果页
   - 拒绝落地页
   - 行权分发页
   - 还款页
2. 梳理关键事件：
   - 页面曝光
   - 金额调整
   - 确认借款
   - 发短信
   - 确认签约
   - 放弃权益
   - 轮询状态
   - 结果页到达
3. 明确事件命名、公共字段、业务主键、脱敏与隐私边界
4. 明确上报失败重试和降级策略
5. 输出一版埋点方案文档

### P1：重排 `004-benefit-loan-flow` 未完成任务

**目标**

- 让 `T013-T033` 不再只是“未完成清单”，而是变成可执行的下一阶段任务入口

**计划动作**

1. 逐项复核 `specs/004-benefit-loan-flow/tasks.md` 的 `T013-T033`
2. 将任务标记为三类：
   - 可继续按原计划推进
   - 需要依据 `2026-04-23` 新结论改写
   - 依赖云卡 / 小花 / 法大大接口，暂缓开发
3. 优先识别受影响最大的任务：
   - 借款确认与审批状态相关任务
   - 协议阅读 / 签约 / loading 相关任务
   - 拒绝落地页 / 放弃权益 / 分发页相关任务
4. 输出一版“任务重排建议”

### P1：控制本轮边界

**目标**

- 确保今天的产出是“方案收口 + 任务重排”，而不是再次跳进高返工实现

**计划动作**

1. 不直接改造 `/api/loan/apply` 真实主链路
2. 不抢跑云卡尚未定版的状态机接口开发
3. 不抢跑法大大真实签章接入
4. 不把 H5 自动埋点直接写成代码实现
5. 如确需起草实现任务，只停留在文档和任务拆解层

## 3. 今日完成标准

如果 `2026-04-24` 结束前达到以下结果，可认为当天达标：

1. 用信新链路改造方案已形成可讨论版本
2. 云卡状态查询与 H5 状态映射方案已形成可讨论版本
3. H5 自动埋点方案已形成可讨论版本
4. `004-benefit-loan-flow` 的 `T013-T033` 已完成重新分类
5. 已明确哪些事项可以继续开发，哪些必须等待外部接口
6. 今天没有误启动高返工风险代码改造

## 4. 风险提示

1. 若不先改写主链路方案，后续继续按旧链路推进会产生明显返工
2. 若不区分云卡状态与小花结果，审批中 / 审批结果页面会继续混乱
3. 若不先做任务重排，`004` 的未完成任务会越来越难判断是否还能直接执行
4. 若今天直接进入真实代码实现，极易在云卡 / 小花 / 法大大外部口径未定前做出错误边界

## 5. 一句话判断

> `2026-04-24` 的关键不是马上继续写代码，而是先把新链路、新状态、新页面责任和 `004` 剩余任务顺序重新固定下来，让后续实现有稳定边界。
