# 2026-04-28 工作计划

> 今日主线：先完成 `feature/h5-api` 与 `feature/joint-login` 的收口，再把 `2026-04-27` 未真正执行完的计划项并入当天主线，最终切到 `20260427_重构降摩擦.md` 的 `Phase 1A ~ 2E` 执行与收口。

## 执行结果补记（2026-04-29）

这份文档对应的 **“2026-04-28 当天计划”已执行完成**，但要区分两个层级：

1. **这份日计划已完成**
   - 双分支收口已完成
   - `2026-04-27` 未真正收口的事项已重新归类并部分补齐
   - `Phase 1A ~ 2E` 已完成计划、实现与验证闭环
   - 后续阶段顺序已固定
2. **“重构降摩擦”总工程并不因为这份日计划完成就自动结束**
   - 后续剩余阶段原本为 `Phase 0 / 5 / 3 / 4`
   - 截至 `2026-04-29`，这些阶段也已继续推进并完成

因此，对本文件的正确理解是：

- 它是一份 **已完成的日计划记录**
- 不是整个项目后续所有工作的唯一终局说明
- 后续承接以 `docs/plan/CURRENT_STATE.md` 和 `docs/plan/20260429.md` 为准

### 2026-04-29 事后复核

`2026-04-29` 已额外完成两件事：

1. 决定补跑 `MySqlAsyncCompensationIntegrationTest`
2. 决定提交本轮文档收口改动

补跑结果的最终结论是：

- `2026-04-29` 首轮补跑时，`MySqlAsyncCompensationIntegrationTest` 曾暴露 QW Spring 装配回归
- 直接原因已定位为 `AllinpayDirectQwBenefitClient` duplicated `@Autowired` constructors
- 该问题随后已由 `32722e7` 修复
- 当前 `MySqlAsyncCompensationIntegrationTest` 已恢复通过

因此，这份日计划现在可以无保留地视为 **已完成**：

- `2026-04-28` 当天计划目标已完成
- 事后暴露的新问题已在 `2026-04-29` 完成修复和回归验证
- 不应再把该历史缺陷视为当前阻塞

## 0. 对 2026-04-27 计划的承接说明

`docs/plan/20260427.md` 当前并不在根目录保留，实际存在的是归档版：

- `docs/plan/archive/202604/20260427.md`

对照这份归档计划，`2026-04-27` 原本希望推进的 4 类事项里，实际完成度如下：

1. **H5 前后端接口第一批实施顺序**
   - 已部分承接。
   - 当天后续实际落地成了：
     - `purpose` 字段前后端闭环
     - H5/API 与联合登录双线拆分推进
   - 但并没有在 `CURRENT_STATE.md` 或统一入口里彻底沉淀成长期稳定口径。
2. **补齐 `CURRENT_STATE.md` 的真实口径**
   - 已在当天后续补齐。
   - 当前已可作为开工先读的单点入口使用。
3. **稳定 `docs/plan` 新治理方式**
   - 部分完成。
   - 目录结构、archive 和 topics 已落下，但“根目录保留什么、如何持续维护”的执行仍未完全闭环。
4. **确认 H5 本地联调边界**
   - 部分完成。
   - 联调入口、mock 与真实接口边界已有阶段性结论，但没有作为当天主线最终收口。

因此，`2026-04-28` 不应只记录“双分支合并”，还需要把上述未完成项并入当天主线统一处理。

## 1. 今日目标

基于当前已确认事实：

1. `feature/h5-api` 与 `feature/joint-login` 已分别完成各自主实现；
2. 双分支在 `2026-04-27` 已完成灰区 owner 划分与合并顺序约定；
3. `2026-04-27` 的部分计划项已被当天继续承接，其中：
   - `CURRENT_STATE.md` 已补齐
   - `docs/plan` 新治理方式仍待后续阶段继续稳定
   - H5 联调边界仍缺统一入口沉淀
4. `docs/plan/20260427_重构降摩擦.md` 已成为当天后续高 ROI 主线。

因此 `2026-04-28` 的重点实际分为三段：

1. 完成双分支验证、合并、推送和清理
2. 接住 `2026-04-27` 未真正收口的文档治理与入口口径问题，并补齐 `CURRENT_STATE.md`
3. 推进“重构降摩擦” `Phase 1A ~ 2E`，把高摩擦后端结构真正拆开

## 2. 今日优先级

### P0：双分支收口

**目标**

- 把 `feature/h5-api` 与 `feature/joint-login` 从“各自完成”收口成“主线已验证、已合并、已清理”的交付态。

**计划动作**

1. 核对两个 worktree 的 `git status`
2. 核对灰区文件是否发生重叠修改
3. 分别执行分支级验证
4. 按顺序合并：
   - 先 `feature/h5-api`
   - 再 `feature/joint-login`
5. 在合并后的 `main` 上再做一次集成态验证
6. 完成推送、worktree 清理和本地分支清理

### P0：承接 2026-04-27 未收口项

**目标**

- 不让 `2026-04-27` 的计划目标在归档后失真，明确哪些已经被后续动作吸收，哪些仍待后续阶段真正处理。

**计划动作**

1. 明确 H5 前后端接口第一批实施顺序中，哪些已被 `purpose` 闭环与双分支推进吸收
2. 明确 `CURRENT_STATE.md` 已完成回填，不再保留占位口径
3. 明确 `docs/plan` 新治理方式已完成结构调整，但持续维护口径仍待 `Phase 0`
4. 明确 H5 本地联调边界已有阶段结论，但仍缺统一入口沉淀

### P0：重构降摩擦 Phase 1 / 2 落地

**目标**

- 不停留在高层方案，把后端高摩擦结构按阶段拆到可验证状态。

**计划动作**

1. 补齐并修订可执行计划：
   - `Phase 1A`
   - `Phase 1B`
   - `Phase 2A`
   - `Phase 2B`
   - `Phase 2C`
   - `Phase 2D`
   - `Phase 2E`
2. 通过主实现会话按任务串行推进
3. 由协调会话逐阶段复核 commit 边界与验证结果
4. 重点完成：
   - shared utils 抽取
   - Yunka call template 收口
   - `LoanServiceImpl` facade 化
   - `LoanApplicationGateway` 抽取
   - async compensation typed payload 收口

### P1：后续阶段准备

**目标**

- 在当天结束前，把剩余大阶段收敛成可连续执行的顺序，而不是留成松散想法。

**计划动作**

1. 固定后续顺序：
   - `Phase 0`
   - `Phase 5`
   - `Phase 3`
   - `Phase 4`
2. 形成可直接发给主实现会话的统一执行指令
3. 明确哪些事项今天不再继续假装完成：
   - `Phase 0`
   - `Phase 5`
   - `Phase 3`
   - `Phase 4`

## 3. 今日完成标准

如果 `2026-04-28` 结束前达到以下结果，可认为当天达标：

1. `feature/h5-api` 与 `feature/joint-login` 已完成验证、合并、清理
2. `2026-04-27` 未完成项已被明确归类并部分补齐：
   - 已被吸收
   - 仍待后续
3. `Phase 1A ~ 2E` 已形成计划、实现与验证闭环
4. 后续剩余阶段已有统一执行顺序与总指令
5. 当天文档口径能够真实反映“哪些做了，哪些没做”

## 4. 风险提示

1. 如果只记录“双分支已合并”，会掩盖 `2026-04-27` 计划中部分文档治理事项曾未完成、需在当天继续补齐的事实。
2. 如果在 `Phase 2` 拆分中顺手修改业务语义，会把结构重构放大成业务规则变更。
3. 如果当天不把剩余阶段顺序固定，后续又会退回“想到哪改到哪”的高摩擦节奏。

## 5. 一句话判断

> `2026-04-28` 的关键不只是把双分支收口，更是把 `2026-04-27` 未真正执行完的事项接住，并借此把“重构降摩擦”推进到 `Phase 1 / 2` 真正落地完成。
