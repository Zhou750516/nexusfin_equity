# 2026-04-28 工作计划

> 今日主线：把 `feature/h5-api` 与 `feature/joint-login` 两条已完成分支，从“各自完成”收口到“主线已验证、已合并、已推送、已清理”的交付态。

## 1. 今日目标

基于当前已确认事实：

1. `feature/h5-api` 与 `feature/joint-login` 已分别完成各自主实现；
2. 两条线在 2026-04-27 已完成灰区 owner 划分与合并顺序约定；
3. 当前最重要的问题已经不是“还能不能继续写”，而是“如何在不制造新冲突的前提下，完成最终验证与主线合并”。

因此 `2026-04-28` 的重点是：

1. 核对两条线是否真正完成，而不是口头完成
2. 分别执行每条线自己的最小必要验证
3. 按既定顺序合并到 `main`
4. 在合并后的 `main` 上执行一次集成态验证
5. 完成推送、分支清理和工作区清理
6. 回填昨天与今天的必要文档

## 2. 今日优先级

### P0：双分支完成度核对

**目标**

- 确认 `feature/h5-api` 与 `feature/joint-login` 不是“感觉完成”，而是已经具备真实收口条件。

**计划动作**

1. 核对两个 worktree 的 `git status`
2. 核对灰区文件是否发生重叠修改
3. 核对两条线的改动范围是否仍符合昨天的 owner 结论

### P0：分支级验证

**目标**

- 在 merge 之前先分别证明两条线各自能通过基本验证。

**计划动作**

1. H5/API 线执行：
   - `H5` 类型检查
   - 相关页面逻辑测试
   - Java 目标单测 / 集成测试
2. 联合登录线执行：
   - `H5` 类型检查
   - 联合登录相关前端测试
   - Java 联合登录与鉴权链路测试

### P0：主线合并与集成验证

**目标**

- 把“两个都过了”收口成“合在一起也过”。

**计划动作**

1. 先合 `feature/h5-api`
2. 再合 `feature/joint-login`
3. 在合并后的代码上补跑一次集成态验证：
   - H5 类型检查
   - 相关前端测试
   - Java 目标测试集

### P1：仓库清理与文档补记

**目标**

- 把今天的交付态做干净，不把“功能已合并，仓库仍很乱”留给后面的人。

**计划动作**

1. 推送 `main` 到远端
2. 清理两个已完成 worktree
3. 清理两个已合并的本地功能分支
4. 回填：
   - `docs/日报/20260427.md`
   - `docs/decision-review/DAILY_DECISION_20260427.md`
   - `docs/plan/20260427_checklist.md`
   - `docs/plan/20260428.md`
   - `docs/plan/20260428_checklist.md`

## 3. 今日完成标准

如果 `2026-04-28` 结束前达到以下结果，可认为当天达标：

1. `feature/h5-api` 与 `feature/joint-login` 都经过独立验证
2. 两条线已按顺序合并到 `main`
3. 合并后的 `main` 再次验证通过
4. `main` 已推送到远端
5. 两个 worktree 与本地功能分支已清理
6. 昨天与今天的必要文档已补齐

## 4. 风险提示

1. 如果只验证分支、不验证合并后的 `main`，极容易把冲突与耦合问题留到推送后暴露。
2. 如果在 `main` 仍保留大量本地脏状态，会模糊“哪些是交付物、哪些只是本地辅助文件”。
3. 如果跳过文档回填，本轮双线并行的边界管理经验很快会丢失，后续类似场景还会重复踩坑。

## 5. 一句话判断

> `2026-04-28` 的关键不是再增加功能，而是把“两条线都说自己做完了”收口成“主线已验证、已推送、仓库已清理”的真实完成态。
