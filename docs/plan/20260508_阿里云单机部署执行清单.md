# 2026-05-08 阿里云单机部署执行清单

## 0. 当前环境判断

### 0.1 结论

这份清单**有必要保留**，但它不是生产上线手册，而是 2026-05-08 阿里云单机真实联调的最小执行入口。

当前最小目标是：

1. 先把 `Yunka / 小花科技` 真实测试环境打通。
2. `QW` 继续保持 `MOCK`。
3. 科技平台业务服务不作为 ABS 默认业务下游。
4. 只有在当天确实需要验证 `SSO / /api/users/me` 这类 ABS 自身直连 auth 边界时，才按需启 `tech-user-stub` 或改指向真实 auth 边界。
4. H5 通过 Node production server 跑在 `3000`，由 nginx 挂载到 `https://www.aibosoftware.com/equity/`。
5. nginx 保留官网首页 `/` 静态站，`/api/` 转发后端，`/equity/` 转发 H5。

今天联调现场还要额外锁住两个真值：

1. Yunka 访问域名固定用 `https://www.yongcar.com.cn`，不要临时改成 IP。
2. 阿里云出口白名单 IP 当前是 `101.201.236.106`，这属于 Yunka 外部放通前置，不是 ABS 本地代码可替代的配置。

### 0.2 必要项

这些项和当前代码、脚本、联调目标一致，必须保留：

1. `Java 17`：后端是 Spring Boot 3.2.12。
2. `Maven`：`scripts/aliyun/start-backend.sh` 在缺少 jar 或强制重建时会执行 `mvn -q -DskipTests package`。
3. `Node.js 20+` 和 `pnpm`：H5 生产模式是 `pnpm build` 后 `pnpm start`。
4. `nginx`：对外暴露 `80/443`，将 `/api/` 转发到 `backend:8080`，将 `/equity/` 转发到 `H5:3000`，并保留官网首页 `/`。
5. `git`：用于拉取和更新当前仓库。
6. `MySQL 8.0`：后端必须连接 MySQL。本次按 ECS 本机 MySQL 执行，先检查，未安装再装。
7. `YUNKA_MODE=REST`：今天真实联调主线是 Yunka，不能继续跑默认 `MOCK`。
8. `QW_MODE=MOCK`：第一天不建议和 Yunka 同时切真。
9. `TECH_PLATFORM_API_ENABLED=false`：科技平台业务通知能力今天不进入 ABS 主链默认依赖。
10. `TECH_PLATFORM_BASE_URL` 仅属于 `SSO / /api/users/me` 这类 auth 直连边界，不属于 Yunka 主链必填项。

### 0.3 可选项

这些项按现场条件选择，不是第一天联调的硬前置：

1. `HTTPS`：本次域名明确为 `https://www.aibosoftware.com/`，必须启用；`AUTH_COOKIE_SECURE=true`。
2. `systemd`：当前仓库没有 systemd 服务文件，先用现有脚本和 pid 文件联调；稳定后再补守护进程。

### 0.4 暂不做项

这些项当前没有必要放进执行路径：

1. 不上 Docker：仓库没有 Dockerfile / docker-compose，今天引入会扩大部署变量。
2. 不把 `8080 / 3000 / 18080 / 3306` 对公网开放：只允许本机回环或内网访问。
3. 不接 RDS：本次 MySQL 固定为本机安装。
4. 不默认接真实科技平台：除非云卡明确存在必须直连边界。
5. 不默认切真实 QW：`benefiturl` 运行时来源仍是 QW exercise redirect URL，第一天同时切真会放大定位面。

---

## 1. 适用范围

这份清单只针对当前最小联调方案：

1. 一台阿里云 ECS。
2. 后端、H5 跑在同一台机器；`tech-user-stub` 只在需要验证 auth 直连边界时按需启动。
3. MySQL 固定使用 ECS 本机 MySQL；先检查是否已安装，未安装再安装。
4. 先联调 Yunka / 小花科技真实测试环境。
5. 不上 Docker。
6. QW 先保持 `MOCK`。
7. 科技平台业务服务不接进 ABS 默认主链；auth 直连边界如需验证，再单独决定是否启 `tech-user-stub`。
8. 公网访问域名为 `https://www.aibosoftware.com/`，艾博生 H5 入口为 `https://www.aibosoftware.com/equity/`。

默认操作系统按 `Alibaba Cloud Linux 3` 写。

---

## 2. 第一步：安装基础软件

先切到 `root`：

```bash
sudo -i
```

更新系统：

```bash
dnf makecache
dnf update -y
```

安装 Git、Java、Maven、Nginx、MySQL 客户端：

```bash
dnf install -y git java-17-openjdk java-17-openjdk-devel maven nginx mysql
```

MySQL Server 按“先检查，未安装再安装”的口径执行：

```bash
if rpm -q mysql-server >/dev/null 2>&1; then
  echo "mysql-server already installed"
else
  dnf install -y mysql-server
fi
```

安装 Node.js 20：

```bash
curl -fsSL https://rpm.nodesource.com/setup_20.x | bash -
dnf install -y nodejs
```

安装 pnpm：

```bash
npm install -g pnpm
```

检查版本：

```bash
java -version
mvn -version
node -v
pnpm -v
nginx -v
mysql --version
```

---

## 3. 第二步：准备 MySQL

### 3.1 本次数据库口径

本次固定使用 ECS 本机 MySQL，不接 RDS。

最终要准备：

1. 数据库：`nexusfin_equity`
2. 应用账号：`work`
3. 字符集：`utf8mb4`
4. 后端连接：`127.0.0.1:3306`
5. 当前现场密码已确认，但不直接写入仓库文档；部署时按现场机密值填入 `MYSQL_PASSWORD`

### 3.2 检查 MySQL Server 是否已安装

```bash
if rpm -q mysql-server >/dev/null 2>&1; then
  echo "mysql-server already installed"
else
  dnf install -y mysql-server
fi
```

确认命令可用：

```bash
mysqld --version
mysql --version
```

### 3.3 启动本机 MySQL

启动并设置开机自启：

```bash
systemctl enable mysqld
systemctl start mysqld
systemctl status mysqld --no-pager
```

如果是首次安装，先看临时 root 密码：

```bash
grep 'temporary password' /var/log/mysqld.log || true
```

如果你的 MySQL 已经完成初始化，可以直接继续下一步。

---

## 4. 第三步：进入已有代码目录

你已经把代码拉到：

```bash
cd ~/project/nexusfin_equity
```

如果要更新代码，直接在这个目录执行：

```bash
cd ~/project/nexusfin_equity
git pull
```

---

## 5. 第四步：创建数据库、work 账号、导入表

执行我们刚补好的脚本：

```bash
cd ~/project/nexusfin_equity
export MYSQL_ADMIN_PASSWORD='你的MySQL管理员密码'
export WORK_DB_PASSWORD='你要给work设置的密码'
./scripts/setup-aliyun-mysql.sh
```

如果你要手工确认，可以再跑：

```bash
mysql -h '<MYSQL_HOST>' -P '<MYSQL_PORT>' -u work -p -D nexusfin_equity -e "show tables;"
```

---

## 6. 第五步：准备运行环境变量

先在服务器上建一个本地环境文件：

```bash
cat >~/project/nexusfin_equity/.env.aliyun <<'EOF'
# app
export SERVER_PORT='8080'
export LOG_LEVEL_ROOT='INFO'
export LOG_LEVEL_APP='INFO'
export LOG_LEVEL_YUNKA='INFO'
export LOG_LEVEL_QW='INFO'

# mysql
export MYSQL_HOST='127.0.0.1'
export MYSQL_PORT='3306'
export MYSQL_DATABASE='nexusfin_equity'
export MYSQL_USERNAME='work'
export MYSQL_PASSWORD='按现场已确认的 work 密码填写，不要把明文提交回仓库'

# auth
export AUTH_COOKIE_NAME='NEXUSFIN_AUTH'
# 当前域名使用 https://www.aibosoftware.com/，所以这里必须是 true
export AUTH_COOKIE_SECURE='true'
export AUTH_JWT_SECRET='替换成你自己的jwt密钥'
export NEXUSFIN_APP_ID='abs-app'
export NEXUSFIN_APP_SECRET='替换成你自己的app密钥'

# yunka / xiaohua
export YUNKA_ENABLED='true'
export YUNKA_MODE='REST'
export YUNKA_BASE_URL='https://www.yongcar.com.cn'
export YUNKA_GATEWAY_PATH='/api/gateway/proxy'
export YUNKA_CONNECT_TIMEOUT_MS='3000'
export YUNKA_READ_TIMEOUT_MS='5000'
export YUNKA_APP_ID='ABS_YK_UAT_20260511'
export YUNKA_APP_SECRET='按现场已确认值填写，不要把明文提交回仓库'

export YUNKA_LOAN_CALCULATE_PATH='/loan/trial'
export YUNKA_LOAN_QUERY_PATH='/loan/query'
export YUNKA_LOAN_APPLY_PATH='/loan/apply'
export YUNKA_REPAY_TRIAL_PATH='/repay/trial'
export YUNKA_REPAY_APPLY_PATH='/repay/apply'
export YUNKA_REPAY_QUERY_PATH='/repay/query'
export YUNKA_PROTOCOL_QUERY_PATH='/protocol/queryProtocolAggregationLink'
export YUNKA_USER_TOKEN_PATH='/user/token'
export YUNKA_USER_QUERY_PATH='/user/query'
export YUNKA_CARD_SMS_SEND_PATH='/card/smsSend'
export YUNKA_CARD_SMS_CONFIRM_PATH='/card/smsConfirm'
export YUNKA_BENEFIT_SYNC_PATH='/benefit/sync'

# qw
export QW_ENABLED='true'
export QW_MODE='MOCK'
export QW_BASE_URL='https://t-api.test.qweimobile.com'
export QW_METHOD_PATH='/api/abs/method'
export QW_PARTNER_NO='abs'
export QW_VERSION='v1.0'
export QW_SIGN_KEY='mock-only'
export QW_AES_KEY_BASE64='MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY='
export QW_CONNECT_TIMEOUT_MS='3000'
export QW_READ_TIMEOUT_MS='5000'

# tech-platform business capability
# 今天不把科技平台业务服务作为 ABS 默认主链下游，所以这里先明确关掉
export TECH_PLATFORM_API_ENABLED='false'
export TECH_PLATFORM_API_MODE='MOCK'

# tech-platform auth boundary
# 仅用于 SSO / /api/users/me 这类 ABS 自身直连 auth 边界
# 如果今天不测这条边界，可以先保留本地 stub 值但不启动 tech-user-stub
export TECH_PLATFORM_BASE_URL='http://127.0.0.1:18080'
export TECH_PLATFORM_USER_ME_PATH='/api/users/me'
export TECH_PLATFORM_CONNECT_TIMEOUT_MS='2000'
export TECH_PLATFORM_READ_TIMEOUT_MS='3000'
export TECH_PLATFORM_RETRY_MAX_ATTEMPTS='2'

# optional
# 只有今天确实要测 auth 直连边界时，才改成 1 启动 tech-user-stub
export START_TECH_USER_STUB='0'
EOF
```

生效：

```bash
cd ~/project/nexusfin_equity
source .env.aliyun
```

检查关键变量：

```bash
env | grep -E 'MYSQL_|YUNKA_|QW_MODE|TECH_PLATFORM_API_|TECH_PLATFORM_BASE_URL|START_TECH_USER_STUB|AUTH_COOKIE_SECURE|AUTH_JWT_SECRET|NEXUSFIN_APP_SECRET'
```

必须确认：

1. `YUNKA_MODE=REST`
2. `YUNKA_BASE_URL=https://www.yongcar.com.cn`，不是 `https://yunka.local`，也不是任何 IP 或本地 stub 地址。
3. `YUNKA_APP_ID=ABS_YK_UAT_20260511`
4. `YUNKA_APP_SECRET` 已按现场值填写，但不写回仓库
5. `QW_MODE=MOCK`
6. `TECH_PLATFORM_API_ENABLED=false`
7. `TECH_PLATFORM_BASE_URL` 只有在你今天需要测 auth 直连边界时才重要
8. `START_TECH_USER_STUB=0`（除非你今天要测 SSO / `/api/users/me`）
9. 当前走 `https://www.aibosoftware.com/`，所以 `AUTH_COOKIE_SECURE=true`
10. Yunka 已对白名单 IP `101.201.236.106` 放通；如果这项未生效，ABS 再怎么改本地配置也打不通真实联调

---

## 7. 第六步：一键启动联调环境

在代码目录执行：

```bash
cd ~/project/nexusfin_equity
source .env.aliyun
./scripts/aliyun/start-all.sh
```

这条命令会顺序启动：

1. backend，端口 `8080`
2. H5，端口 `3000`
3. `tech-user-stub`，端口 `18080`，仅在 `START_TECH_USER_STUB=1` 时启动

脚本行为说明：

1. backend 如果找不到 jar，会自动执行 `mvn -q -DskipTests package`。
2. H5 如果没有依赖，会自动执行 `pnpm install`。
3. H5 如果没有生产构建，会自动执行 `pnpm build`。
4. H5 生产服务通过 `pnpm start` 启动，监听 `3000`。

运行日志默认在：

```text
~/project/nexusfin_equity/runtime/aliyun/
```

查看日志：

```bash
tail -f ~/project/nexusfin_equity/runtime/aliyun/backend.log
tail -f ~/project/nexusfin_equity/runtime/aliyun/h5.log
```

联调现场如果想一条命令同时看 nginx / H5 / backend，直接用仓库里的脚本：

```bash
cd ~/project/nexusfin_equity
./scripts/watch-logs.sh
./scripts/watch-logs.sh all
./scripts/watch-logs.sh backend
```

如果今天还要看 auth 直连边界对应的本地 stub，再单独开：

```bash
tail -f ~/project/nexusfin_equity/runtime/aliyun/tech-user-stub.log
```

如果要停止：

```bash
cd ~/project/nexusfin_equity
./scripts/aliyun/stop-all.sh
```

如果要强制重新打包后端和 H5：

```bash
cd ~/project/nexusfin_equity
source .env.aliyun
FORCE_REBUILD=1 ./scripts/aliyun/start-all.sh
```

---

## 8. 第七步：检查端口

先确认核心 2 个进程都真的起来了：

```bash
ss -lntp | grep -E ':18080|:8080|:3000'
```

期望看到：

1. `8080`：backend
2. `3000`：H5
3. `18080`：tech-user-stub，仅在 `START_TECH_USER_STUB=1` 时需要

---

## 9. 第八步：检查后端启动真值

先看 backend 日志里的关键字：

```bash
grep -E "yunka gateway client initialized|qw benefit client routing initialized|Started NexusfinEquityApplication" ~/project/nexusfin_equity/runtime/aliyun/backend.log
```

你要重点确认：

1. `yunka gateway client initialized enabled=true mode=REST`
2. `baseUrl=云卡真实测试地址`
3. `gatewayPath=/api/gateway/proxy`
4. `yunka_app_id=ABS_YK_UAT_20260511`
5. `qw benefit client routing initialized mode=MOCK`
6. `Started NexusfinEquityApplication`

同时看 `./scripts/aliyun/start-backend.sh` 的启动输出，确认：

1. `tech_platform_api_enabled=false`
2. `tech_platform_api_mode=MOCK`
3. `yunka_app_id=ABS_YK_UAT_20260511`
4. `tech_platform_auth_base_url` 只有在你今天要测 auth 直连边界时才需要继续关注

如果这里不对，先别往下跑页面。

---

## 10. 第九步：配置 nginx

### 10.1 推荐：执行脚本自动应用

当前仓库已提供 nginx 配置应用脚本：

```bash
cd ~/project/nexusfin_equity
./scripts/aliyun/apply-nginx-aibosoftware.sh
```

脚本会自动执行：

1. 备份 `/etc/nginx/nginx.conf`
2. 写入 `www.aibosoftware.com` 的 nginx 配置
3. 执行 `sudo nginx -t`
4. 检查通过后执行 `sudo systemctl reload nginx`
5. 如果 `nginx -t` 失败，会回滚到备份配置

### 10.2 手工复制样板

复制样板配置：

```bash
cp ~/project/nexusfin_equity/deploy/nginx/nexusfin-equity-single-server.conf /etc/nginx/conf.d/nexusfin-equity.conf
```

把配置里的：

```nginx
server_name _;
```

改成当前域名：

```nginx
server_name www.aibosoftware.com;
```

检查配置：

```bash
nginx -t
```

启动并设置开机自启：

```bash
systemctl enable nginx
systemctl restart nginx
systemctl status nginx --no-pager
```

nginx 默认会把：

1. `/api/` 转发到 `127.0.0.1:8080`
2. `/equity/` 转发到 `127.0.0.1:3000`
3. `/` 继续保留官网静态首页 `/usr/share/nginx/html`

当前按 `https://www.aibosoftware.com/` 联调，必须启用 443 server block，并保留：

1. `/api/` -> `127.0.0.1:8080`
2. `/equity/` -> `127.0.0.1:3000`
3. `/equity` -> `301 /equity/`
4. `X-Forwarded-Proto https`

---

## 11. 第十步：放行安全组

阿里云安全组至少放行：

1. `22`
2. `80`
3. `443`

不要直接对公网放：

1. `3000`
2. `8080`
3. `18080`
4. `3306`

---

## 12. 第十一步：最小 smoke

### 12.1 健康检查

先查后端直连：

```bash
curl -i http://127.0.0.1:8080/api/equity/health
```

再查 nginx 后路径：

```bash
curl -i http://127.0.0.1/
curl -i http://127.0.0.1/api/equity/health
curl -i http://127.0.0.1/equity/
```

检查当前公网域名：

```bash
curl -i https://www.aibosoftware.com/
curl -i https://www.aibosoftware.com/equity/
curl -i https://www.aibosoftware.com/api/equity/health
```

### 12.2 tech-user-stub 自检（仅在今天要测 auth 直连边界时执行）

```bash
curl -i http://127.0.0.1:18080/api/users/me -H 'Authorization: Bearer mock-tech-token'
```

### 12.3 联调前 Cookie 准备（仅在今天要测 auth 直连边界时执行）

当前域名是 HTTPS：

```bash
curl -i -c /tmp/nexusfin_auth.txt "https://www.aibosoftware.com/api/auth/sso-callback?token=mock-tech-token"
curl -i -b /tmp/nexusfin_auth.txt "https://www.aibosoftware.com/api/users/me"
```

判断 token 是否有效时，按今天口径执行：

1. 不根据 JWT `exp` 字段先做本地预判。
2. 以 `POST /api/auth/joint-login` 或 `GET /api/auth/sso-callback` 的真实响应、页面落点、backend 日志为准。
3. 只有 Yunka 真实返回明确 token 无效 / session 失效时，才把问题归类为 token 失效。

### 12.4 核心链路顺序

环境确认后，再按下面顺序跑：

1. `POST /api/auth/joint-login`
2. `POST /api/loan/calculate`
3. `GET /api/benefits/card-detail`
4. `POST /api/benefits/activate`
5. `GET /api/repayment/info/{loanId}`

每条链路至少留证：

1. 请求时间
2. 请求 URL
3. HTTP 状态码
4. response body
5. backend 日志里的 `traceId`
6. Yunka 出站日志里的 `path / requestId / bizOrderNo / requestBodyJson / responseBodyJson / elapsedMs`

---

## 13. 常用排障命令

### 13.1 看 backend 最近 200 行日志

```bash
tail -n 200 ~/project/nexusfin_equity/runtime/aliyun/backend.log
```

### 13.2 看 H5 最近 100 行日志

```bash
tail -n 100 ~/project/nexusfin_equity/runtime/aliyun/h5.log
```

### 13.3 看 tech-user-stub 日志（仅在 `START_TECH_USER_STUB=1` 时需要）

```bash
tail -n 100 ~/project/nexusfin_equity/runtime/aliyun/tech-user-stub.log
```

### 13.4 看 nginx 错误日志

```bash
tail -n 100 /var/log/nginx/error.log
```

### 13.5 看端口

```bash
ss -lntp | grep -E ':18080|:8080|:3000|:80|:443'
```

### 13.6 重启整套服务

```bash
cd ~/project/nexusfin_equity
./scripts/aliyun/stop-all.sh
source .env.aliyun
./scripts/aliyun/start-all.sh
```

### 13.7 判定环境没配对

看到下面任一现象，先按环境问题处理：

1. backend 日志里 `mode=MOCK`，说明 `YUNKA_MODE` 没生效。
2. backend 日志里 `baseUrl=https://yunka.local`，说明 `YUNKA_BASE_URL` 没生效。
3. backend 启动脚本提示缺少 `YUNKA_BASE_URL / YUNKA_APP_ID / YUNKA_APP_SECRET`，说明 `.env.aliyun` 没 source 或变量没填。
4. 通过 `https://www.aibosoftware.com/` 登录后 cookie 不写入，检查 `AUTH_COOKIE_SECURE=true` 和 443 nginx 配置。
5. nginx 返回 502，先查 `ss -lntp` 确认 `8080 / 3000` 是否监听。
6. `/api/users/me` 401，只在你今天确实在测 auth 直连边界时再检查 `tech-user-stub` / `TECH_PLATFORM_BASE_URL`；否则这条不属于 Yunka 主链阻塞。

---

## 14. 当前联调口径提醒

这次单机阿里云环境，继续按下面口径执行：

1. 访问域名是 `https://www.aibosoftware.com/`。
2. 官网首页 `/` 继续保留 nginx 静态站。
3. 艾博生 H5 入口是 `https://www.aibosoftware.com/equity/`。
4. 后端 API 入口是 `https://www.aibosoftware.com/api/`。
5. MySQL 使用本机安装版本，先检查，未安装再装。
6. 先联调 Yunka。
7. `YUNKA_MODE=REST`。
8. `QW_MODE=MOCK`。
9. 科技平台业务服务不作为 ABS 默认主链下游。
10. `TECH_PLATFORM_API_ENABLED=false`。
11. `TECH_PLATFORM_BASE_URL` 仅属于 auth 直连边界，不属于 Yunka 主链必填项。
12. `benefiturl` 当前真实来源仍是 QW exercise redirect URL。
13. 不要把这套环境误说成“所有外部系统都切真”。
14. token 是否可用，以 ABS 调 Yunka 后的真实返回为准，不按本地解析 JWT `exp` 预判。
