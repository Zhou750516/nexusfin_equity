# KJ 未就绪阶段 B-02 至 B-06 云服务器测试记录

## 1. 测试概况

- 测试日期：`2026-04-15`
- 测试环境：云服务器 `101.201.236.106`
- 测试入口：`http://101.201.236.106/api/gateway/proxy`
- 管理端查询入口：
  - `http://101.201.236.106/api/admin/logs/gateway-api`
  - `http://101.201.236.106/api/admin/logs/sys-audit`
- 测试范围：`KJ未就绪阶段测试计划.md` 中阶段 B 的 `B-02` 至 `B-06`
- 执行方式：本地通过 `curl --noproxy '*'` 访问云服务器公网 HTTP 地址

## 2. 前置检查

### 2.1 字典接口

请求：

```bash
curl --noproxy '*' -sS -m 15 http://101.201.236.106/api/admin/logs/dictionaries
```

结果：

- `success=true`
- `code=0`
- 返回时间：`2026-04-15T15:50:55.707781252`
- 结论：管理端基础字典接口正常

### 2.2 网关日志分页

请求：

```bash
curl --noproxy '*' -sS -m 15 "http://101.201.236.106/api/admin/logs/gateway-api?pageNo=1&pageSize=5"
```

结果：

- `success=true`
- `code=0`
- 返回时间：`2026-04-15T15:50:55.792948964`
- 当时数据量：`total=0`
- 结论：管理端网关日志分页接口正常

## 3. B-02：缺少 X-Trace-Id

### 3.1 请求

```bash
curl --noproxy '*' -sS -m 20 -i -X POST "http://101.201.236.106/api/gateway/proxy" \
  -H "Content-Type: application/json" \
  -H "X-Request-Id: REQ_NO_KJ_B02_REMOTE_001" \
  -H "X-Timestamp: 2026-04-15T10:40:00+08:00" \
  -H "X-Channel-Code: ABS" \
  -H "X-Signature: TEST_SIGNATURE" \
  -d '{"requestId":"REQ_NO_KJ_B02_REMOTE_001","path":"/loan/query","bizOrderNo":"BO_NO_KJ_B02_REMOTE_001","data":{"loanId":"LN_NO_KJ_B02_REMOTE_001"}}'
```

### 3.2 实际返回

HTTP 状态：

```text
HTTP/1.1 200
```

响应体：

```json
{"code":"1000","message":"PARAM_ERROR","requestId":"REQ_NO_KJ_B02_REMOTE_001","data":{"status":"UNKNOWN","providerMessage":"X-Trace-Id不能为空","retryable":false,"errorType":"PARAM_ERROR","payload":null}}
```

响应头关键值：

```text
X-Trace-Id: TRACE-F7F42C6782CF4CA890D7436BB4A492AA
X-Request-Id: REQ_NO_KJ_B02_REMOTE_001
```

### 3.3 日志回查

网关日志查询：

```text
requestId=REQ_NO_KJ_B02_REMOTE_001
total=0
records=[]
```

审计日志查询：

```text
requestId=REQ_NO_KJ_B02_REMOTE_001
total=0
records=[]
```

### 3.4 结论

- 结果符合预期。
- 缺少 `X-Trace-Id` 时返回统一参数错误：`code=1000`、`message=PARAM_ERROR`。
- 该请求在 Controller 参数校验阶段被拦截，未进入网关业务处理，因此未落 `gateway_api_log` / `sys_audit_log`。

## 4. B-03：Header / Body 请求号不一致

### 4.1 请求

```bash
curl --noproxy '*' -sS -m 20 -i -X POST "http://101.201.236.106/api/gateway/proxy" \
  -H "Content-Type: application/json" \
  -H "X-Trace-Id: TRACE_NO_KJ_B03_REMOTE_001" \
  -H "X-Request-Id: REQ_NO_KJ_B03_REMOTE_HEAD_001" \
  -H "X-Timestamp: 2026-04-15T10:41:00+08:00" \
  -H "X-Channel-Code: ABS" \
  -H "X-Signature: TEST_SIGNATURE" \
  -d '{"requestId":"REQ_NO_KJ_B03_REMOTE_BODY_001","path":"/loan/query","bizOrderNo":"BO_NO_KJ_B03_REMOTE_001","data":{"loanId":"LN_NO_KJ_B03_REMOTE_001"}}'
```

### 4.2 实际返回

HTTP 状态：

```text
HTTP/1.1 200
```

响应体：

```json
{"code":"1000","message":"PARAM_ERROR","traceId":"TRACE_NO_KJ_B03_REMOTE_001","requestId":"REQ_NO_KJ_B03_REMOTE_BODY_001","data":{"status":"UNKNOWN","providerMessage":"Header与Body的requestId不一致","retryable":false,"errorType":"PARAM_ERROR","payload":null}}
```

响应头关键值：

```text
X-Trace-Id: TRACE_NO_KJ_B03_REMOTE_001
X-Request-Id: REQ_NO_KJ_B03_REMOTE_HEAD_001
```

### 4.3 日志回查

网关日志查询：

```text
requestId=REQ_NO_KJ_B03_REMOTE_BODY_001
total=0
records=[]
```

审计日志查询：

```text
requestId=REQ_NO_KJ_B03_REMOTE_BODY_001
total=0
records=[]
```

### 4.4 结论

- 结果符合预期。
- Header 与 Body 的 `requestId` 不一致时返回统一参数错误：`code=1000`、`message=PARAM_ERROR`。
- 该请求在网关业务参数校验阶段直接返回，未进入路由解析和下游调用，因此未落 `gateway_api_log` / `sys_audit_log`。

## 5. B-04：缺少 data

### 5.1 请求

```bash
curl --noproxy '*' -sS -m 20 -i -X POST "http://101.201.236.106/api/gateway/proxy" \
  -H "Content-Type: application/json" \
  -H "X-Trace-Id: TRACE_NO_KJ_B04_REMOTE_001" \
  -H "X-Request-Id: REQ_NO_KJ_B04_REMOTE_001" \
  -H "X-Timestamp: 2026-04-15T10:42:00+08:00" \
  -H "X-Channel-Code: ABS" \
  -H "X-Signature: TEST_SIGNATURE" \
  -d '{"requestId":"REQ_NO_KJ_B04_REMOTE_001","path":"/loan/query","bizOrderNo":"BO_NO_KJ_B04_REMOTE_001"}'
```

### 5.2 实际返回

HTTP 状态：

```text
HTTP/1.1 200
```

响应体：

```json
{"code":"1000","message":"PARAM_ERROR","traceId":"TRACE_NO_KJ_B04_REMOTE_001","requestId":"REQ_NO_KJ_B04_REMOTE_001","data":{"status":"UNKNOWN","providerMessage":"data:data不能为空","retryable":false,"errorType":"PARAM_ERROR","payload":null}}
```

### 5.3 日志回查

网关日志查询：

```text
requestId=REQ_NO_KJ_B04_REMOTE_001
total=0
records=[]
```

审计日志查询：

```text
requestId=REQ_NO_KJ_B04_REMOTE_001
total=0
records=[]
```

### 5.4 结论

- 结果符合预期。
- 缺少 `data` 时返回统一参数错误：`code=1000`、`message=PARAM_ERROR`。
- 该请求在请求体 Bean Validation 阶段被拦截，未进入网关业务处理，因此未落 `gateway_api_log` / `sys_audit_log`。

## 6. B-05：非白名单路径

### 6.1 请求

```bash
curl --noproxy '*' -sS -m 20 -i -X POST "http://101.201.236.106/api/gateway/proxy" \
  -H "Content-Type: application/json" \
  -H "X-Trace-Id: TRACE_NO_KJ_B05_REMOTE_001" \
  -H "X-Request-Id: REQ_NO_KJ_B05_REMOTE_001" \
  -H "X-Timestamp: 2026-04-15T10:43:00+08:00" \
  -H "X-Channel-Code: ABS" \
  -H "X-Signature: TEST_SIGNATURE" \
  -d '{"requestId":"REQ_NO_KJ_B05_REMOTE_001","path":"/unknown/path","bizOrderNo":"BO_NO_KJ_B05_REMOTE_001","data":{"loanId":"LN_NO_KJ_B05_REMOTE_001"}}'
```

### 6.2 实际返回

HTTP 状态：

```text
HTTP/1.1 200
```

响应体：

```json
{"code":"1001","message":"PATH_NOT_SUPPORTED","traceId":"TRACE_NO_KJ_B05_REMOTE_001","requestId":"REQ_NO_KJ_B05_REMOTE_001","data":{"status":"UNKNOWN","providerMessage":"path 不在允许白名单内: /unknown/path","retryable":false,"errorType":"PARAM_ERROR","payload":null}}
```

### 6.3 日志回查

网关日志查询：

```text
requestId=REQ_NO_KJ_B05_REMOTE_001
total=0
records=[]
```

审计日志查询：

```text
requestId=REQ_NO_KJ_B05_REMOTE_001
total=0
records=[]
```

### 6.4 结论

- 结果符合预期。
- 非白名单路径返回：`code=1001`、`message=PATH_NOT_SUPPORTED`。
- 该请求在路由白名单解析阶段被拒绝，尚未初始化网关流水，因此未落 `gateway_api_log` / `sys_audit_log`。

## 7. B-06：KJ 未定义路径

### 7.1 请求

```bash
curl --noproxy '*' -sS -m 20 -i -X POST "http://101.201.236.106/api/gateway/proxy" \
  -H "Content-Type: application/json" \
  -H "X-Trace-Id: TRACE_NO_KJ_B06_REMOTE_001" \
  -H "X-Request-Id: REQ_NO_KJ_B06_REMOTE_001" \
  -H "X-Timestamp: 2026-04-15T10:44:00+08:00" \
  -H "X-Channel-Code: ABS" \
  -H "X-Signature: TEST_SIGNATURE" \
  -d '{"requestId":"REQ_NO_KJ_B06_REMOTE_001","path":"/auth/token/verify","bizOrderNo":"BO_NO_KJ_B06_REMOTE_001","data":{"loanId":"LN_NO_KJ_B06_REMOTE_001"}}'
```

### 7.2 实际返回

HTTP 状态：

```text
HTTP/1.1 200
```

响应体：

```json
{"code":"1001","message":"PATH_NOT_SUPPORTED","traceId":"TRACE_NO_KJ_B06_REMOTE_001","requestId":"REQ_NO_KJ_B06_REMOTE_001","data":{"status":"UNKNOWN","providerMessage":"path 当前未启用: 科技平台未提供正式接口定义","retryable":false,"errorType":"PARAM_ERROR","payload":null}}
```

### 7.3 日志回查

网关日志查询：

```text
requestId=REQ_NO_KJ_B06_REMOTE_001
total=0
records=[]
```

审计日志查询：

```text
requestId=REQ_NO_KJ_B06_REMOTE_001
total=0
records=[]
```

### 7.4 结论

- 结果符合预期。
- KJ 尚未正式定义的路径返回：`code=1001`、`message=PATH_NOT_SUPPORTED`。
- 返回说明为：`path 当前未启用: 科技平台未提供正式接口定义`。
- 该请求在路由启用状态检查阶段被拒绝，尚未初始化网关流水，因此未落 `gateway_api_log` / `sys_audit_log`。

## 8. 汇总

| 用例 | 场景 | HTTP 状态 | 业务码 | 业务消息 | 网关日志 | 审计日志 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| B-02 | 缺少 `X-Trace-Id` | `200` | `1000` | `PARAM_ERROR` | `0` | `0` | 通过 |
| B-03 | Header / Body 请求号不一致 | `200` | `1000` | `PARAM_ERROR` | `0` | `0` | 通过 |
| B-04 | 缺少 `data` | `200` | `1000` | `PARAM_ERROR` | `0` | `0` | 通过 |
| B-05 | 非白名单路径 | `200` | `1001` | `PATH_NOT_SUPPORTED` | `0` | `0` | 通过 |
| B-06 | KJ 未定义路径 | `200` | `1001` | `PATH_NOT_SUPPORTED` | `0` | `0` | 通过 |

## 9. 总体结论

- `B-02` 至 `B-06` 均已在云服务器 `101.201.236.106` 实际执行完成。
- 所有用例均返回统一 JSON 响应，没有出现 Tomcat 默认错误页或未处理异常。
- 参数校验失败、请求号不一致、缺少 `data`、非白名单路径、KJ 未定义路径均能按预期返回明确错误。
- `B-02` 至 `B-06` 均未落 `gateway_api_log` / `sys_audit_log`，符合当前实现行为：这些请求都在进入真实网关转发处理前被拦截。
- 与 `B-01` 不同，`B-01` 已进入 KJ 出站请求处理并发生 KJ 参数异常，因此会落网关流水和审计日志；`B-02` 至 `B-06` 属于前置校验 / 路由拒绝场景。

