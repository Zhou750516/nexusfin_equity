# 2026-05-01 外部联调待确认问题清单

> 用于 `2026-05-07` 前对外发群确认。只保留当前尚未闭合、且会影响联调推进的问题；本地夜间自动化已经证明过的事项不再重复提问。

## 1. 给齐为的问题

1. 当前 `benefiturl` 我方运行态来源是 QW `getExerciseUrl` 返回的 exercise redirect URL，本轮联调是否可按该口径先作为权益同步字段使用？
2. `abs.push.order` 里 `cardNo` 与“续期 / 非首开”边界如何判定？文档中提到 `issue > 1`，但未见 `issue` 字段定义，请给明确口径。
3. `abs.sign.query / abs.sign.apply / abs.sign.confirm` 在失败场景下，外层 `code/msg` 与解密后 `data` 的真实结构能否各给一组样例？
4. `userSignId` 与 `cardNo` 是否存在一对多场景？同一签约是否允许多卡续期，请给明确限制。

## 2. 给云卡的问题

1. `POST /api/gateway/proxy` 是否仍是本轮正式联调统一入口？`path` 字段是否只接受相对路径，是否允许带 query string？
2. 借款试算真实 path 请最终确认是 `/loan/trial` 还是 `/loan/trail`，以哪份文档为准？
3. `X-Signature` 的正式签名算法、原串拼接方式、空值处理规则、测试环境密钥分发方式，请一次性给清楚。
4. `X-Channel-Code` 的正式取值是什么？测试和生产是否同值？
5. 当前 `KJ_NOT_READY / kj.private-key 配置无效` 预计何时解除？5 月 7 日联调窗口前是否可恢复可用？
6. 云卡侧异步通知的完整回调 URL、header、验签规则、失败重试策略，本轮请给联调口径。
7. 电子签章接入方案本轮是否已经定稿？如果未定稿，5 月 7 日联调范围内哪些链路先不验证？

## 3. 给科技平台的问题

1. 测试环境基础域名请给最终值；如借款、还款、回调不共域名，也请分开列清楚。
2. 借款试算接口真实 path 请确认；若平台文档仍写 `/loan/trail`，是否需要统一修正文档？
3. 联合登录相关能力里，测试环境是否已准备好真实 token 校验和用户信息查询接口？如已准备，请给 URL、鉴权方式、时效和失败码。
4. 回调通知的 header、签名、是否重试、最大重试次数、成功响应判定，请给最终联调口径。
5. 还款场景从科技平台跳转艾博生时，用户标识、借据标识、签名字段和时效规则分别是什么？
6. 若本轮权益同步报文中带 `benefiturl`，科技平台是否接受当前“后端生成、运行态来源为 QW exercise redirect URL”的字段语义？
