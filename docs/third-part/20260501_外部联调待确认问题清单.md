# 2026-05-01 外部联调待确认问题清单

> 用于 `2026-05-07` 前对外发群确认。只保留当前尚未闭合、且会影响联调推进的问题；本地夜间自动化已经证明过的事项不再重复提问。
>
> 当前默认边界：
>
> 1. 艾博生不直接感知科技平台服务
> 2. 科技平台相关服务细节由云卡统一收口
> 3. 除非存在直接 token / 回调 / 跳转边界，否则不向科技平台索要接口级细节

## 1. 给齐为的问题

1. 当前 `benefiturl` 我方运行态来源是 QW `getExerciseUrl` 返回的 exercise redirect URL，本轮联调是否可按该口径先作为权益同步字段使用？
2. `abs.push.order` 里 `cardNo` 与“续期 / 非首开”边界如何判定？文档中提到 `issue > 1`，但未见 `issue` 字段定义，请给明确口径。
3. `abs.sign.query / abs.sign.apply / abs.sign.confirm` 在失败场景下，外层 `code/msg` 与解密后 `data` 的真实结构能否各给一组样例？
4. `userSignId` 与 `cardNo` 是否存在一对多场景？同一签约是否允许多卡续期，请给明确限制。

## 2. 给云卡的问题

1. 借款试算真实 path 请最终确认是 `/loan/trial` 还是 `/loan/trail`，并明确以哪份文档为准。
2. 当前 `benefiturl` 运行态来源是 QW exercise redirect URL；该字段语义在云卡及其下游链路中是否可按当前口径受理，请给明确答复。
3. callback / 签名 / 重试最终口径请云卡统一回复：包括验签方式、必要 header、失败重试策略。
4. 当前 `KJ_NOT_READY / kj.private-key 配置无效` 预计何时解除？5 月 7 日联调窗口前是否可恢复可用？
5. 电子签章接入方案本轮是否已经定稿？如果未定稿，请明确 5 月 7 日联调先排除哪些链路。
6. 请明确当前是否存在“艾博生必须直接对接科技平台”的边界；如果存在，请只列出直接边界类型：
   - token 校验
   - 用户信息查询
   - 页面跳转
   - 回调通知

## 3. 给科技平台的问题

默认无单独问题。

仅当云卡明确告知“存在艾博生与科技平台的直接 token / 回调 / 跳转边界”时，才补发科技平台问题；且问题范围只限于该直接边界本身，不扩展到科技平台内部服务细节。
