# 2026-04-02 通联服务对接流程待补充说明

## 1. 目的

本文档用于记录后续需要补充的 **通联服务对接流程**，作为齐为联调问题收敛后的后续事项留痕。

## 2. 当前背景

截至 `2026-04-02`，已确认：

1. 齐为侧扣款所需的真实 `payProtocolNo`
2. 来自艾博生与第三方支付公司 **通联** 签约后获取的协议号
3. 一个用户对应一个协议号
4. 当前项目 **尚未对接通联**
5. 因此当前项目内并没有真实可用的支付协议号来源

## 3. 当前阶段结论

当前需要区分两件事：

### 3.1 短期联调方案

1. 等齐为提供一个 **mock 协议号**
2. 先用于继续推进齐为联调
3. 验证 `abs.push.order` 与 `abs.token.get` 是否能继续闭环

### 3.2 长期正式方案

1. 后续补充通联服务对接流程
2. 打通真实签约协议号获取能力
3. 让齐为 `payProtocolNo` 使用真实协议号，而不是内部占位值

## 4. 后续需要补充的内容

后续正式整理通联服务对接流程时，至少需要覆盖：

1. 通联签约触发时机
2. 通联签约接口 / 服务边界
3. 支付协议号获取方式
4. 协议号落库字段设计
5. 协议号在齐为 `abs.push.order` 中的使用方式
6. mock 协议号切换到真实协议号的上线策略

## 5. 当前关联事项

相关背景文档：

1. `docs/plan/20260402_齐为payProtocolNo待确认TODO.md`
2. `docs/plan/20260402_齐为跟进记录.md`
3. `docs/third-part/齐为/20260401_齐为线上部署说明.md`

## 6. 一句话记录

> 后续需要在 `docs/third-part` 下正式补齐“通联服务对接流程”，用于获取真实支付协议号并接入齐为链路。
