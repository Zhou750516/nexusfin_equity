# 2026-04-20 日报

## 1. 今日完成

1. 完成异步补偿自动调度运行口径第一轮细化，明确：
   - `workerId` 默认生成规则与显式配置建议
   - `heartbeat` 中 `IDLE / RUNNING` 的语义边界
   - `currentTaskId` 表示本次 tick 已认领并进入处理的任务
2. 完成自动调度启用说明沉淀，覆盖：
   - 默认关闭、显式开启、显式分片策略
   - 单实例启用配置示例
   - 双实例显式分片配置示例
   - 启用后的重点观察项
3. 完成自动调度启用后观察与人工介入 SOP，覆盖：
   - 启用前检查
   - 启用后第一观察点
   - backlog 不下降、失败增长、`DEAD` 增长等排查顺序
   - 人工重试、人工补单、人工关闭建议
4. 完成聚焦验证，确认本轮调度语义调整没有破坏现有主链路：
   - 调度 coordinator / worker / supervisor 相关测试通过
   - Spring Boot 启动回归通过
   - Phase 9 补偿主链路回归通过
5. 完成控制器级业务口径回归确认，继续保持：
   - 权益购买成功、云卡明确失败：返回 `loan_failed`，明确权益购买成功
   - 权益购买成功、云卡超时：返回 `pending`，前端按“已提交 / 审核中”承接
   - 权益购买成功、云卡受理成功：返回 `pending`，保留申请映射
6. 完成齐为、电子签章、小花科技阶段性 TODO review，并沉淀：
   - `plan/20260420_项目阶段性TODO清单.md`
   - `plan/20260420_项目阶段性TODO_checklist.md`

## 2. 今日结论

1. 自动调度已经从“第一轮可运行骨架”推进到“可启用、可观察、可人工介入”的说明阶段
2. 当前自动调度策略仍保持：
   - 默认关闭
   - 显式开启
   - 显式配置 `workerId`
   - 显式配置 `ownedPartitions`
3. `RUNNING + currentTaskId` 的口径已经收敛为“本次 tick 已认领具体任务”，避免误把空转 tick 解释成正在处理任务
4. 当前不扩展新的异步补偿范围，退款、绑卡、权益状态回推仍等待外部文档和后续方案确认
5. 项目阶段性 TODO 已形成统一入口，后续可按“外部确认 -> 本侧收口 -> 开发联调”持续消项

## 3. 风险与注意事项

1. 自动调度虽然已有启用说明，但仍不建议默认开启，避免污染联调环境
2. 当前 `currentTaskId` 是单次 tick 的瞬时观察值，不适合作为长任务监控的唯一依据
3. 多实例自动抢占、rebalance、长任务持续心跳仍未进入当前阶段
4. 齐为绑卡、退款、状态回推仍依赖齐为 / 科技平台正式文档
5. 小花联合登录真实协议、`partnerId` 来源、加解密 demo 仍依赖小花确认与我方后续评估
6. 法大大电子签章仍停留在“供应商切换已决策、正式接入文档待补”阶段

## 4. 明日建议

1. 优先补项目阶段性 TODO 的统一入口，方便后续每天从同一位置查看齐为、电子签章、小花科技待办
2. 继续跟进外部确认项：
   - 小花 `partnerId` 来源方案
   - 小花联合登录正式技术方案
   - 小花接口文档更新
   - 齐为绑卡、分发页、退款、状态回推正式文档
3. 若继续推进我方可控事项，建议优先做：
   - 小花加解密 demo 评估计划
   - 艾博生还款跳转页地址与参数口径设计
   - 法大大对接说明文档框架
4. 自动调度方向保持收口，不继续横向扩到退款 / 绑卡 / 状态回推补偿

## 5. 今日新增沉淀

1. `plan/20260420_自动调度运行口径与启用说明.md`
2. `plan/20260420_自动调度启用后观察与人工介入SOP.md`
3. `plan/20260420_项目阶段性TODO清单.md`
4. `plan/20260420_项目阶段性TODO_checklist.md`
5. `日报/20260420.md`
6. `decision-review/DAILY_DECISION_20260420.md`
7. `plan/20260421.md`
8. `plan/20260421_checklist.md`

## 6. 今日完成判断

对照 `plan/20260420.md` 既定目标，今日主线可判断为已完成：

1. 自动调度运行口径已完成第一轮细化 ✅
2. 自动调度启用说明与观察 SOP 已补齐 ✅
3. 控制器级业务口径已完成回归确认 ✅
4. 运维承接材料继续增强，且未扩到新的补偿业务范围 ✅
5. 项目阶段性 TODO 已形成统一清单与 checklist ✅
