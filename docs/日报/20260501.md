# 2026-05-01 日报

## 1. 今日完成

1. 联调边界正式收紧：
   - 明确“艾博生不直接感知科技平台服务”；
   - 科技平台相关服务细节由云卡统一收口；
   - 除非存在直接 `token / 回调 / 跳转` 边界，否则不向科技平台索要接口级细节。
2. 联调文档收口为当前可执行版本：
   - `docs/test/20260506_联调case设计方案.md` 已改成当前真实可执行版本；
   - case 已按 `COVERED / PENDING / N/A / BLOCKED` 重分类；
   - 当前只保留主决策会话、开发会话、测试会话三类协作角色。
3. 对外联调前文档已按新原则收紧：
   - `docs/test/20260501_对外联调前技术检查清单.md` 已明确只检查艾博生直接边界；
   - `docs/third-part/20260501_外部联调待确认问题清单.md` 已收紧为默认只追云卡、齐为；
   - 科技平台默认不是问题接收方，除非云卡先确认存在直连边界。
4. `benefiturl` 主链和运行态口径已收口：
   - `benefiturl` 已正式接入 `BenefitsServiceImpl.activate -> XiaohuaGatewayService.syncBenefitOrder`；
   - 已确认它当前真实来源仍是 `QW exercise redirect URL`；
   - 已完成“主链接上”和“语义仍有风险”两条口径的拆分管理。
5. 联调 case 覆盖矩阵已建立并开始按差量推进：
   - `docs/test/20260506_联调结果.md`、`docs/test/20260506_联调case覆盖矩阵.md` 已形成；
   - 当前已把 `§4` 主链 case 大部分收口；
   - `§5` 异常 case 已开始按真实缺口逐条推进。
6. `repayment submit` 超时通用 500 问题已修复并回归通过：
   - `/api/repayment/submit` 在 `/repay/apply timeout` 下不再落通用 `500 / Internal server error`；
   - 现在统一收口为受控业务错误 `YUNKA_UPSTREAM_TIMEOUT:Repayment submit temporarily unavailable`。
7. 还款线两个新真实问题也已闭合：
   - 超额还款现在会先用 `/repay/trial` 校验可还金额，超额时直接返回 `REPAYMENT_AMOUNT_EXCEEDED`，不会继续打 `/repay/apply`；
   - 重复还款现在有 `120` 秒短窗口防重，同一 `uid + loanId + amount` 的第二次提交会返回 `REPAYMENT_SUBMIT_DUPLICATED`，不会重复打 `/repay/apply`。
8. 还款线当前最关键的 3 个真问题都已补完运行态证据：
   - `EX-E.1` 超额还款；
   - `EX-E.3` 提交超时；
   - `EX-E.4` 重复还款；
   - 三条都已在运行态回归通过，并已回写联调结果文档和覆盖矩阵。

## 2. 今日结论

1. 联调口径已经从“默认下钻科技平台内部服务”收紧为“只看艾博生直接边界”，后续测试方向更清晰。
2. `benefiturl` 这条线目前可以对外说“主链接上了”，但不能说“字段语义已经完全无风险确认”。
3. `repayment submit` 当前最影响联调推进的 3 个真实 bug 都已关闭，后续可以继续推进剩余 `pending case`。
4. 当前最值得继续投入的方向，不再是回头刷已通过的还款异常，而是签约、借款、审批这几段剩余待测项。

## 3. 风险与注意事项

1. `benefiturl` 当前真实来源仍是 `QW exercise redirect URL`，这只是“当前 runtime source 已明确”，不等于外部三方已经完全接受其泛化语义。
2. 重复还款保护目前是“`120` 秒短窗口防重”，它对当前联调够用，但不是最终最强幂等方案。
3. `EX-A.3 token 篡改` 和 `EX-F.3` 这类 case，仍可能受 stub fault 注入能力限制，不能先入为主地当作本地 bug。
4. 后续测试仍应坚持：只测 FE <-> BE、ABS <-> Yunka、ABS <-> QW，以及被明确确认存在的直连边界；不默认扩到科技平台内部实现。

## 4. 明日建议

1. 测试会话继续推进 `docs/test/20260506_联调case设计方案.md` 中剩余 `pending case`：
   - `M-B.5 ~ M-B.7` 签约主链；
   - `M-B.9 ~ M-B.11` 借款/审批/还款完整主链；
   - `EX-C.*`、`EX-D.*`、`EX-A.3`、`EX-F.3`。
2. 每批 case 跑完都要继续更新：
   - `docs/test/20260506_联调结果.md`
   - `docs/test/20260506_联调case覆盖矩阵.md`
3. 如果再出现新的高价值真实 bug，继续按“先停当前批次、先修 bug、再继续扩测”的节奏处理。
4. 外部联调准备继续只追云卡和齐为；科技平台只有在云卡明确确认存在直连边界时才进入问题单。

## 5. 今日新增沉淀

1. `docs/日报/20260501.md`
2. `docs/decision-review/DAILY_DECISION_20260501.md`
3. `docs/plan/20260502.md`
4. `docs/plan/20260502_checklist.md`
5. `docs/test/20260506_联调结果.md`
6. `docs/test/20260506_联调case覆盖矩阵.md`

## 6. 今日完成判断

对照今天的真实推进，可判断为：

1. 联调边界收紧完成 ✅
2. `benefiturl` 主链接线与语义分层管理完成 ✅
3. 联调覆盖矩阵与差量推进机制建立完成 ✅
4. `repayment submit` 超时 500 修复完成 ✅
5. 超额还款拦截修复完成 ✅
6. 重复还款防重修复完成 ✅
7. 剩余 `pending case` 仍待继续推进 ⏳
8. `benefiturl` 字段语义风险仍待外部继续确认 ⏳
