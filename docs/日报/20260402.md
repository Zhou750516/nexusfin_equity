# 2026-04-02 日报

> 补充说明（`2026-04-03` 回填）：本文保留 `2026-04-02` 当天的真实推进过程，但其中“通联真实协议号”相关表述，当前仅代表阶段性假设与当时收敛口径，不再代表最终已拍板支付渠道。当前统一口径应为：**协议号管理结构有效，最终支付渠道待定。**

## 1. 面向技术负责人的汇报摘要

### 1.1 今日输入

基于 `2026-04-02` 当天的真实推进情况，今天的核心输入主要集中在以下四条：

1. 齐为对 `515 会员卡未生效` 的原因给出正式反馈：不是开卡未成功，而是首扣失败，且失败原因为 `payProtocolNo` 错误。
2. 齐为补充确认：
   - `openFlag=0` 表示开卡成功
   - `abs.token.get` 仍可继续只传 `uniqueId + partnerOrderNo`
   - `abs.lending.notify` 的 `status` 含义为 `0=失败，1=成功`
3. 齐为提供 mock 协议号 `AIP211926033187CF73483`，用于继续完成测试环境真实联调。
4. 用户明确要求：科技平台整条线先整体 `HOLD`，今天不再继续推进代码与联调细节。

### 1.2 今日完成情况

今天的重点已经从“继续猜测齐为为什么不通”切换为“把根因打实、把主流程补全、把上线边界说清楚”，主要完成如下：

1. 完成齐为问题根因收敛：
   - 明确 `515` 的本质不是卡未生效，而是首扣失败
   - 明确首扣失败原因是 `payProtocolNo` 错误
   - 明确 `agreementNo` 不能再被误用为真实支付协议号
2. 完成齐为三条主动接口的真实联调闭环验证：
   - `abs.push.order`
   - `abs.token.get`
   - `abs.lending.notify`
3. 完成 mock 协议号下的真实验证：
   - 使用齐为提供的 mock 协议号继续联调
   - `abs.token.get` 已返回可用 `token`
   - `abs.lending.notify` 已完成首轮真实联调与日志沉淀
4. 完成主流程第一轮代码改造落地：
   - 新增用户支付协议号模型与服务层
   - 下单主流程优先读取真实协议号
   - 订单写入协议号快照与来源快照
   - 增加测试 override 的环境边界保护
5. 完成真实协议号写入入口设计与实现：
   - 新增 `POST /api/callbacks/payment-protocol`
   - 为后续支付侧回传真实协议号预留正式入库入口
6. 完成文档沉淀与边界说明：
   - 输出真实 `payProtocolNo` 来源接入整体设计方案（当日按通联假设收敛）
   - 输出开发任务清单
   - 输出齐为主流程落地边界说明
   - 固定科技平台 `HOLD` 说明
7. 完成代码自检：
   - 相关测试已通过
   - `checkstyle` 已通过

### 1.3 技术判断

今天最重要的判断不是“齐为到底能不能调通”，而是“后续生产真正卡在哪里”。当前判断如下：

1. 齐为协议层和三条主动接口联调路径已经基本打通，当前主阻塞不在齐为接口实现本身。
2. 生产环境真正的核心阻塞已经收敛为：**尚未接入最终支付渠道的真实签约协议号来源**。
3. `payProtocolNo` 是用户级资产，不应继续挂靠在订单临时字段或内部 `agreementNo` 上混用。
4. 当前最合理的落地方式是：
   - 先补“真实协议号入库 + 主流程读取 + 订单快照”
   - 再通过环境保护确保 mock override 只留在测试场景
5. `2026-04-02` 当天按通联口径推进是合理的阶段性收敛；但截至 `2026-04-03`，支付渠道可能调整，因此应保留“渠道无关的协议号管理结构”，避免把主流程永久绑定到单一渠道。
6. 科技平台后续还会整体调整，因此今天继续深挖该链路会带来返工风险，保持 `HOLD` 是正确选择。

### 1.4 当前风险与结论

截至今天结束，剩余风险主要有以下几类：

1. 最终支付渠道的真实签约流程尚未接通，当前生产级 `payProtocolNo` 仍无正式来源。
2. `member_payment_protocol` 当前已具备承接结构，但状态回写策略仍需继续细化，例如：
   - `ACTIVE`
   - `EXPIRED`
   - `REVOKED`
3. 齐为回调链路 `M3.2` 仍受公网测试域名、暴露方式、白名单策略影响，尚未能排期启动。
4. 科技平台事项已 `HOLD`，需等待后续整体调整口径再统一恢复推进。

结论：

1. 今天已经把齐为问题从“联调现象”推进成“生产根因”。
2. 主流程第一轮改造已经具备落地基础，后续重点转向真实协议号来源接通、渠道最终确认和上线切换准备。

## 2. 面向研发团队的复盘

### 2.1 Codex 实际完成的工作

1. 回顾齐为当天回复，重新收敛 `515` 问题的真实根因。
2. 补齐 `abs.lending.notify` 成功 / 失败样例说明，并沉淀真实联调日志。
3. 产出“真实 `payProtocolNo` 来源接入整体设计方案（当日按通联假设收敛）”和配套开发任务清单。
4. 完成 `T1 + T2 + T3` 第一轮实现：
   - `member_payment_protocol`
   - `PaymentProtocolService`
   - `BenefitOrderServiceImpl` 主流程取值改造
5. 完成 `T4` 第一轮实现：
   - `QwPayProtocolOverrideGuard`
   - 新增测试 override 环境保护配置
6. 新增真实协议号回写入口：
   - `PaymentProtocolCallbackController`
   - `PaymentProtocolCallbackService`
7. 完成配套测试与静态检查。

### 2.2 关键实现点

#### 实现点一：把 `payProtocolNo` 从“临时联调参数”提升为“正式业务资产”

今天最重要的代码改造不是继续修齐为 client，而是明确：

1. 真实支付协议号属于用户资产
2. 应独立维护当前有效协议号
3. 订单侧只保留本单实际使用的快照

这一步的价值在于，后续不论是重新签约、协议失效还是历史审计，都有清晰的数据落点。

#### 实现点二：给主流程增加“真实来源优先、测试 override 兜底”的环境边界

今天通过主流程改造，已经把协议号取值规则明确成：

1. 优先读取真实支付协议号
2. 无真实协议号时，仅测试环境可按配置使用 override
3. 正式环境不得继续依赖 mock 或占位协议号

这一步的价值在于，既能继续保证测试环境联调推进，又不会把临时手段带进正式链路。

#### 实现点三：提前补齐真实协议号回写入口

虽然当前还没正式接通最终支付渠道，但今天已经把“真实协议号怎么入库”这一层先打出来：

1. 接收外部回写
2. 写入用户当前有效协议号
3. 为后续主流程读取提供稳定来源

这样后续接入通联时，不需要再临时倒逼改主流程结构。

### 2.3 今日遇到的问题与解决方式

#### 问题一：`515 会员卡未生效` 容易误判为齐为开卡问题

问题本质：

- 表面现象是卡未生效，真实根因是齐为首次扣款失败。

解决方式：

1. 结合齐为当天回复重新校正判断
2. 继续追问 `openFlag`、`abs.token.get` 最小入参、`status` 定义
3. 最终把问题明确收敛到 `payProtocolNo` 错误，而不是继续怀疑开卡状态

#### 问题二：当前项目没有真实 `payProtocolNo` 来源

问题本质：

- 齐为链路已可验证，但生产主流程缺少真实支付协议号来源。

解决方式：

1. 先用 mock 协议号继续完成测试环境联调
2. 同时补“真实协议号管理能力层”
3. 再新增回写入口，为后续支付渠道接入预留结构

#### 问题三：科技平台事项容易继续分散注意力

问题本质：

- 科技平台协议和代码后续还会整体调整，今天继续推进收益不高。

解决方式：

1. 明确记录科技平台整条线 `HOLD`
2. 今日不继续推进科技平台代码与联调细节
3. 把精力集中到齐为主链和真实协议号接入设计

### 2.4 今日验证结论

今天形成的主要验证结论如下：

1. 齐为侧已明确 `openFlag=0` 表示开卡成功。
2. 齐为侧已明确 `abs.token.get` 仍可只传 `uniqueId + partnerOrderNo`。
3. 齐为侧已明确 `status: 0=失败，1=成功`。
4. mock 协议号可用于继续测试环境联调，`abs.token.get` 已可返回 `token`。
5. 主流程已经具备“真实协议号优先 + 测试 override 兜底 + 订单快照留痕”的第一轮实现能力。

## 3. 今日认知提升

### 3.1 面向技术负责人

1. 当第三方联调已经跑通时，下一步最重要的是找出“生产唯一阻塞点”，而不是继续泛化修代码。
2. 临时联调参数如果不及时抽离为正式业务概念，后续会持续污染主流程设计。
3. 对仍未最终接通的外部系统，先把入口、落库和边界预留出来，比等待全部条件齐备后再集中改造更稳妥。

### 3.2 面向研发团队

1. 不要再把内部 `agreementNo` 和真实支付协议号混用。
2. 真实联调通了，不等于生产闭环已经具备；还要继续追真实资产来源。
3. 遇到外部链路还会整体调整时，及时 `HOLD` 某条线，本身就是有效推进。

## 4. 指导性建议

1. 明天优先补齐 `docs/third-part` 下的支付协议号来源对接与上线切换文档。
2. 明天继续细化支付协议号状态回写策略和主流程取值规则。
3. 明天继续跟进网关 / 运维，争取明确 `M3.2` 启动条件。
4. 明天继续把“通联”口径降级为“阶段性基线”，避免误认为最终渠道已拍板。
5. 科技平台事项继续保持 `HOLD`，等待后续整体调整后再恢复推进。
