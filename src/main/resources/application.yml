spring:
  application:
    name: nexusfin-equity
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:nexusfin_equity}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql

server:
  port: ${SERVER_PORT:8080}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      table-underline: true

logging:
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
    com.nexusfin.equity: ${LOG_LEVEL_APP:INFO}
    com.nexusfin.equity.thirdparty.yunka: ${LOG_LEVEL_YUNKA:INFO}
    com.nexusfin.equity.thirdparty.qw: ${LOG_LEVEL_QW:INFO}
    com.nexusfin.equity.thirdparty.techplatform: ${LOG_LEVEL_TECH_PLATFORM:INFO}

nexusfin:
  async-compensation:
    enabled: ${ASYNC_COMPENSATION_ENABLED:false}
    worker-enabled: ${ASYNC_COMPENSATION_WORKER_ENABLED:false}
    supervisor-enabled: ${ASYNC_COMPENSATION_SUPERVISOR_ENABLED:false}
    worker-poll-interval-ms: ${ASYNC_COMPENSATION_WORKER_POLL_INTERVAL_MS:3000}
    supervisor-interval-ms: ${ASYNC_COMPENSATION_SUPERVISOR_INTERVAL_MS:15000}
    worker-id: ${ASYNC_COMPENSATION_WORKER_ID:}
    owned-partitions: ${ASYNC_COMPENSATION_OWNED_PARTITIONS:}
    partition-count: ${ASYNC_COMPENSATION_PARTITION_COUNT:8}
    max-retry-count: ${ASYNC_COMPENSATION_MAX_RETRY_COUNT:5}
    lease-seconds: ${ASYNC_COMPENSATION_LEASE_SECONDS:60}
    retry-initial-delay-seconds: ${ASYNC_COMPENSATION_RETRY_INITIAL_DELAY_SECONDS:60}
    retry-max-delay-seconds: ${ASYNC_COMPENSATION_RETRY_MAX_DELAY_SECONDS:900}
    worker-heartbeat-timeout-seconds: ${ASYNC_COMPENSATION_WORKER_HEARTBEAT_TIMEOUT_SECONDS:120}
    # 示例：
    # enabled: true
    # worker-enabled: true
    # supervisor-enabled: true
    # worker-id: worker-a
    # owned-partitions: 0,1,2,3
  h5:
    loan:
      amount-range:
        min: 100
        max: 5000
        step: 100
        default-amount: 3000
      term-options:
        - label: 3期
          value: 3
      annual-rate: 0.18
      lender: XX商业银行
      receiving-account:
        bank-name: 招商银行
        last-four: "8648"
        account-id: acc_001
    benefits:
      product-code: HUXUAN_CARD
      activate:
        default-loan-amount: 300000
        supported-card-type: huixuan_card
        success-message: 惠选卡开通成功
      detail:
        card-name: 惠选卡
        price: 300
        total-saving: 448
        features:
          - title: 先享后付，无压力消费
            description: 支持多种消费场景，权益开通后可按规则使用。
          - title: 费用预估¥100/月，共3期
            description: 当前按 3 期固定展示，便于前端与后端先联通。
          - title: 匹配优质权益，不成功不收费
            description: 结合当前权益服务能力返回统一展示信息。
        categories:
          - name: 影音会员
            icon: tv
            benefits:
              - discount: 5折
                title: 每月4选1影视VIP会员
                description: 腾讯视频、优酷、爱奇艺、芒果TV任选
                validity: 有效期30天/次
                original-price: 30
                saving: 15
          - name: 出行服务
            icon: car
            benefits:
              - discount: 8折
                title: 出行礼包
                description: 覆盖打车和代驾等高频场景
                validity: 有效期30天/次
                original-price: 50
                saving: 10
        tips:
          - 权益服务费基于惠聚会员服务收取，请以页面展示说明为准。
          - 本费用为互联网增值服务费用，成功开通后不支持无理由撤销。
          - 您充分知晓服务内容后再进行开通操作。
        protocols:
          - name: 用户服务协议
            url: /protocols/user-service
          - name: 隐私条款声明
            url: /protocols/privacy
          - name: 委托扣款协议
            url: /protocols/debit
          - name: 权益服务协议
            url: /protocols/benefits
  business:
    default-provider-code: ${DEFAULT_BENEFIT_PROVIDER_CODE:QW}
  third-party:
    yunka:
      enabled: ${YUNKA_ENABLED:true}
      mode: ${YUNKA_MODE:MOCK}
      base-url: ${YUNKA_BASE_URL:https://yunka.local}
      gateway-path: ${YUNKA_GATEWAY_PATH:/api/gateway/proxy}
      connect-timeout-ms: ${YUNKA_CONNECT_TIMEOUT_MS:2000}
      read-timeout-ms: ${YUNKA_READ_TIMEOUT_MS:3000}
      paths:
        loan-calculate: ${YUNKA_LOAN_CALCULATE_PATH:/loan/trail}
        loan-query: ${YUNKA_LOAN_QUERY_PATH:/loan/query}
        loan-apply: ${YUNKA_LOAN_APPLY_PATH:/loan/apply}
        repay-trial: ${YUNKA_REPAY_TRIAL_PATH:/repay/trial}
        repay-apply: ${YUNKA_REPAY_APPLY_PATH:/repay/apply}
        repay-query: ${YUNKA_REPAY_QUERY_PATH:/repay/query}
        protocol-query: ${YUNKA_PROTOCOL_QUERY_PATH:/protocol/queryProtocolAggregationLink}
        user-token: ${YUNKA_USER_TOKEN_PATH:/user/token}
        user-query: ${YUNKA_USER_QUERY_PATH:/user/query}
        loan-repay-plan: ${YUNKA_LOAN_REPAY_PLAN_PATH:/loan/repayPlan}
        card-sms-send: ${YUNKA_CARD_SMS_SEND_PATH:/card/smsSend}
        card-sms-confirm: ${YUNKA_CARD_SMS_CONFIRM_PATH:/card/smsConfirm}
        card-user-cards: ${YUNKA_CARD_USER_CARDS_PATH:/card/userCards}
        credit-image-query: ${YUNKA_CREDIT_IMAGE_QUERY_PATH:/credit/image/query}
        benefit-sync: ${YUNKA_BENEFIT_SYNC_PATH:/benefit/sync}
    tech-platform:
      enabled: ${TECH_PLATFORM_API_ENABLED:true}
      mode: ${TECH_PLATFORM_API_MODE:MOCK}
      base-url: ${TECH_PLATFORM_API_BASE_URL:https://tech-platform.local}
      channel-id: ${TECH_PLATFORM_API_CHANNEL_ID:abs-app}
      version: ${TECH_PLATFORM_API_VERSION:1.0.0}
      sign-secret: ${TECH_PLATFORM_API_SIGN_SECRET:tech-platform-secret}
      sign-algorithm: ${TECH_PLATFORM_API_SIGN_ALGORITHM:HMAC_SHA256}
      aes-key-base64: ${TECH_PLATFORM_API_AES_KEY_BASE64:MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=}
      aes-algorithm: ${TECH_PLATFORM_API_AES_ALGORITHM:AES/ECB/PKCS5Padding}
      gcm-tag-bits: ${TECH_PLATFORM_API_GCM_TAG_BITS:128}
      iv-length-bytes: ${TECH_PLATFORM_API_IV_LENGTH_BYTES:12}
      connect-timeout-ms: ${TECH_PLATFORM_API_CONNECT_TIMEOUT_MS:2000}
      read-timeout-ms: ${TECH_PLATFORM_API_READ_TIMEOUT_MS:3000}
      paths:
        credit-status-notice: ${TECH_PLATFORM_API_CREDIT_STATUS_NOTICE_PATH:/guide/api/creditStatusNotice}
        loan-info-notice: ${TECH_PLATFORM_API_LOAN_INFO_NOTICE_PATH:/guide/api/loanInfoNotice}
        repay-info-notice: ${TECH_PLATFORM_API_REPAY_INFO_NOTICE_PATH:/guide/api/repayInfoNotice}
    qw:
      enabled: ${QW_ENABLED:true}
      mode: ${QW_MODE:MOCK}
      base-url: ${QW_BASE_URL:https://t-api.test.qweimobile.com}
      method-path: ${QW_METHOD_PATH:/api/abs/method}
      partner-no: ${QW_PARTNER_NO:abs}
      version: ${QW_VERSION:v1.0}
      sign-key: ${QW_SIGN_KEY:abs-secret-key}
      connect-timeout-ms: ${QW_CONNECT_TIMEOUT_MS:2000}
      read-timeout-ms: ${QW_READ_TIMEOUT_MS:3000}
      aes-key: ${QW_AES_KEY:0123456789abcdef}
      aes-key-base64: ${QW_AES_KEY_BASE64:MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=}
      aes-key-encoding: ${QW_AES_KEY_ENCODING:RAW}
      aes-algorithm: ${QW_AES_ALGORITHM:AES/ECB/PKCS5Padding}
      ciphertext-encoding: ${QW_CIPHERTEXT_ENCODING:HEX}
      gcm-tag-bits: ${QW_GCM_TAG_BITS:128}
      iv-length-bytes: ${QW_IV_LENGTH_BYTES:12}
      default-pay-protocol-prefix: ${QW_DEFAULT_PAY_PROTOCOL_PREFIX:proto-}
      # 仅联调环境使用；生产环境禁止依赖该 mock 协议号。
      member-sync-pay-protocol-no-override: ${QW_MEMBER_SYNC_PAY_PROTOCOL_NO_OVERRIDE:}
      # 仅在允许的联调 profile 下可开启；误开将于启动期直接失败。
      allow-member-sync-pay-protocol-no-override: ${QW_ALLOW_MEMBER_SYNC_PAY_PROTOCOL_NO_OVERRIDE:false}
      member-sync-pay-protocol-no-override-allowed-profiles: ${QW_MEMBER_SYNC_PAY_PROTOCOL_NO_OVERRIDE_ALLOWED_PROFILES:test,mysql-it,local}
      mock-exercise-base-url: ${QW_MOCK_EXERCISE_BASE_URL:https://mock-qw.local/exercise}
      direct:
        base-url: ${QW_DIRECT_BASE_URL:https://tlt-test.allinpay.com}
        process-path: ${QW_DIRECT_PROCESS_PATH:/aipg/ProcessServlet}
        merchant-id: ${QW_DIRECT_MERCHANT_ID:}
        user-name: ${QW_DIRECT_USER_NAME:}
        user-password: ${QW_DIRECT_USER_PASSWORD:}
        channel-code: ${QW_DIRECT_CHANNEL_CODE:}
        account-no: ${QW_DIRECT_ACCOUNT_NO:}
        pkcs12-path: ${QW_DIRECT_PKCS12_PATH:}
        pkcs12-password: ${QW_DIRECT_PKCS12_PASSWORD:}
        verify-cert-path: ${QW_DIRECT_VERIFY_CERT_PATH:}
        connect-timeout-ms: ${QW_DIRECT_CONNECT_TIMEOUT_MS:3000}
        read-timeout-ms: ${QW_DIRECT_READ_TIMEOUT_MS:5000}
        member-sync-service-code: ${QW_DIRECT_MEMBER_SYNC_SERVICE_CODE:}
        exercise-url-service-code: ${QW_DIRECT_EXERCISE_URL_SERVICE_CODE:}
        lending-notify-service-code: ${QW_DIRECT_LENDING_NOTIFY_SERVICE_CODE:}
  crypto:
    dek:
      key-id: ${NEXUSFIN_CRYPTO_DEK_KEY_ID:DEK_USER_PROFILE}
      key-version: ${NEXUSFIN_CRYPTO_DEK_KEY_VERSION:1}
      algorithm: AES/GCM/NoPadding
      purpose: encrypt_user_sensitive_fields
      status: ACTIVE
      plaintext-key-base64: ${NEXUSFIN_CRYPTO_DEK_PLAINTEXT_BASE64:MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=}
    inbound:
      default-mode: ${NEXUSFIN_CRYPTO_INBOUND_DEFAULT_MODE:PLAIN}
      channel-modes:
        KJ: ${NEXUSFIN_CRYPTO_INBOUND_KJ_MODE:PLAIN}
  signature:
    app-id: ${NEXUSFIN_APP_ID:abs-app}
    secret: ${NEXUSFIN_APP_SECRET:abs-secret-key}
    max-skew-seconds: 300
    protected-path-prefixes:
      - /api/callbacks
  auth:
    tech-platform-base-url: ${TECH_PLATFORM_BASE_URL:http://localhost:18080}
    tech-platform-user-me-path: ${TECH_PLATFORM_USER_ME_PATH:/api/users/me}
    connect-timeout-ms: ${TECH_PLATFORM_CONNECT_TIMEOUT_MS:2000}
    read-timeout-ms: ${TECH_PLATFORM_READ_TIMEOUT_MS:3000}
    retry-max-attempts: ${TECH_PLATFORM_RETRY_MAX_ATTEMPTS:2}
    default-channel-code: ${AUTH_DEFAULT_CHANNEL_CODE:KJ}
    default-redirect-url: ${AUTH_DEFAULT_REDIRECT_URL:/equity/index}
    redirect-whitelist:
      - /equity/
      - https://equity.nexusfin.com/
    protected-path-prefixes:
      - /api/equity/products
      - /api/equity/orders
      - /api/equity/exercise-url
      - /api/bank-card
      - /api/loan
      - /api/benefits
      - /api/repayment
      - /api/users/me
    excluded-path-prefixes:
      - /api/equity/health
      - /api/auth/sso-callback
      - /api/auth/joint-login
      - /api/callbacks
    jwt:
      issuer: ${AUTH_JWT_ISSUER:nexusfin-equity}
      secret: ${AUTH_JWT_SECRET:equity-jwt-secret-key-equity-jwt-secret}
      ttl-seconds: ${AUTH_JWT_TTL_SECONDS:7200}
      cookie-name: ${AUTH_COOKIE_NAME:NEXUSFIN_AUTH}
      cookie-path: /
      cookie-secure: ${AUTH_COOKIE_SECURE:true}
      cookie-http-only: true
      cookie-same-site: Lax
